# 函数地图 4：运行脚本与 MCP

> 扫描范围：`scripts/**/*.py` 生产脚本；排除测试文件。
> 扫描方式：Python `ast` 扫描同步/异步函数、类方法、嵌套函数与 MCP 工具装饰函数；`physics_vault_mcp_server.py` 以 `utf-8-sig` 解析 BOM。

## 扫描结果

| 项目 | 结果 |
| --- | ---: |
| 生产 Python 文件 | 14 |
| 函数、异步函数、类方法、嵌套函数 | 283 |
| 未解析文件 | 0 |

副作用标记覆盖 CLI 参数与标准输出、MCP 工具注册、文件读写、SQLite、HTTP、子进程、环境变量与异步运行时。上下游为 AST 静态近似：模块级函数的上游为 CLI/MCP/外部调用方，嵌套函数的上游为其外围函数；下游列出可见调用。

## scripts/dev/mock_llm_mcp.py

共 2 个函数记录

| 标识 | 签名 / 类别 | 职责 | 输入 ⇒ 输出 | 副作用 | 上游 / 下游 | 归属 | 优化提示 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| <code>scripts/dev/mock_llm_mcp.py:7::_response</code> | _response(task_id: str, result: dict[str, object]) -> dict[str, object]<br>function | Performs a local runtime calculation or state transition. | _response(task_id: str, result: dict[str, object]) -> dict[str, object] | none found statically | CLI/MCP/external caller<br>downstream: no direct call found | runtime / development mock | keep |
| <code>scripts/dev/mock_llm_mcp.py:19::main</code> | main() -> int<br>function | Coordinates sys.stdin.read, json.loads, payload.get, sys.stdout.write, json.dumps, request_payload.get. | main() -> int | none found statically | CLI/MCP/external caller<br>downstream: sys.stdin.read, json.loads, payload.get, sys.stdout.write, json.dumps, request_payload.get | runtime / development mock | keep |
## scripts/dev/mock_vl_mcp.py

共 2 个函数记录

| 标识 | 签名 / 类别 | 职责 | 输入 ⇒ 输出 | 副作用 | 上游 / 下游 | 归属 | 优化提示 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| <code>scripts/dev/mock_vl_mcp.py:9::_response</code> | _response(task_id: str, result: dict[str, Any]) -> dict[str, Any]<br>function | Performs a local runtime calculation or state transition. | _response(task_id: str, result: dict[str, Any]) -> dict[str, Any] | none found statically | CLI/MCP/external caller<br>downstream: no direct call found | runtime / development mock | keep |
| <code>scripts/dev/mock_vl_mcp.py:21::main</code> | main() -> int<br>function | Coordinates sys.stdin.read, json.loads, payload.get, sys.stdout.write, request_payload['document'].get, json.dumps. | main() -> int | file I/O | CLI/MCP/external caller<br>downstream: sys.stdin.read, json.loads, payload.get, sys.stdout.write, request_payload['document'].get, json.dumps | runtime / development mock | keep |
## scripts/maintenance/evaluate_method_retrieval.py

共 5 个函数记录

| 标识 | 签名 / 类别 | 职责 | 输入 ⇒ 输出 | 副作用 | 上游 / 下游 | 归属 | 优化提示 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| <code>scripts/maintenance/evaluate_method_retrieval.py:80::_load_mcp_module</code> | _load_mcp_module() -> Any<br>function | Coordinates importlib.util.spec_from_file_location, importlib.util.module_from_spec, spec.loader.exec_module, RuntimeError. | _load_mcp_module() -> Any | none found statically | CLI/MCP/external caller<br>downstream: importlib.util.spec_from_file_location, importlib.util.module_from_spec, spec.loader.exec_module, RuntimeError | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/evaluate_method_retrieval.py:90::_percentile</code> | _percentile(values: list[float], percentile: float) -> float<br>function | Coordinates sorted, max, math.ceil, len. | _percentile(values: list[float], percentile: float) -> float | none found statically | CLI/MCP/external caller<br>downstream: sorted, max, math.ceil, len | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/evaluate_method_retrieval.py:97::evaluate</code> | evaluate(limit: int) -> dict[str, Any]<br>function | Coordinates _load_mcp_module, module.search_method_questions, load_method_feedback_constraints, method_feature_index_health, time.perf_counter, latencies.append. | evaluate(limit: int) -> dict[str, Any] | none found statically | CLI/MCP/external caller<br>downstream: _load_mcp_module, module.search_method_questions, load_method_feedback_constraints, method_feature_index_health, time.perf_counter, latencies.append | runtime / maintenance CLI | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/maintenance/evaluate_method_retrieval.py:193::_check</code> | _check(report: dict[str, Any]) -> list[str]<br>function | Coordinates thresholds.items, int, failures.append, float, metrics.get. | _check(report: dict[str, Any]) -> list[str] | none found statically | CLI/MCP/external caller<br>downstream: thresholds.items, int, failures.append, float, metrics.get | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/evaluate_method_retrieval.py:225::main</code> | main() -> int<br>function | Coordinates argparse.ArgumentParser, parser.add_argument, parser.parse_args, evaluate, args.output.parent.mkdir, args.output.write_text. | main() -> int | file I/O?CLI/stdout | CLI/MCP/external caller<br>downstream: argparse.ArgumentParser, parser.add_argument, parser.parse_args, evaluate, args.output.parent.mkdir, args.output.write_text | runtime / maintenance CLI | keep |
## scripts/maintenance/evaluate_question_retrieval.py

共 8 个函数记录

| 标识 | 签名 / 类别 | 职责 | 输入 ⇒ 输出 | 副作用 | 上游 / 下游 | 归属 | 优化提示 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| <code>scripts/maintenance/evaluate_question_retrieval.py:164::_normalise</code> | _normalise(text: Any) -> str<br>function | Coordinates ''.join, character.casefold, str, character.isalnum. | _normalise(text: Any) -> str | none found statically | CLI/MCP/external caller<br>downstream: ''.join, character.casefold, str, character.isalnum | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/evaluate_question_retrieval.py:168::_topic_ids</code> | _topic_ids(row: dict[str, Any]) -> set[str]<br>function | Coordinates str, point.get, row.get, isinstance. | _topic_ids(row: dict[str, Any]) -> set[str] | none found statically | CLI/MCP/external caller<br>downstream: str, point.get, row.get, isinstance | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/evaluate_question_retrieval.py:176::_content</code> | _content(row: dict[str, Any]) -> str<br>function | Coordinates ' '.join, _normalise, str, row.get, isinstance, point.get. | _content(row: dict[str, Any]) -> str | none found statically | CLI/MCP/external caller<br>downstream: ' '.join, _normalise, str, row.get, isinstance, point.get | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/evaluate_question_retrieval.py:194::_is_relevant</code> | _is_relevant(case: BenchmarkCase, row: dict[str, Any]) -> tuple[bool, str]<br>function | Coordinates set, _content, _topic_ids, all, _normalise. | _is_relevant(case: BenchmarkCase, row: dict[str, Any]) -> tuple[bool, str] | none found statically | CLI/MCP/external caller<br>downstream: set, _content, _topic_ids, all, _normalise | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/evaluate_question_retrieval.py:205::_percentile</code> | _percentile(values: list[float], percentile: float) -> float<br>function | Coordinates sorted, max, math.ceil, len. | _percentile(values: list[float], percentile: float) -> float | none found statically | CLI/MCP/external caller<br>downstream: sorted, max, math.ceil, len | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/evaluate_question_retrieval.py:213::evaluate</code> | evaluate(mode: SearchMode) -> dict[str, Any]<br>function | Coordinates QuestionSearchRepository, QuestionSearchService, os.getenv('PHYSICS_VAULT_EMBEDDING_PROVIDER', 'openai').strip().lower, os.getenv('PHYSICS_VAULT_EMBEDDING_MODEL', 'text-embedding-3-small').strip, resolve_embedding_model, os.getenv('PHYSICS_VAULT_EMBEDDING_MODEL_VERSION', '').strip. | evaluate(mode: SearchMode) -> dict[str, Any] | CLI/stdout | CLI/MCP/external caller<br>downstream: QuestionSearchRepository, QuestionSearchService, os.getenv('PHYSICS_VAULT_EMBEDDING_PROVIDER', 'openai').strip().lower, os.getenv('PHYSICS_VAULT_EMBEDDING_MODEL', 'text-embedding-3-small').strip, resolve_embedding_model, os.getenv('PHYSICS_VAULT_EMBEDDING_MODEL_VERSION', '').strip | runtime / maintenance CLI | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/maintenance/evaluate_question_retrieval.py:315::_passes_thresholds</code> | _passes_thresholds(report: dict[str, Any]) -> tuple[bool, list[str]]<br>function | Coordinates thresholds.items, float, failures.append. | _passes_thresholds(report: dict[str, Any]) -> tuple[bool, list[str]] | none found statically | CLI/MCP/external caller<br>downstream: thresholds.items, float, failures.append | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/evaluate_question_retrieval.py:335::main</code> | main() -> int<br>function | Coordinates argparse.ArgumentParser, parser.add_argument, parser.parse_args, evaluate, args.output.parent.mkdir, args.output.write_text. | main() -> int | file I/O?CLI/stdout | CLI/MCP/external caller<br>downstream: argparse.ArgumentParser, parser.add_argument, parser.parse_args, evaluate, args.output.parent.mkdir, args.output.write_text | runtime / maintenance CLI | keep |
## scripts/maintenance/fix_legacy_encoding.py

共 9 个函数记录

| 标识 | 签名 / 类别 | 职责 | 输入 ⇒ 输出 | 副作用 | 上游 / 下游 | 归属 | 优化提示 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| <code>scripts/maintenance/fix_legacy_encoding.py:74::is_clean_chinese</code> | is_clean_chinese(text: str) -> bool<br>function | Coordinates text.count, sum, len, ord. | is_clean_chinese(text: str) -> bool | none found statically | CLI/MCP/external caller<br>downstream: text.count, sum, len, ord | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/fix_legacy_encoding.py:92::try_recover</code> | try_recover(raw: bytes) -> str \| None<br>function | Coordinates raw.decode, is_clean_chinese, as_latin1.encode, re_encoded.decode, as_latin1_2.encode, re_encoded_2.decode. | try_recover(raw: bytes) -> str \| None | none found statically | CLI/MCP/external caller<br>downstream: raw.decode, is_clean_chinese, as_latin1.encode, re_encoded.decode, as_latin1_2.encode, re_encoded_2.decode | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/fix_legacy_encoding.py:132::_has_cjk</code> | _has_cjk(text: str) -> bool<br>function | Coordinates any. | _has_cjk(text: str) -> bool | none found statically | CLI/MCP/external caller<br>downstream: any | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/fix_legacy_encoding.py:137::fix_json_field</code> | fix_json_field(raw: bytes) -> bytes \| None<br>function | Coordinates isinstance, raw.decode, json.loads, json.dumps(data, ensure_ascii=False).encode, try_recover, item.items. | fix_json_field(raw: bytes) -> bytes \| None | none found statically | CLI/MCP/external caller<br>downstream: isinstance, raw.decode, json.loads, json.dumps(data, ensure_ascii=False).encode, try_recover, item.items | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/fix_legacy_encoding.py:172::fix_database</code> | fix_database(db_path: Path, dry_run: bool) -> dict[str, int]<br>function | Coordinates sqlite3.connect, TEXT_COLUMNS.items, conn.close, db_path.exists, FileNotFoundError, str. | fix_database(db_path: Path, dry_run: bool) -> dict[str, int] | SQLite | CLI/MCP/external caller<br>downstream: sqlite3.connect, TEXT_COLUMNS.items, conn.close, db_path.exists, FileNotFoundError, str | runtime / maintenance CLI | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/maintenance/fix_legacy_encoding.py:248::_existing_columns</code> | _existing_columns(conn: sqlite3.Connection, table: str, candidates: list[str]) -> list[str]<br>function | Coordinates conn.execute(f'PRAGMA table_info({table})').fetchall, conn.execute. | _existing_columns(conn: sqlite3.Connection, table: str, candidates: list[str]) -> list[str] | SQLite | CLI/MCP/external caller<br>downstream: conn.execute(f'PRAGMA table_info({table})').fetchall, conn.execute | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/fix_legacy_encoding.py:260::_primary_key</code> | _primary_key(conn: sqlite3.Connection, table: str) -> str \| None<br>function | Coordinates conn.execute(f'PRAGMA table_info({table})').fetchall, conn.execute. | _primary_key(conn: sqlite3.Connection, table: str) -> str \| None | SQLite | CLI/MCP/external caller<br>downstream: conn.execute(f'PRAGMA table_info({table})').fetchall, conn.execute | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/fix_legacy_encoding.py:274::build_parser</code> | build_parser() -> argparse.ArgumentParser<br>function | Coordinates argparse.ArgumentParser, p.add_argument, str. | build_parser() -> argparse.ArgumentParser | CLI/stdout | CLI/MCP/external caller<br>downstream: argparse.ArgumentParser, p.add_argument, str | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/fix_legacy_encoding.py:291::main</code> | main() -> int<br>function | Coordinates build_parser().parse_args, Path, print, fix_database, build_parser. | main() -> int | file I/O?CLI/stdout | CLI/MCP/external caller<br>downstream: build_parser().parse_args, Path, print, fix_database, build_parser | runtime / maintenance CLI | keep |
## scripts/maintenance/load_legacy_samples.py

共 6 个函数记录

| 标识 | 签名 / 类别 | 职责 | 输入 ⇒ 输出 | 副作用 | 上游 / 下游 | 归属 | 优化提示 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| <code>scripts/maintenance/load_legacy_samples.py:31::find_old_db</code> | find_old_db() -> Path<br>function | Coordinates list, WORKSPACE_ROOT.glob, FileNotFoundError. | find_old_db() -> Path | SQLite | CLI/MCP/external caller<br>downstream: list, WORKSPACE_ROOT.glob, FileNotFoundError | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/load_legacy_samples.py:38::find_old_image_dir</code> | find_old_image_dir() -> Path<br>function | Coordinates list, WORKSPACE_ROOT.glob, FileNotFoundError. | find_old_image_dir() -> Path | none found statically | CLI/MCP/external caller<br>downstream: list, WORKSPACE_ROOT.glob, FileNotFoundError | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/load_legacy_samples.py:45::review_status_from_status</code> | review_status_from_status(status: str \| None) -> str<br>function | Performs a local runtime calculation or state transition. | review_status_from_status(status: str \| None) -> str | none found statically | CLI/MCP/external caller<br>downstream: no direct call found | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/load_legacy_samples.py:57::json_text</code> | json_text(value) -> str<br>function | Coordinates json.dumps. | json_text(value) -> str | none found statically | CLI/MCP/external caller<br>downstream: json.dumps | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/load_legacy_samples.py:61::load_rows</code> | load_rows(conn: sqlite3.Connection, sql: str, params) -> list[sqlite3.Row]<br>function | Coordinates conn.execute(sql, params).fetchall, conn.execute. | load_rows(conn: sqlite3.Connection, sql: str, params) -> list[sqlite3.Row] | SQLite | CLI/MCP/external caller<br>downstream: conn.execute(sql, params).fetchall, conn.execute | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/load_legacy_samples.py:66::main</code> | main() -> int<br>function | Coordinates find_old_db, find_old_image_dir, TARGET_ASSETS_DIR.mkdir, fix_database, print, sqlite3.connect. | main() -> int | file I/O?SQLite?CLI/stdout | CLI/MCP/external caller<br>downstream: find_old_db, find_old_image_dir, TARGET_ASSETS_DIR.mkdir, fix_database, print, sqlite3.connect | runtime / maintenance CLI | split orchestration/I-O boundary if it grows; otherwise keep |
## scripts/maintenance/prepare_obsidian_review_import.py

共 10 个函数记录

| 标识 | 签名 / 类别 | 职责 | 输入 ⇒ 输出 | 副作用 | 上游 / 下游 | 归属 | 优化提示 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| <code>scripts/maintenance/prepare_obsidian_review_import.py:35::_read_front_matter</code> | _read_front_matter(text: str, path: Path) -> tuple[dict[str, Any], str]<br>function | Coordinates re.match, text.startswith, ValueError, yaml.safe_load, isinstance, match.group. | _read_front_matter(text: str, path: Path) -> tuple[dict[str, Any], str] | none found statically | CLI/MCP/external caller<br>downstream: re.match, text.startswith, ValueError, yaml.safe_load, isinstance, match.group | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/prepare_obsidian_review_import.py:47::_clean_line</code> | _clean_line(value: str) -> str<br>function | Coordinates re.sub('\\s+', ' ', value).strip, re.sub. | _clean_line(value: str) -> str | none found statically | CLI/MCP/external caller<br>downstream: re.sub('\\s+', ' ', value).strip, re.sub | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/prepare_obsidian_review_import.py:51::_section_value</code> | _section_value(body: str, name: str) -> str<br>function | Coordinates re.escape, '\|'.join, re.search, found.group(1).strip, found.group. | _section_value(body: str, name: str) -> str | none found statically | CLI/MCP/external caller<br>downstream: re.escape, '\|'.join, re.search, found.group(1).strip, found.group | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/prepare_obsidian_review_import.py:58::_remove_heading_noise</code> | _remove_heading_noise(text: str) -> str<br>function | Coordinates text.splitlines, '\n'.join(lines).strip, line.strip, lines.append, compact.startswith, compact.endswith. | _remove_heading_noise(text: str) -> str | none found statically | CLI/MCP/external caller<br>downstream: text.splitlines, '\n'.join(lines).strip, line.strip, lines.append, compact.startswith, compact.endswith | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/prepare_obsidian_review_import.py:70::_extract_options</code> | _extract_options(stem: str) -> tuple[str, list[dict[str, str]]]<br>function | Coordinates stem.splitlines, re.sub('^\\s*>\\s?', '', line).strip, re.match, '\n'.join(retained).strip, options.append, retained.append. | _extract_options(stem: str) -> tuple[str, list[dict[str, str]]] | none found statically | CLI/MCP/external caller<br>downstream: stem.splitlines, re.sub('^\\s*>\\s?', '', line).strip, re.match, '\n'.join(retained).strip, options.append, retained.append | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/prepare_obsidian_review_import.py:83::_images_from_metadata</code> | _images_from_metadata(metadata: dict[str, Any], question_id: str) -> list[dict[str, str]]<br>function | Coordinates metadata.get, enumerate, isinstance, str(item.get('filename') or '').strip, figures.append, str(raw_filename).strip. | _images_from_metadata(metadata: dict[str, Any], question_id: str) -> list[dict[str, str]] | none found statically | CLI/MCP/external caller<br>downstream: metadata.get, enumerate, isinstance, str(item.get('filename') or '').strip, figures.append, str(raw_filename).strip | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/prepare_obsidian_review_import.py:121::_prepare_question</code> | _prepare_question(path: Path) -> PreparedQuestion<br>function | Coordinates _read_front_matter, str(metadata.get('question_id') or metadata.get('id') or '').strip, _remove_heading_noise, _images_from_metadata, _section_value, re.split('\\n【(?:答案\|知识点\|详解\|解析)】', question_body, maxsplit=1)[0].strip. | _prepare_question(path: Path) -> PreparedQuestion | file I/O | CLI/MCP/external caller<br>downstream: _read_front_matter, str(metadata.get('question_id') or metadata.get('id') or '').strip, _remove_heading_noise, _images_from_metadata, _section_value, re.split('\\n【(?:答案\|知识点\|详解\|解析)】', question_body, maxsplit=1)[0].strip | runtime / maintenance CLI | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/maintenance/prepare_obsidian_review_import.py:180::_sha256</code> | _sha256(path: Path) -> str<br>function | Coordinates hashlib.sha256, digest.hexdigest, path.open, iter, digest.update, handle.read. | _sha256(path: Path) -> str | file I/O | CLI/MCP/external caller<br>downstream: hashlib.sha256, digest.hexdigest, path.open, iter, digest.update, handle.read | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/prepare_obsidian_review_import.py:188::_copy_assets</code> | _copy_assets(source_dir: Path, target_dir: Path) -> tuple[int, int]<br>function | Coordinates target_dir.mkdir, sorted, target.exists, shutil.copy2, ValueError, source_dir.iterdir. | _copy_assets(source_dir: Path, target_dir: Path) -> tuple[int, int] | file I/O | CLI/MCP/external caller<br>downstream: target_dir.mkdir, sorted, target.exists, shutil.copy2, ValueError, source_dir.iterdir | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/prepare_obsidian_review_import.py:203::main</code> | main() -> int<br>function | Coordinates argparse.ArgumentParser, parser.add_argument, parser.parse_args, sorted, args.output_dir.mkdir, chunks_dir.mkdir. | main() -> int | file I/O?CLI/stdout | CLI/MCP/external caller<br>downstream: argparse.ArgumentParser, parser.add_argument, parser.parse_args, sorted, args.output_dir.mkdir, chunks_dir.mkdir | runtime / maintenance CLI | split orchestration/I-O boundary if it grows; otherwise keep |
## scripts/maintenance/reset_standard_db.py

共 2 个函数记录

| 标识 | 签名 / 类别 | 职责 | 输入 ⇒ 输出 | 副作用 | 上游 / 下游 | 归属 | 优化提示 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| <code>scripts/maintenance/reset_standard_db.py:17::build_parser</code> | build_parser() -> argparse.ArgumentParser<br>function | Coordinates argparse.ArgumentParser, parser.add_argument, str, default_db_path. | build_parser() -> argparse.ArgumentParser | SQLite?CLI/stdout | CLI/MCP/external caller<br>downstream: argparse.ArgumentParser, parser.add_argument, str, default_db_path | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/reset_standard_db.py:39::main</code> | main() -> int<br>function | Coordinates build_parser().parse_args, Path, reset_database, print, initialize_database, build_parser. | main() -> int | file I/O?CLI/stdout | CLI/MCP/external caller<br>downstream: build_parser().parse_args, Path, reset_database, print, initialize_database, build_parser | runtime / maintenance CLI | keep |
## scripts/maintenance/seed_demo_data.py

共 14 个函数记录

| 标识 | 签名 / 类别 | 职责 | 输入 ⇒ 输出 | 副作用 | 上游 / 下游 | 归属 | 优化提示 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| <code>scripts/maintenance/seed_demo_data.py:245::resolve_db_path</code> | resolve_db_path(raw: str \| None) -> Path<br>function | Coordinates os.getenv, Path, str. | resolve_db_path(raw: str \| None) -> Path | file I/O | CLI/MCP/external caller<br>downstream: os.getenv, Path, str | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_demo_data.py:251::json_text</code> | json_text(value: Any) -> str<br>function | Coordinates json.dumps. | json_text(value: Any) -> str | none found statically | CLI/MCP/external caller<br>downstream: json.dumps | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_demo_data.py:255::ensure_schema</code> | ensure_schema(conn: sqlite3.Connection) -> None<br>function | Coordinates conn.execute. | ensure_schema(conn: sqlite3.Connection) -> None | SQLite | CLI/MCP/external caller<br>downstream: conn.execute | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_demo_data.py:282::ensure_question_columns</code> | ensure_question_columns(conn: sqlite3.Connection) -> None<br>function | Coordinates conn.execute, conn.execute('PRAGMA table_info(questions)').fetchall. | ensure_question_columns(conn: sqlite3.Connection) -> None | SQLite | CLI/MCP/external caller<br>downstream: conn.execute, conn.execute('PRAGMA table_info(questions)').fetchall | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_demo_data.py:298::upsert_papers</code> | upsert_papers(conn: sqlite3.Connection) -> None<br>function | Coordinates conn.execute. | upsert_papers(conn: sqlite3.Connection) -> None | SQLite | CLI/MCP/external caller<br>downstream: conn.execute | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_demo_data.py:333::upsert_knowledge_points</code> | upsert_knowledge_points(conn: sqlite3.Connection) -> None<br>function | Coordinates conn.execute. | upsert_knowledge_points(conn: sqlite3.Connection) -> None | SQLite | CLI/MCP/external caller<br>downstream: conn.execute | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_demo_data.py:365::upsert_questions</code> | upsert_questions(conn: sqlite3.Connection) -> None<br>function | Coordinates conn.execute. | upsert_questions(conn: sqlite3.Connection) -> None | SQLite | CLI/MCP/external caller<br>downstream: conn.execute | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_demo_data.py:410::upsert_question_text_index</code> | upsert_question_text_index(conn: sqlite3.Connection) -> None<br>function | Coordinates conn.execute, json_text. | upsert_question_text_index(conn: sqlite3.Connection) -> None | SQLite | CLI/MCP/external caller<br>downstream: conn.execute, json_text | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_demo_data.py:455::upsert_question_sources</code> | upsert_question_sources(conn: sqlite3.Connection) -> None<br>function | Coordinates conn.execute. | upsert_question_sources(conn: sqlite3.Connection) -> None | SQLite | CLI/MCP/external caller<br>downstream: conn.execute | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_demo_data.py:485::upsert_question_kps</code> | upsert_question_kps(conn: sqlite3.Connection) -> None<br>function | Coordinates conn.execute. | upsert_question_kps(conn: sqlite3.Connection) -> None | SQLite | CLI/MCP/external caller<br>downstream: conn.execute | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_demo_data.py:510::upsert_favorites</code> | upsert_favorites(conn: sqlite3.Connection) -> None<br>function | Coordinates conn.execute. | upsert_favorites(conn: sqlite3.Connection) -> None | SQLite | CLI/MCP/external caller<br>downstream: conn.execute | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_demo_data.py:537::reset_demo_rows</code> | reset_demo_rows(conn: sqlite3.Connection) -> None<br>function | Coordinates ','.join, conn.execute. | reset_demo_rows(conn: sqlite3.Connection) -> None | SQLite | CLI/MCP/external caller<br>downstream: ','.join, conn.execute | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_demo_data.py:568::seed</code> | seed(db_path: Path, reset: bool) -> None<br>function | Coordinates db_path.exists, FileNotFoundError, closing, conn.execute, ensure_schema, ensure_question_columns. | seed(db_path: Path, reset: bool) -> None | SQLite | CLI/MCP/external caller<br>downstream: db_path.exists, FileNotFoundError, closing, conn.execute, ensure_schema, ensure_question_columns | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_demo_data.py:592::main</code> | main() -> None<br>function | Coordinates argparse.ArgumentParser, parser.add_argument, parser.parse_args, resolve_db_path, seed, print. | main() -> None | SQLite?CLI/stdout | CLI/MCP/external caller<br>downstream: argparse.ArgumentParser, parser.add_argument, parser.parse_args, resolve_db_path, seed, print | runtime / maintenance CLI | keep |
## scripts/maintenance/seed_metadata_catalog.py

共 3 个函数记录

| 标识 | 签名 / 类别 | 职责 | 输入 ⇒ 输出 | 副作用 | 上游 / 下游 | 归属 | 优化提示 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| <code>scripts/maintenance/seed_metadata_catalog.py:124::knowledge_points</code> | knowledge_points() -> list[dict[str, Any]]<br>function | Performs a local runtime calculation or state transition. | knowledge_points() -> list[dict[str, Any]] | none found statically | CLI/MCP/external caller<br>downstream: no direct call found | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_metadata_catalog.py:140::seed</code> | seed(db_path: Path) -> dict[str, int]<br>function | Coordinates initialize_database, MetadataManagementService(db_path).create_knowledge_points, knowledge_points, connect_db, TAG_CATALOG.items, conn.commit. | seed(db_path: Path) -> dict[str, int] | SQLite | CLI/MCP/external caller<br>downstream: initialize_database, MetadataManagementService(db_path).create_knowledge_points, knowledge_points, connect_db, TAG_CATALOG.items, conn.commit | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_metadata_catalog.py:166::main</code> | main() -> None<br>function | Coordinates argparse.ArgumentParser, parser.add_argument, parser.parse_args, seed, print, result.items. | main() -> None | CLI/stdout | CLI/MCP/external caller<br>downstream: argparse.ArgumentParser, parser.add_argument, parser.parse_args, seed, print, result.items | runtime / maintenance CLI | keep |
## scripts/maintenance/seed_standard_dataset.py

共 17 个函数记录

| 标识 | 签名 / 类别 | 职责 | 输入 ⇒ 输出 | 副作用 | 上游 / 下游 | 归属 | 优化提示 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| <code>scripts/maintenance/seed_standard_dataset.py:380::compact_text</code> | compact_text(value: str) -> str<br>function | Coordinates ' '.join, value.split. | compact_text(value: str) -> str | none found statically | CLI/MCP/external caller<br>downstream: ' '.join, value.split | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_standard_dataset.py:384::json_text</code> | json_text(value: Any) -> str<br>function | Coordinates json.dumps. | json_text(value: Any) -> str | none found statically | CLI/MCP/external caller<br>downstream: json.dumps | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_standard_dataset.py:388::content_hash</code> | content_hash(question: dict[str, Any]) -> str<br>function | Coordinates '\n'.join, hashlib.sha256(source.encode('utf-8')).hexdigest, hashlib.sha256, source.encode. | content_hash(question: dict[str, Any]) -> str | none found statically | CLI/MCP/external caller<br>downstream: '\n'.join, hashlib.sha256(source.encode('utf-8')).hexdigest, hashlib.sha256, source.encode | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_standard_dataset.py:400::kp_by_id</code> | kp_by_id() -> dict[str, dict[str, Any]]<br>function | Performs a local runtime calculation or state transition. | kp_by_id() -> dict[str, dict[str, Any]] | none found statically | CLI/MCP/external caller<br>downstream: no direct call found | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_standard_dataset.py:404::clear_domain_data</code> | clear_domain_data(conn) -> None<br>function | Coordinates conn.execute. | clear_domain_data(conn) -> None | SQLite | CLI/MCP/external caller<br>downstream: conn.execute | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_standard_dataset.py:430::seed_import_batch</code> | seed_import_batch(conn) -> None<br>function | Coordinates conn.execute, len. | seed_import_batch(conn) -> None | SQLite | CLI/MCP/external caller<br>downstream: conn.execute, len | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_standard_dataset.py:452::seed_papers</code> | seed_papers(conn) -> None<br>function | Coordinates conn.execute. | seed_papers(conn) -> None | SQLite | CLI/MCP/external caller<br>downstream: conn.execute | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_standard_dataset.py:474::seed_knowledge_points</code> | seed_knowledge_points(conn) -> None<br>function | Coordinates conn.execute. | seed_knowledge_points(conn) -> None | SQLite | CLI/MCP/external caller<br>downstream: conn.execute | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_standard_dataset.py:496::seed_questions</code> | seed_questions(conn) -> None<br>function | Coordinates kp_by_id, conn.execute, content_hash, compact_text, json_text. | seed_questions(conn) -> None | SQLite | CLI/MCP/external caller<br>downstream: kp_by_id, conn.execute, content_hash, compact_text, json_text | runtime / maintenance CLI | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/maintenance/seed_standard_dataset.py:588::seed_collections</code> | seed_collections(conn) -> None<br>function | Coordinates conn.execute. | seed_collections(conn) -> None | SQLite | CLI/MCP/external caller<br>downstream: conn.execute | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_standard_dataset.py:618::seed_favorites</code> | seed_favorites(conn) -> None<br>function | Coordinates conn.execute. | seed_favorites(conn) -> None | SQLite | CLI/MCP/external caller<br>downstream: conn.execute | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_standard_dataset.py:646::seed_review_queue</code> | seed_review_queue(conn) -> None<br>function | Coordinates conn.execute, json_text. | seed_review_queue(conn) -> None | SQLite | CLI/MCP/external caller<br>downstream: conn.execute, json_text | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_standard_dataset.py:667::seed_processing_run</code> | seed_processing_run(conn) -> None<br>function | Coordinates conn.execute, json_text, len. | seed_processing_run(conn) -> None | SQLite | CLI/MCP/external caller<br>downstream: conn.execute, json_text, len | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_standard_dataset.py:696::rebuild_fts</code> | rebuild_fts(conn) -> None<br>function | Coordinates conn.execute("\n SELECT 1\n FROM sqlite_master\n WHERE type = 'table' AND name = 'question_search_fts'\n ").fetchone, conn.execute. | rebuild_fts(conn) -> None | SQLite | CLI/MCP/external caller<br>downstream: conn.execute("\n SELECT 1\n FROM sqlite_master\n WHERE type = 'table' AND name = 'question_search_fts'\n ").fetchone, conn.execute | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_standard_dataset.py:724::seed_standard_dataset</code> | seed_standard_dataset(db_path: Path) -> tuple[Path, Path \| None]<br>function | Coordinates reset_database, initialize_database, connect_db, clear_domain_data, seed_import_batch, seed_papers. | seed_standard_dataset(db_path: Path) -> tuple[Path, Path \| None] | none found statically | CLI/MCP/external caller<br>downstream: reset_database, initialize_database, connect_db, clear_domain_data, seed_import_batch, seed_papers | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_standard_dataset.py:747::build_parser</code> | build_parser() -> argparse.ArgumentParser<br>function | Coordinates argparse.ArgumentParser, parser.add_argument, str, default_db_path. | build_parser() -> argparse.ArgumentParser | SQLite?CLI/stdout | CLI/MCP/external caller<br>downstream: argparse.ArgumentParser, parser.add_argument, str, default_db_path | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/seed_standard_dataset.py:769::main</code> | main() -> int<br>function | Coordinates build_parser().parse_args, Path, seed_standard_dataset, print, build_parser, len. | main() -> int | file I/O?CLI/stdout | CLI/MCP/external caller<br>downstream: build_parser().parse_args, Path, seed_standard_dataset, print, build_parser, len | runtime / maintenance CLI | keep |
## scripts/maintenance/submit_obsidian_review_import.py

共 4 个函数记录

| 标识 | 签名 / 类别 | 职责 | 输入 ⇒ 输出 | 副作用 | 上游 / 下游 | 归属 | 优化提示 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| <code>scripts/maintenance/submit_obsidian_review_import.py:23::_request</code> | _request(process: subprocess.Popen[str], request_id: int, method: str, params: dict[str, Any]) -> dict[str, Any]<br>function | Coordinates process.stdin.write, process.stdin.flush, RuntimeError, process.stdout.readline, json.loads, json.dumps. | _request(process: subprocess.Popen[str], request_id: int, method: str, params: dict[str, Any]) -> dict[str, Any] | subprocess | CLI/MCP/external caller<br>downstream: process.stdin.write, process.stdin.flush, RuntimeError, process.stdout.readline, json.loads, json.dumps | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/submit_obsidian_review_import.py:39::_notify</code> | _notify(process: subprocess.Popen[str], method: str, params: dict[str, Any] \| None) -> None<br>function | Coordinates process.stdin.write, process.stdin.flush, RuntimeError, json.dumps. | _notify(process: subprocess.Popen[str], method: str, params: dict[str, Any] \| None) -> None | subprocess | CLI/MCP/external caller<br>downstream: process.stdin.write, process.stdin.flush, RuntimeError, json.dumps | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/submit_obsidian_review_import.py:49::_tool_data</code> | _tool_data(result: dict[str, Any]) -> dict[str, Any]<br>function | Coordinates result.get, item.get, json.loads, ast.literal_eval, isinstance, RuntimeError. | _tool_data(result: dict[str, Any]) -> dict[str, Any] | none found statically | CLI/MCP/external caller<br>downstream: result.get, item.get, json.loads, ast.literal_eval, isinstance, RuntimeError | runtime / maintenance CLI | keep |
| <code>scripts/maintenance/submit_obsidian_review_import.py:64::main</code> | main() -> int<br>function | Coordinates argparse.ArgumentParser, parser.add_argument, parser.parse_args, sorted, subprocess.Popen, print. | main() -> int | subprocess?CLI/stdout | CLI/MCP/external caller<br>downstream: argparse.ArgumentParser, parser.add_argument, parser.parse_args, sorted, subprocess.Popen, print | runtime / maintenance CLI | split orchestration/I-O boundary if it grows; otherwise keep |
## scripts/migrate_review_queue.py

共 1 个函数记录

| 标识 | 签名 / 类别 | 职责 | 输入 ⇒ 输出 | 副作用 | 上游 / 下游 | 归属 | 优化提示 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| <code>scripts/migrate_review_queue.py:17::main</code> | main() -> int<br>function | Coordinates argparse.ArgumentParser, parser.add_argument, parser.parse_args, ReviewQueueRepository, print, parser.error. | main() -> int | CLI/stdout | CLI/MCP/external caller<br>downstream: argparse.ArgumentParser, parser.add_argument, parser.parse_args, ReviewQueueRepository, print, parser.error | runtime / migration CLI | keep |
## scripts/physics_vault_mcp_server.py

共 200 个函数记录

| 标识 | 签名 / 类别 | 职责 | 输入 ⇒ 输出 | 副作用 | 上游 / 下游 | 归属 | 优化提示 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| <code>scripts/physics_vault_mcp_server.py:107::_read_classroom_sessions</code> | _read_classroom_sessions() -> list[dict[str, Any]]<br>function | Coordinates json.loads, isinstance, _CLASSROOM_SESSION_STORE.read_text, item.get. | _read_classroom_sessions() -> list[dict[str, Any]] | file I/O | CLI/MCP/external caller<br>downstream: json.loads, isinstance, _CLASSROOM_SESSION_STORE.read_text, item.get | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:115::_write_classroom_sessions</code> | _write_classroom_sessions(items: list[dict[str, Any]]) -> None<br>function | Coordinates _CLASSROOM_SESSION_STORE.parent.mkdir, _CLASSROOM_SESSION_STORE.with_suffix, temporary.write_text, os.replace, json.dumps, uuid.uuid4. | _write_classroom_sessions(items: list[dict[str, Any]]) -> None | file I/O | CLI/MCP/external caller<br>downstream: _CLASSROOM_SESSION_STORE.parent.mkdir, _CLASSROOM_SESSION_STORE.with_suffix, temporary.write_text, os.replace, json.dumps, uuid.uuid4 | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:240::_profiled_mcp_tool</code> | _profiled_mcp_tool() -> inferred<br>function | Coordinates _mcp_server_tool, decorator, _MCP_PROFILE_TOOLS.get, set. | _profiled_mcp_tool() -> inferred | none found statically | CLI/MCP/external caller<br>downstream: _mcp_server_tool, decorator, _MCP_PROFILE_TOOLS.get, set | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:243::register</code> | register(func: Any) -> Any<br>function | Coordinates decorator, _MCP_PROFILE_TOOLS.get, set. | register(func: Any) -> Any | none found statically | nested in _profiled_mcp_tool<br>downstream: decorator, _MCP_PROFILE_TOOLS.get, set | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:254::_search_service</code> | _search_service() -> QuestionSearchService<br>function | Coordinates QuestionSearchService. | _search_service() -> QuestionSearchService | none found statically | CLI/MCP/external caller<br>downstream: QuestionSearchService | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:258::_import_service</code> | _import_service() -> ImportPipelineService<br>function | Coordinates ImportPipelineService, SQLiteImportTaskRepository, PandocAdapter, DocumentCleaningService, StructuredQuestionParsingService, str. | _import_service() -> ImportPipelineService | none found statically | CLI/MCP/external caller<br>downstream: ImportPipelineService, SQLiteImportTaskRepository, PandocAdapter, DocumentCleaningService, StructuredQuestionParsingService, str | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:267::_task_center_service</code> | _task_center_service() -> TaskCenterService<br>function | Coordinates _import_service, TaskCenterService, LessonExportService. | _task_center_service() -> TaskCenterService | none found statically | CLI/MCP/external caller<br>downstream: _import_service, TaskCenterService, LessonExportService | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:275::_typst_export_service</code> | _typst_export_service() -> TypstQuestionExportService<br>function | Coordinates TypstQuestionExportService, _formal_db_path. | _typst_export_service() -> TypstQuestionExportService | none found statically | CLI/MCP/external caller<br>downstream: TypstQuestionExportService, _formal_db_path | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:279::_paper_draft_service</code> | _paper_draft_service() -> PaperDraftService<br>function | Coordinates PaperDraftService. | _paper_draft_service() -> PaperDraftService | none found statically | CLI/MCP/external caller<br>downstream: PaperDraftService | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:283::_change_audit_service</code> | _change_audit_service() -> ChangeAuditService<br>function | Coordinates ChangeAuditService, _formal_db_path, _review_db_path. | _change_audit_service() -> ChangeAuditService | none found statically | CLI/MCP/external caller<br>downstream: ChangeAuditService, _formal_db_path, _review_db_path | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:290::_metadata_management_service</code> | _metadata_management_service() -> MetadataManagementService<br>function | Coordinates MetadataManagementService, _formal_db_path. | _metadata_management_service() -> MetadataManagementService | none found statically | CLI/MCP/external caller<br>downstream: MetadataManagementService, _formal_db_path | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:294::_formal_db_path</code> | _formal_db_path() -> Path<br>function | Coordinates default_db_path. | _formal_db_path() -> Path | none found statically | CLI/MCP/external caller<br>downstream: default_db_path | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:298::_review_db_path</code> | _review_db_path() -> Path<br>function | Coordinates default_review_db_path, review_path.parent.mkdir. | _review_db_path() -> Path | file I/O | CLI/MCP/external caller<br>downstream: default_review_db_path, review_path.parent.mkdir | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:304::_connect_formal_read_db</code> | _connect_formal_read_db() -> sqlite3.Connection<br>function | Coordinates _formal_db_path, sqlite3.connect, conn.execute. | _connect_formal_read_db() -> sqlite3.Connection | SQLite | CLI/MCP/external caller<br>downstream: _formal_db_path, sqlite3.connect, conn.execute | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:314::_connect_formal_write_db</code> | _connect_formal_write_db() -> sqlite3.Connection<br>function | Coordinates sqlite3.connect, conn.execute, _formal_db_path. | _connect_formal_write_db() -> sqlite3.Connection | SQLite | CLI/MCP/external caller<br>downstream: sqlite3.connect, conn.execute, _formal_db_path | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:322::_connect_review_db</code> | _connect_review_db() -> sqlite3.Connection<br>function | Coordinates sqlite3.connect, conn.execute, _ensure_review_db_schema, _review_db_path. | _connect_review_db() -> sqlite3.Connection | SQLite | CLI/MCP/external caller<br>downstream: sqlite3.connect, conn.execute, _ensure_review_db_schema, _review_db_path | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:335::_mcp_download_directory</code> | _mcp_download_directory(destination_subdir: str \| None) -> Path \| None<br>function | Coordinates str(destination_subdir or '').strip().replace, (project_root() / 'data' / 'mcp-downloads').resolve, (base / relative).resolve, Path, relative.is_absolute, any. | _mcp_download_directory(destination_subdir: str \| None) -> Path \| None | file I/O | CLI/MCP/external caller<br>downstream: str(destination_subdir or '').strip().replace, (project_root() / 'data' / 'mcp-downloads').resolve, (base / relative).resolve, Path, relative.is_absolute, any | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:346::_resolve_managed_image_path</code> | _resolve_managed_image_path(file_path: str) -> Path \| None<br>function | Coordinates str(file_path or '').strip, project_root().resolve, Path(raw_path).expanduser, candidate.is_absolute, candidate.resolve, (root / candidate).resolve. | _resolve_managed_image_path(file_path: str) -> Path \| None | file I/O | CLI/MCP/external caller<br>downstream: str(file_path or '').strip, project_root().resolve, Path(raw_path).expanduser, candidate.is_absolute, candidate.resolve, (root / candidate).resolve | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:360::_decode_json_list</code> | _decode_json_list(value: Any) -> list[Any]<br>function | Coordinates isinstance, json.loads. | _decode_json_list(value: Any) -> list[Any] | none found statically | CLI/MCP/external caller<br>downstream: isinstance, json.loads | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:368::_question_image_records</code> | _question_image_records(question_id: str) -> list[dict[str, str]]<br>function | Coordinates _connect_formal_read_db, conn.execute('\n SELECT ia.asset_id, ia.filename, ia.file_path\n FROM question_assets qa\n JOIN image_assets ia ON ia.asset_id = qa.asset_id\n WHERE qa.question_id = ?\n ORDER BY qa.sort_order, ia.asset_id\n ', (question_id,)).fetchall, records.extend, conn.execute('\n SELECT figures_json, image_asset_ids_json, image_filenames_json\n FROM question_text_index WHERE question_id = ?\n ', (question_id,)).fetchone, conn.execute("SELECT COUNT(*) FROM sqlite_master WHERE type = 'table' AND name IN ('image_assets', 'question_assets')").fetchone, conn.execute('\n SELECT asset_id, filename, file_path\n FROM image_assets\n WHERE question_id = ?\n ORDER BY asset_id\n ', (question_id,)).fetchall. | _question_image_records(question_id: str) -> list[dict[str, str]] | file I/O?SQLite | CLI/MCP/external caller<br>downstream: _connect_formal_read_db, conn.execute('\n SELECT ia.asset_id, ia.filename, ia.file_path\n FROM question_assets qa\n JOIN image_assets ia ON ia.asset_id = qa.asset_id\n WHERE qa.question_id = ?\n ORDER BY qa.sort_order, ia.asset_id\n ', (question_id,)).fetchall, records.extend, conn.execute('\n SELECT figures_json, image_asset_ids_json, image_filenames_json\n FROM question_text_index WHERE question_id = ?\n ', (question_id,)).fetchone, conn.execute("SELECT COUNT(*) FROM sqlite_master WHERE type = 'table' AND name IN ('image_assets', 'question_assets')").fetchone, conn.execute('\n SELECT asset_id, filename, file_path\n FROM image_assets\n WHERE question_id = ?\n ORDER BY asset_id\n ', (question_id,)).fetchall | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:431::_resolve_review_task_id</code> | _resolve_review_task_id(task_id: str) -> str \| None<br>function | Coordinates str(task_id or '').strip, _connect_review_db, conn.execute('SELECT task_id FROM import_pipeline_tasks WHERE task_id = ?', (tid,)).fetchone, conn.execute('SELECT task_id FROM import_pipeline_tasks WHERE task_id LIKE ? ORDER BY updated_at DESC LIMIT 2', (f'{tid}%',)).fetchall, len, str. | _resolve_review_task_id(task_id: str) -> str \| None | SQLite | CLI/MCP/external caller<br>downstream: str(task_id or '').strip, _connect_review_db, conn.execute('SELECT task_id FROM import_pipeline_tasks WHERE task_id = ?', (tid,)).fetchone, conn.execute('SELECT task_id FROM import_pipeline_tasks WHERE task_id LIKE ? ORDER BY updated_at DESC LIMIT 2', (f'{tid}%',)).fetchall, len, str | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:452::_load_review_task</code> | _load_review_task(task_id: str) -> tuple[str \| None, dict[str, Any] \| None, dict[str, Any] \| None]<br>function | Coordinates str(task_id or '').strip, _resolve_review_task_id, _connect_review_db, conn.execute('\n SELECT task_id, task_type, status, created_at, updated_at,\n input_summary_json, result_json, error\n FROM import_pipeline_tasks\n WHERE task_id = ?\n ', (resolved,)).fetchone, dict, str. | _load_review_task(task_id: str) -> tuple[str \| None, dict[str, Any] \| None, dict[str, Any] \| None] | SQLite | CLI/MCP/external caller<br>downstream: str(task_id or '').strip, _resolve_review_task_id, _connect_review_db, conn.execute('\n SELECT task_id, task_type, status, created_at, updated_at,\n input_summary_json, result_json, error\n FROM import_pipeline_tasks\n WHERE task_id = ?\n ', (resolved,)).fetchone, dict, str | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:493::_ensure_review_db_schema</code> | _ensure_review_db_schema(conn: sqlite3.Connection) -> None<br>function | Coordinates conn.execute, conn.commit. | _ensure_review_db_schema(conn: sqlite3.Connection) -> None | SQLite | CLI/MCP/external caller<br>downstream: conn.execute, conn.commit | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:547::_ensure_change_audit_schema</code> | _ensure_change_audit_schema(conn: sqlite3.Connection) -> None<br>function | Coordinates conn.execute. | _ensure_change_audit_schema(conn: sqlite3.Connection) -> None | SQLite | CLI/MCP/external caller<br>downstream: conn.execute | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:596::_ensure_review_queue_outbox_schema</code> | _ensure_review_queue_outbox_schema(conn: sqlite3.Connection) -> None<br>function | Coordinates conn.execute. | _ensure_review_queue_outbox_schema(conn: sqlite3.Connection) -> None | SQLite | CLI/MCP/external caller<br>downstream: conn.execute | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:628::_deliver_review_queue_outbox</code> | _deliver_review_queue_outbox(operation_id: str) -> dict[str, Any]<br>function | Coordinates _connect_formal_write_db, _ensure_review_queue_outbox_schema, conn.execute('\n SELECT operation_id, review_id, question_id, reason, payload_json,\n audit_batch_id, delivery_status, delivery_attempts, last_error\n FROM review_queue_outbox WHERE operation_id = ?\n ', (operation_id,)).fetchone, conn.commit, _tool_error, conn.execute. | _deliver_review_queue_outbox(operation_id: str) -> dict[str, Any] | SQLite | CLI/MCP/external caller<br>downstream: _connect_formal_write_db, _ensure_review_queue_outbox_schema, conn.execute('\n SELECT operation_id, review_id, question_id, reason, payload_json,\n audit_batch_id, delivery_status, delivery_attempts, last_error\n FROM review_queue_outbox WHERE operation_id = ?\n ', (operation_id,)).fetchone, conn.commit, _tool_error, conn.execute | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:718::_record_change_batch</code> | _record_change_batch(conn: sqlite3.Connection) -> str<br>function | Coordinates _ensure_change_audit_schema, conn.execute, _short_id, len, item.get, json.dumps. | _record_change_batch(conn: sqlite3.Connection) -> str | SQLite | CLI/MCP/external caller<br>downstream: _ensure_change_audit_schema, conn.execute, _short_id, len, item.get, json.dumps | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:759::_dump_model</code> | _dump_model(value: Any) -> Any<br>function | Coordinates hasattr, isinstance, value.model_dump, _dump_model, value.items. | _dump_model(value: Any) -> Any | none found statically | CLI/MCP/external caller<br>downstream: hasattr, isinstance, value.model_dump, _dump_model, value.items | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:769::_clean_args</code> | _clean_args(args: dict[str, Any]) -> dict[str, Any]<br>function | Coordinates args.items. | _clean_args(args: dict[str, Any]) -> dict[str, Any] | none found statically | CLI/MCP/external caller<br>downstream: args.items | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:773::_tool_error</code> | _tool_error(code: str, message: str, **extra) -> dict[str, Any]<br>function | Performs a local runtime calculation or state transition. | _tool_error(code: str, message: str, **extra) -> dict[str, Any] | none found statically | CLI/MCP/external caller<br>downstream: no direct call found | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:799::__init__</code> | __init__(self, task_id: str, expected: str, current: str) -> None<br>class method | Coordinates super().__init__, super. | __init__(self, task_id: str, expected: str, current: str) -> None | none found statically | nested in class:ReviewTaskConflictError<br>downstream: super().__init__, super | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:806::_format_spec_diff</code> | _format_spec_diff(before: dict[str, Any] \| None, after: dict[str, Any]) -> dict[str, Any]<br>function | Coordinates isinstance, sorted, previous.get, after.get, set, previous_section.get. | _format_spec_diff(before: dict[str, Any] \| None, after: dict[str, Any]) -> dict[str, Any] | none found statically | CLI/MCP/external caller<br>downstream: isinstance, sorted, previous.get, after.get, set, previous_section.get | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:842::_looks_like_review_intent</code> | _looks_like_review_intent(text: str \| None) -> bool<br>function | Coordinates str, bool, _REVIEW_INTENT_RE.search, _FORMAL_INTENT_RE.search. | _looks_like_review_intent(text: str \| None) -> bool | none found statically | CLI/MCP/external caller<br>downstream: str, bool, _REVIEW_INTENT_RE.search, _FORMAL_INTENT_RE.search | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:848::list_teaching_projects</code> | list_teaching_projects(limit: int) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | list_teaching_projects(limit: int) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _list_teaching_projects | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:854::get_teaching_project</code> | get_teaching_project(project_id: str) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | get_teaching_project(project_id: str) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _get_teaching_project, _tool_error | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:863::get_teaching_project_status</code> | get_teaching_project_status(project_id: str) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | get_teaching_project_status(project_id: str) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _get_teaching_project, _tool_error, project.get, isinstance, artifact.get | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:888::duplicate_teaching_project</code> | duplicate_teaching_project(project_id: str, title: str \| None) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | duplicate_teaching_project(project_id: str, title: str \| None) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _duplicate_teaching_project, _tool_error | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:897::publish_teaching_artifact</code> | publish_teaching_artifact(project_id: str, artifact: Literal['handout', 'slides'], confirmed: bool) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | publish_teaching_artifact(project_id: str, artifact: Literal['handout', 'slides'], confirmed: bool) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _get_teaching_project, datetime.now(timezone.utc).isoformat, json.loads, _tool_error, isinstance | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:940::preflight_teaching_handout</code> | preflight_teaching_handout(project_id: str, use_published: bool) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | preflight_teaching_handout(project_id: str, use_published: bool) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _get_teaching_project, _tool_error, isinstance, project.get, artifact.get | runtime / MCP …20565 tokens truncated…bmit_pptx_export_job(lesson_package: dict[str, Any], include_answers: bool, include_analysis: bool, file_name: str \| None, source: str, session_id: str \| None, operator: str) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _submit_export_job, _task_action_context | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:6367::get_job_status</code> | get_job_status(task_id: str) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | get_job_status(task_id: str) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, str(task_id or '').strip, _tool_error, _task_center_service().get_task, _compact_job, str | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:6380::list_jobs</code> | list_jobs(statuses: list[str] \| None, task_types: list[str] \| None, created_from: str \| None, created_to: str \| None, page: int, page_size: int) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | list_jobs(statuses: list[str] \| None, task_types: list[str] \| None, created_from: str \| None, created_to: str \| None, page: int, page_size: int) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, max, min, int, _task_center_service().list_tasks, _job_tool_error | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:6412::retry_job</code> | retry_job(task_id: str, confirmed: bool, source: str, session_id: str \| None, operator: str) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | retry_job(task_id: str, confirmed: bool, source: str, session_id: str \| None, operator: str) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, str(task_id or '').strip, _task_center_service, service.retry_job, _compact_job, str | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:6454::cancel_job</code> | cancel_job(task_id: str, confirmed: bool, source: str, session_id: str \| None, operator: str) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | cancel_job(task_id: str, confirmed: bool, source: str, session_id: str \| None, operator: str) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, str(task_id or '').strip, _task_center_service, service.cancel_job, _compact_job, str | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:6494::_task_action_context</code> | _task_action_context(source: str, session_id: str \| None, operator: str) -> TaskActionContext<br>function | Coordinates TaskActionContext, str(source or 'physics_vault_mcp').strip, str(session_id).strip, str(operator or 'MCP user').strip, str. | _task_action_context(source: str, session_id: str \| None, operator: str) -> TaskActionContext | none found statically | CLI/MCP/external caller<br>downstream: TaskActionContext, str(source or 'physics_vault_mcp').strip, str(session_id).strip, str(operator or 'MCP user').strip, str | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6509::_compact_job</code> | _compact_job(task: dict[str, Any]) -> dict[str, Any]<br>function | Coordinates compact.get, isinstance, task.get, _mcp_scalar, bool, str. | _compact_job(task: dict[str, Any]) -> dict[str, Any] | none found statically | CLI/MCP/external caller<br>downstream: compact.get, isinstance, task.get, _mcp_scalar, bool, str | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6542::_mcp_scalar</code> | _mcp_scalar(value: Any) -> Any<br>function | Coordinates hasattr, callable, value.isoformat. | _mcp_scalar(value: Any) -> Any | none found statically | CLI/MCP/external caller<br>downstream: hasattr, callable, value.isoformat | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6548::_parse_job_datetime</code> | _parse_job_datetime(raw: str \| None) -> inferred<br>function | Coordinates datetime.fromisoformat, str(raw).strip().replace, parsed.replace, str(raw).strip, str. | _parse_job_datetime(raw: str \| None) -> inferred | file I/O | CLI/MCP/external caller<br>downstream: datetime.fromisoformat, str(raw).strip().replace, parsed.replace, str(raw).strip, str | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6557::_submit_export_job</code> | _submit_export_job(export_format: Literal['word', 'pptx'], lesson_package: dict[str, Any]) -> dict[str, Any]<br>function | Coordinates _task_center_service, getattr, _tool_error, format_spec_for_template, validate_format_spec, bool. | _submit_export_job(export_format: Literal['word', 'pptx'], lesson_package: dict[str, Any]) -> dict[str, Any] | none found statically | CLI/MCP/external caller<br>downstream: _task_center_service, getattr, _tool_error, format_spec_for_template, validate_format_spec, bool | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:6619::_job_tool_error</code> | _job_tool_error(exc: Exception) -> dict[str, Any]<br>function | Coordinates getattr, str, _tool_error. | _job_tool_error(exc: Exception) -> dict[str, Any] | none found statically | CLI/MCP/external caller<br>downstream: getattr, str, _tool_error | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6632::_query_knowledge_points</code> | _query_knowledge_points(keyword: str, limit: int) -> list[dict[str, Any]]<br>function | Coordinates params.append, params.extend, _connect_formal_read_db, conn.execute(f'\n SELECT topic1_id, topic1_name, topic2_id, topic2_name, topic3_id, topic3_name,\n source_chapter, status, note\n FROM knowledge_points\n {where}\n ORDER BY topic1_id, topic2_id, topic3_id\n LIMIT ?\n ', params).fetchall, dict, conn.execute. | _query_knowledge_points(keyword: str, limit: int) -> list[dict[str, Any]] | SQLite | CLI/MCP/external caller<br>downstream: params.append, params.extend, _connect_formal_read_db, conn.execute(f'\n SELECT topic1_id, topic1_name, topic2_id, topic2_name, topic3_id, topic3_name,\n source_chapter, status, note\n FROM knowledge_points\n {where}\n ORDER BY topic1_id, topic2_id, topic3_id\n LIMIT ?\n ', params).fetchall, dict, conn.execute | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6662::_query_question_tags</code> | _query_question_tags(query: str, question_ids: list[str], limit: int) -> list[dict[str, Any]]<br>function | Coordinates params.append, where_parts.append, params.extend, _connect_formal_read_db, conn.execute(f'\n SELECT q.question_id, q.canonical_title, q.question_type, q.difficulty,\n q.module, q.source, qti.title_text, qti.tags_json, qti.source_text\n FROM questions q\n LEFT JOIN question_text_index qti ON qti.question_id = q.question_id\n {where_sql}\n ORDER BY q.updated_at DESC, q.question_id\n LIMIT ?\n ', params).fetchall, dict. | _query_question_tags(query: str, question_ids: list[str], limit: int) -> list[dict[str, Any]] | SQLite | CLI/MCP/external caller<br>downstream: params.append, where_parts.append, params.extend, _connect_formal_read_db, conn.execute(f'\n SELECT q.question_id, q.canonical_title, q.question_type, q.difficulty,\n q.module, q.source, qti.title_text, qti.tags_json, qti.source_text\n FROM questions q\n LEFT JOIN question_text_index qti ON qti.question_id = q.question_id\n {where_sql}\n ORDER BY q.updated_at DESC, q.question_id\n LIMIT ?\n ', params).fetchall, dict | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6702::_fetch_formal_question_summaries</code> | _fetch_formal_question_summaries(question_ids: list[str]) -> dict[str, dict[str, Any]]<br>function | Coordinates ','.join, list, _connect_formal_read_db, conn.execute(f'\n SELECT q.question_id, q.canonical_title, q.question_type, q.difficulty,\n q.status, q.review_status, q.source,\n qti.title_text, qti.stem_text, qti.tags_json, qti.source_text\n FROM questions q\n LEFT JOIN question_text_index qti ON qti.question_id = q.question_id\n WHERE q.question_id IN ({placeholders})\n ', safe_ids).fetchall, dict.fromkeys, _parse_tags. | _fetch_formal_question_summaries(question_ids: list[str]) -> dict[str, dict[str, Any]] | SQLite | CLI/MCP/external caller<br>downstream: ','.join, list, _connect_formal_read_db, conn.execute(f'\n SELECT q.question_id, q.canonical_title, q.question_type, q.difficulty,\n q.status, q.review_status, q.source,\n qti.title_text, qti.stem_text, qti.tags_json, qti.source_text\n FROM questions q\n LEFT JOIN question_text_index qti ON qti.question_id = q.question_id\n WHERE q.question_id IN ({placeholders})\n ', safe_ids).fetchall, dict.fromkeys, _parse_tags | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6735::_table_exists</code> | _table_exists(conn: sqlite3.Connection, table_name: str) -> bool<br>function | Coordinates conn.execute("SELECT name FROM sqlite_master WHERE type='table' AND name = ?", (table_name,)).fetchone, conn.execute. | _table_exists(conn: sqlite3.Connection, table_name: str) -> bool | SQLite | CLI/MCP/external caller<br>downstream: conn.execute("SELECT name FROM sqlite_master WHERE type='table' AND name = ?", (table_name,)).fetchone, conn.execute | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6760::_clean_latex_value</code> | _clean_latex_value(value: Any, key: str \| None) -> tuple[Any, int]<br>function | Coordinates isinstance, _normalize_ocr_tables_in_text, _REVIEW_TRAILING_ESCAPED_DOLLAR_RE.subn, _REVIEW_SPLIT_MATH_OPERATOR_RE.subn, _REVIEW_MATH_ITALIC_RE.sub, normalize_math_delimiters. | _clean_latex_value(value: Any, key: str \| None) -> tuple[Any, int] | file I/O | CLI/MCP/external caller<br>downstream: isinstance, _normalize_ocr_tables_in_text, _REVIEW_TRAILING_ESCAPED_DOLLAR_RE.subn, _REVIEW_SPLIT_MATH_OPERATOR_RE.subn, _REVIEW_MATH_ITALIC_RE.sub, normalize_math_delimiters | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:6766::replace</code> | replace(match: re.Match[str]) -> str<br>function | Coordinates ' '.join, match.group(1).split, match.group, len. | replace(match: re.Match[str]) -> str | file I/O | nested in _clean_latex_value<br>downstream: ' '.join, match.group(1).split, match.group, len | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6806::_normalize_ocr_tables_in_text</code> | _normalize_ocr_tables_in_text(text: str) -> tuple[str, int]<br>function | Coordinates text.replace('\r\n', '\n').replace('\r', '\n').split, len, _OCR_TABLE_BORDER_RE.match, output.append, '\n'.join, text.replace('\r\n', '\n').replace. | _normalize_ocr_tables_in_text(text: str) -> tuple[str, int] | file I/O | CLI/MCP/external caller<br>downstream: text.replace('\r\n', '\n').replace('\r', '\n').split, len, _OCR_TABLE_BORDER_RE.match, output.append, '\n'.join, text.replace('\r\n', '\n').replace | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6826::_parse_ocr_table</code> | _parse_ocr_table(lines: list[str], start_index: int) -> tuple[list[str], int] \| None<br>function | Coordinates len, any, re.findall, line.strip, _split_ocr_table_row, _markdown_table_row. | _parse_ocr_table(lines: list[str], start_index: int) -> tuple[list[str], int] \| None | none found statically | CLI/MCP/external caller<br>downstream: len, any, re.findall, line.strip, _split_ocr_table_row, _markdown_table_row | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6854::_split_ocr_table_row</code> | _split_ocr_table_row(line: str, column_count: int) -> list[str] \| None<br>function | Coordinates _MATH_CELL_RE.findall, len, cell.strip, leftover.replace, leftover.strip, re.split. | _split_ocr_table_row(line: str, column_count: int) -> list[str] \| None | file I/O | CLI/MCP/external caller<br>downstream: _MATH_CELL_RE.findall, len, cell.strip, leftover.replace, leftover.strip, re.split | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6875::_markdown_table_row</code> | _markdown_table_row(cells: list[str]) -> str<br>function | Coordinates ' \| '.join, cell.replace. | _markdown_table_row(cells: list[str]) -> str | file I/O | CLI/MCP/external caller<br>downstream: ' \| '.join, cell.replace | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6879::_update_review_task_questions</code> | _update_review_task_questions(task_id: str, updated_questions: list[Any], changed_items: list[dict[str, Any]]) -> None<br>function | Coordinates _connect_review_db, conn.execute, conn.execute('SELECT result_json, updated_at FROM import_pipeline_tasks WHERE task_id = ?', (str(task_id).strip(),)).fetchone, str, str(expected_updated_at or '').strip, _parse_json_dict. | _update_review_task_questions(task_id: str, updated_questions: list[Any], changed_items: list[dict[str, Any]]) -> None | SQLite | CLI/MCP/external caller<br>downstream: _connect_review_db, conn.execute, conn.execute('SELECT result_json, updated_at FROM import_pipeline_tasks WHERE task_id = ?', (str(task_id).strip(),)).fetchone, str, str(expected_updated_at or '').strip, _parse_json_dict | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6926::_safe_count</code> | _safe_count(conn: sqlite3.Connection, table_name: str) -> int \| None<br>function | Coordinates int, conn.execute(f'SELECT COUNT(*) FROM {table_name}').fetchone, conn.execute. | _safe_count(conn: sqlite3.Connection, table_name: str) -> int \| None | SQLite | CLI/MCP/external caller<br>downstream: int, conn.execute(f'SELECT COUNT(*) FROM {table_name}').fetchone, conn.execute | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6933::_fetch_questions</code> | _fetch_questions(conn: sqlite3.Connection, question_ids: list[str]) -> dict[str, sqlite3.Row]<br>function | Coordinates ','.join, conn.execute(f'\n SELECT question_id, canonical_title, question_type, difficulty, module\n FROM questions\n WHERE question_id IN ({placeholders})\n ', question_ids).fetchall, conn.execute. | _fetch_questions(conn: sqlite3.Connection, question_ids: list[str]) -> dict[str, sqlite3.Row] | SQLite | CLI/MCP/external caller<br>downstream: ','.join, conn.execute(f'\n SELECT question_id, canonical_title, question_type, difficulty, module\n FROM questions\n WHERE question_id IN ({placeholders})\n ', question_ids).fetchall, conn.execute | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6948::_fetch_topics</code> | _fetch_topics(conn: sqlite3.Connection, topic3_ids: list[str]) -> dict[str, sqlite3.Row]<br>function | Coordinates ','.join, conn.execute(f"\n SELECT topic1_id, topic1_name, topic2_id, topic2_name, topic3_id, topic3_name, source_chapter, status\n FROM knowledge_points\n WHERE topic3_id IN ({placeholders}) AND status = 'active'\n ", topic3_ids).fetchall, conn.execute. | _fetch_topics(conn: sqlite3.Connection, topic3_ids: list[str]) -> dict[str, sqlite3.Row] | SQLite | CLI/MCP/external caller<br>downstream: ','.join, conn.execute(f"\n SELECT topic1_id, topic1_name, topic2_id, topic2_name, topic3_id, topic3_name, source_chapter, status\n FROM knowledge_points\n WHERE topic3_id IN ({placeholders}) AND status = 'active'\n ", topic3_ids).fetchall, conn.execute | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6963::_topic_payload</code> | _topic_payload(row: sqlite3.Row) -> dict[str, Any]<br>function | Performs a local runtime calculation or state transition. | _topic_payload(row: sqlite3.Row) -> dict[str, Any] | SQLite | CLI/MCP/external caller<br>downstream: no direct call found | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6975::_short_id</code> | _short_id() -> str<br>function | Coordinates uuid.uuid4. | _short_id() -> str | none found statically | CLI/MCP/external caller<br>downstream: uuid.uuid4 | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6979::_parse_tags</code> | _parse_tags(raw: Any) -> list[str]<br>function | Coordinates isinstance, _normalize_tags, json.loads, str, str(raw).replace('，', ',').replace('、', ',').split, str(raw).replace('，', ',').replace. | _parse_tags(raw: Any) -> list[str] | file I/O | CLI/MCP/external caller<br>downstream: isinstance, _normalize_tags, json.loads, str, str(raw).replace('，', ',').replace('、', ',').split, str(raw).replace('，', ',').replace | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6991::_raw_tags</code> | _raw_tags(raw: Any) -> list[str]<br>function | Coordinates isinstance, json.loads, item.strip, str(item).strip, str, str(raw).replace('，', ',').replace('、', ',').split. | _raw_tags(raw: Any) -> list[str] | file I/O | CLI/MCP/external caller<br>downstream: isinstance, json.loads, item.strip, str(item).strip, str, str(raw).replace('，', ',').replace('、', ',').split | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:7005::_parse_json_dict</code> | _parse_json_dict(raw: Any) -> dict[str, Any]<br>function | Coordinates isinstance, json.loads, str. | _parse_json_dict(raw: Any) -> dict[str, Any] | none found statically | CLI/MCP/external caller<br>downstream: isinstance, json.loads, str | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:7017::_preview_text</code> | _preview_text(raw: Any, limit: int) -> str<br>function | Coordinates ' '.join, str(raw or '').split, len, text[:limit].rstrip, str. | _preview_text(raw: Any, limit: int) -> str | none found statically | CLI/MCP/external caller<br>downstream: ' '.join, str(raw or '').split, len, text[:limit].rstrip, str | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:7024::_review_question_summary</code> | _review_question_summary(item: Any, index: int, risk_count: int \| None) -> dict[str, Any]<br>function | Coordinates str(item.get('question_id') or item.get('id') or item.get('draft_id') or '').strip, str(item.get('title') or item.get('canonical_title') or item.get('stem') or '').strip, str(item.get('stem') or item.get('stem_text') or item.get('content') or '').strip, isinstance, item.get, _preview_text. | _review_question_summary(item: Any, index: int, risk_count: int \| None) -> dict[str, Any] | none found statically | CLI/MCP/external caller<br>downstream: str(item.get('question_id') or item.get('id') or item.get('draft_id') or '').strip, str(item.get('title') or item.get('canonical_title') or item.get('stem') or '').strip, str(item.get('stem') or item.get('stem_text') or item.get('content') or '').strip, isinstance, item.get, _preview_text | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:7050::_review_knowledge_summary</code> | _review_knowledge_summary(item: Any, index: int) -> dict[str, Any]<br>function | Coordinates isinstance, item.get, _preview_text. | _review_knowledge_summary(item: Any, index: int) -> dict[str, Any] | none found statically | CLI/MCP/external caller<br>downstream: isinstance, item.get, _preview_text | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:7068::_normalize_tags</code> | _normalize_tags(value: Any) -> list[str]<br>function | Coordinates isinstance, set, value.replace('，', ',').replace('、', ',').split, str(item).strip, ' '.join, tag.casefold. | _normalize_tags(value: Any) -> list[str] | file I/O | CLI/MCP/external caller<br>downstream: isinstance, set, value.replace('，', ',').replace('、', ',').split, str(item).strip, ' '.join, tag.casefold | runtime / MCP gateway | keep |
## scripts/physics_vault_mcp_server.py

共 80 个函数记录

| 标识 | 签名 / 类别 | 职责 | 输入 ⇒ 输出 | 副作用 | 上游 / 下游 | 归属 | 优化提示 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| <code>scripts/physics_vault_mcp_server.py:2326::_move_composition_ids</code> | _move_composition_ids(current_ids: list[str], item_id: str, after_item_id: str \| None) -> list[str] \| None<br>function | Coordinates str(item_id or '').strip, str(after_item_id or '').strip, result.insert, str, result.index. | _move_composition_ids(current_ids: list[str], item_id: str, after_item_id: str \| None) -> list[str] \| None | none found statically | CLI/MCP/external caller<br>downstream: str(item_id or '').strip, str(after_item_id or '').strip, result.insert, str, result.index | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:2342::move_composition_item</code> | move_composition_item(item_id: str, after_item_id: str \| None, draft_id: str \| None, dry_run: bool) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | move_composition_item(item_id: str, after_item_id: str \| None, draft_id: str \| None, dry_run: bool) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, reorder_composition_workbench | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:2358::remove_items_from_composition_workbench</code> | remove_items_from_composition_workbench(item_ids: list[str], draft_id: str \| None, dry_run: bool) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | remove_items_from_composition_workbench(item_ids: list[str], draft_id: str \| None, dry_run: bool) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _get_compose_draft_or_error, _compose_lock_error, list, _save_compose_draft, dict.fromkeys | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:2385::update_composition_item</code> | update_composition_item(item_id: str, payload: dict[str, Any], draft_id: str \| None, dry_run: bool) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | update_composition_item(item_id: str, payload: dict[str, Any], draft_id: str \| None, dry_run: bool) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _get_compose_draft_or_error, _compose_lock_error, list, next, dict | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:2429::lock_composition_workbench</code> | lock_composition_workbench(locked: bool, draft_id: str \| None, reason: str \| None) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | lock_composition_workbench(locked: bool, draft_id: str \| None, reason: str \| None) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _get_compose_draft_or_error, dict, PaperDraftUpsertRequest, _dump_model, bool | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:2460::apply_composition_workbench_plan</code> | apply_composition_workbench_plan(operations: list[dict[str, Any]], draft_id: str \| None, ordered_refs: list[str] \| None, mode: Literal['full', 'patch'], dry_run: bool) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | apply_composition_workbench_plan(operations: list[dict[str, Any]], draft_id: str \| None, ordered_refs: list[str] \| None, mode: Literal['full', 'patch'], dry_run: bool) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _get_compose_draft_or_error, _compose_lock_error, list, set, _fetch_formal_question_summaries | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:2641::_composition_preview_markdown</code> | _composition_preview_markdown(draft: dict[str, Any]) -> str<br>function | Coordinates draft.get, enumerate, lines.extend, str, '\n'.join(lines).strip, item.get. | _composition_preview_markdown(draft: dict[str, Any]) -> str | none found statically | CLI/MCP/external caller<br>downstream: draft.get, enumerate, lines.extend, str, '\n'.join(lines).strip, item.get | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:2663::preview_composition_workbench</code> | preview_composition_workbench(draft_id: str \| None, format: Literal['markdown', 'html']) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | preview_composition_workbench(draft_id: str \| None, format: Literal['markdown', 'html']) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _get_compose_draft_or_error, _composition_preview_markdown, _tool_error, '<br>\n'.join, html.escape | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:2681::_composition_draft_to_lesson_package</code> | _composition_draft_to_lesson_package(draft: dict[str, Any]) -> dict[str, Any]<br>function | Coordinates str(item.get('question_id') or '').strip, _dump_model, draft.get, str, dict, list. | _composition_draft_to_lesson_package(draft: dict[str, Any]) -> dict[str, Any] | none found statically | CLI/MCP/external caller<br>downstream: str(item.get('question_id') or '').strip, _dump_model, draft.get, str, dict, list | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:2724::export_composition_workbench</code> | export_composition_workbench(draft_id: str \| None, format: Literal['word', 'pptx'], include_answers: bool \| None, include_analysis: bool \| None, file_name: str \| None, template_id: str \| None, format_spec: dict[str, Any] \| None, answer_position: Literal['after_question', 'end'] \| None) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | export_composition_workbench(draft_id: str \| None, format: Literal['word', 'pptx'], include_answers: bool \| None, include_analysis: bool \| None, file_name: str \| None, template_id: str \| None, format_spec: dict[str, Any] \| None, answer_position: Literal['after_question', 'end'] \| None) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _get_compose_draft_or_error, _tool_error, _composition_draft_to_lesson_package, format_spec_for_template, _submit_export_job | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:2760::list_word_export_templates</code> | list_word_export_templates() -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | list_word_export_templates() -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _list_word_export_templates_store | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:2766::get_word_export_template</code> | get_word_export_template(template_id: str) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | get_word_export_template(template_id: str) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, next, _tool_error, _list_word_export_templates_store, item.get, str(template_id or '').strip | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:2776::propose_word_export_format</code> | propose_word_export_format(purpose: Literal['formal_exam', 'student_practice', 'teacher_handout', 'custom'], requirements: str \| None) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | propose_word_export_format(purpose: Literal['formal_exam', 'student_practice', 'teacher_handout', 'custom'], requirements: str \| None) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, next, template.get, format_spec_for_template, _tool_error, _list_word_export_templates_store | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:2803::validate_word_export_format</code> | validate_word_export_format(format_spec: dict[str, Any]) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | validate_word_export_format(format_spec: dict[str, Any]) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, validate_format_spec | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:2809::save_word_export_template</code> | save_word_export_template(name: str, format_spec: dict[str, Any], template_id: str \| None, description: str \| None, overwrite: bool) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | save_word_export_template(name: str, format_spec: dict[str, Any], template_id: str \| None, description: str \| None, overwrite: bool) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _save_word_export_template_store | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:2821::rename_word_export_template</code> | rename_word_export_template(template_id: str, name: str) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | rename_word_export_template(template_id: str, name: str) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _rename_word_export_template_store | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:2827::list_saved_handouts</code> | list_saved_handouts(limit: int) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | list_saved_handouts(limit: int) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _list_saved_handouts_store | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:2833::get_saved_handout</code> | get_saved_handout(document_id: str) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | get_saved_handout(document_id: str) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _get_saved_handout_store, _tool_error | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:2842::list_saved_handout_versions</code> | list_saved_handout_versions(document_id: str) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | list_saved_handout_versions(document_id: str) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _list_saved_handout_versions_store, _tool_error | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:2851::restore_saved_handout_version</code> | restore_saved_handout_version(document_id: str, version: int, confirmed: bool) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | restore_saved_handout_version(document_id: str, version: int, confirmed: bool) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _list_saved_handout_versions_store, next, _tool_error, _restore_saved_handout_version_store, str | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:2876::rename_saved_handout</code> | rename_saved_handout(document_id: str, title: str) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | rename_saved_handout(document_id: str, title: str) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _rename_saved_handout_store, _tool_error, str | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:2888::apply_word_format_to_saved_handout</code> | apply_word_format_to_saved_handout(document_id: str, template_id: str \| None, format_spec: dict[str, Any] \| None) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | apply_word_format_to_saved_handout(document_id: str, template_id: str \| None, format_spec: dict[str, Any] \| None) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _get_saved_handout_store, document.get, format_spec_for_template, validate_format_spec, _update_saved_handout_format_store | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:2921::apply_word_format_to_workbench</code> | apply_word_format_to_workbench(draft_id: str \| None, template_id: str \| None, format_spec: dict[str, Any] \| None, dry_run: bool) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | apply_word_format_to_workbench(draft_id: str \| None, template_id: str \| None, format_spec: dict[str, Any] \| None, dry_run: bool) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _get_compose_draft_or_error, dict, metadata.get, format_spec_for_template, validate_format_spec | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:2972::export_saved_handout</code> | export_saved_handout(document_id: str, format: Literal['word'], include_answers: bool \| None, include_analysis: bool \| None, answer_position: Literal['after_question', 'end'] \| None, template_id: str \| None, format_spec: dict[str, Any] \| None, file_name: str \| None) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | export_saved_handout(document_id: str, format: Literal['word'], include_answers: bool \| None, include_analysis: bool \| None, answer_position: Literal['after_question', 'end'] \| None, template_id: str \| None, format_spec: dict[str, Any] \| None, file_name: str \| None) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _get_saved_handout_store, format_spec_for_template, validate_format_spec, _submit_export_job, _tool_error | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:3009::_find_composition_candidates</code> | _find_composition_candidates(query: str, limit: int) -> list[dict[str, Any]]<br>function | Coordinates str(query or '').strip, _comprehensive_query_terms, ' OR '.join, _connect_formal_read_db, conn.execute(f"\n SELECT DISTINCT q.question_id, q.canonical_title, q.question_type, q.difficulty, q.module, q.source,\n qti.title_text, qti.stem_text, qti.tags_json, qti.source_text,\n GROUP_CONCAT(DISTINCT kp.topic3_name) AS knowledge_topic3_names\n FROM questions q\n LEFT JOIN question_text_index qti ON qti.question_id = q.question_id\n LEFT JOIN question_knowledge_points qkp ON qkp.question_id = q.question_id\n LEFT JOIN knowledge_points kp ON kp.topic3_id = qkp.topic3_id\n WHERE (q.status IS NULL OR q.status NOT IN ('deleted', 'archived', 'archived_duplicate'))\n AND ({where})\n GROUP BY q.question_id\n ORDER BY q.updated_at DESC, q.question_id\n LIMIT ?\n ", [*params, min(max(int(limit), 1), 500)]).fetchall, dict. | _find_composition_candidates(query: str, limit: int) -> list[dict[str, Any]] | SQLite | CLI/MCP/external caller<br>downstream: str(query or '').strip, _comprehensive_query_terms, ' OR '.join, _connect_formal_read_db, conn.execute(f"\n SELECT DISTINCT q.question_id, q.canonical_title, q.question_type, q.difficulty, q.module, q.source,\n qti.title_text, qti.stem_text, qti.tags_json, qti.source_text,\n GROUP_CONCAT(DISTINCT kp.topic3_name) AS knowledge_topic3_names\n FROM questions q\n LEFT JOIN question_text_index qti ON qti.question_id = q.question_id\n LEFT JOIN question_knowledge_points qkp ON qkp.question_id = q.question_id\n LEFT JOIN knowledge_points kp ON kp.topic3_id = qkp.topic3_id\n WHERE (q.status IS NULL OR q.status NOT IN ('deleted', 'archived', 'archived_duplicate'))\n AND ({where})\n GROUP BY q.question_id\n ORDER BY q.updated_at DESC, q.question_id\n LIMIT ?\n ", [*params, min(max(int(limit), 1), 500)]).fetchall, dict | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:3043::_select_balanced_composition_candidates</code> | _select_balanced_composition_candidates(candidates: list[dict[str, Any]], target_count: int, query: str) -> list[dict[str, Any]]<br>function | Coordinates list, set, str(query).casefold, _comprehensive_query_terms, max, remaining.remove. | _select_balanced_composition_candidates(candidates: list[dict[str, Any]], target_count: int, query: str) -> list[dict[str, Any]] | none found statically | CLI/MCP/external caller<br>downstream: list, set, str(query).casefold, _comprehensive_query_terms, max, remaining.remove | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:3052::score</code> | score(row: dict[str, Any]) -> tuple[int, str]<br>function | Coordinates str, str(row.get('title_text') or row.get('canonical_title') or '').casefold, str(row.get('stem_text') or '').casefold, ' '.join((str(row.get(field) or '') for field in ('module', 'tags_json', 'knowledge_topic3_names'))).casefold, max, row.get. | score(row: dict[str, Any]) -> tuple[int, str] | none found statically | nested in _select_balanced_composition_candidates<br>downstream: str, str(row.get('title_text') or row.get('canonical_title') or '').casefold, str(row.get('stem_text') or '').casefold, ' '.join((str(row.get(field) or '') for field in ('module', 'tags_json', 'knowledge_topic3_names'))).casefold, max, row.get | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:3089::curate_questions_to_composition_workbench</code> | curate_questions_to_composition_workbench(query: str, target_count: int, draft_id: str \| None, insert_at: int \| None, dry_run: bool) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | curate_questions_to_composition_workbench(query: str, target_count: int, draft_id: str \| None, insert_at: int \| None, dry_run: bool) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _get_compose_draft_or_error, str(query or '').strip, min, _find_composition_candidates, _select_balanced_composition_candidates | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:3140::list_knowledge_tree</code> | list_knowledge_tree(keyword: str \| None, limit: int) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | list_knowledge_tree(keyword: str \| None, limit: int) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, str(keyword or '').strip, min, _query_knowledge_points, max, _metadata_management_service().search_knowledge_points | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:3171::search_knowledge_points</code> | search_knowledge_points(keyword: str, limit: int) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | search_knowledge_points(keyword: str, limit: …2571 tokens truncated…_key</code> | _review_search_key(value: Any) -> str<br>function | Coordinates re.sub, str(value or '').casefold, str. | _review_search_key(value: Any) -> str | none found statically | CLI/MCP/external caller<br>downstream: re.sub, str(value or '').casefold, str | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:4125::_review_question_type</code> | _review_question_type(item: dict[str, Any]) -> str<br>function | Coordinates str(item.get('question_type') or item.get('type') or '').strip().casefold, str(item.get('question_type') or item.get('type') or '').strip, str, item.get. | _review_question_type(item: dict[str, Any]) -> str | none found statically | CLI/MCP/external caller<br>downstream: str(item.get('question_type') or item.get('type') or '').strip().casefold, str(item.get('question_type') or item.get('type') or '').strip, str, item.get | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:4129::_review_options</code> | _review_options(item: dict[str, Any]) -> list[tuple[str, str]]<br>function | Coordinates isinstance, enumerate, item.get, options.append, str(option.get('label') or option.get('key') or option.get('option') or '').strip, label.rstrip. | _review_options(item: dict[str, Any]) -> list[tuple[str, str]] | none found statically | CLI/MCP/external caller<br>downstream: isinstance, enumerate, item.get, options.append, str(option.get('label') or option.get('key') or option.get('option') or '').strip, label.rstrip | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:4151::_review_duplicate_value</code> | _review_duplicate_value(value: Any) -> str<br>function | Coordinates _REVIEW_FIGURE_PLACEHOLDER_RE.sub, re.sub('[\\s\\W_]+', '', without_figure_ids, flags=re.UNICODE).casefold, _review_text, re.sub. | _review_duplicate_value(value: Any) -> str | none found statically | CLI/MCP/external caller<br>downstream: _REVIEW_FIGURE_PLACEHOLDER_RE.sub, re.sub('[\\s\\W_]+', '', without_figure_ids, flags=re.UNICODE).casefold, _review_text, re.sub | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:4157::_review_exact_duplicate_signature</code> | _review_exact_duplicate_signature(item: dict[str, Any]) -> tuple[Any, ...] \| None<br>function | Coordinates _review_duplicate_value, tuple, _review_question_type, item.get, _review_options, len. | _review_exact_duplicate_signature(item: dict[str, Any]) -> tuple[Any, ...] \| None | none found statically | CLI/MCP/external caller<br>downstream: _review_duplicate_value, tuple, _review_question_type, item.get, _review_options, len | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:4173::_review_figures</code> | _review_figures(item: dict[str, Any]) -> tuple[set[str], list[str]]<br>function | Coordinates isinstance, set, item.get, str(figure.get('fig_uuid') or figure.get('placeholder_key') or figure.get('asset_id') or figure.get('image_id') or figure.get('id') or '').strip, figure_ids.add, duplicate_ids.append. | _review_figures(item: dict[str, Any]) -> tuple[set[str], list[str]] | none found statically | CLI/MCP/external caller<br>downstream: isinstance, set, item.get, str(figure.get('fig_uuid') or figure.get('placeholder_key') or figure.get('asset_id') or figure.get('image_id') or figure.get('id') or '').strip, figure_ids.add, duplicate_ids.append | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:4199::_review_risk</code> | _review_risk(code: str, severity: str, field: str, message: str, suggestion: str) -> dict[str, str]<br>function | Performs a local runtime calculation or state transition. | _review_risk(code: str, severity: str, field: str, message: str, suggestion: str) -> dict[str, str] | none found statically | CLI/MCP/external caller<br>downstream: no direct call found | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:4209::_is_informational_import_warning</code> | _is_informational_import_warning(value: Any) -> bool<br>function | Coordinates _review_text, bool, re.fullmatch. | _is_informational_import_warning(value: Any) -> bool | none found statically | CLI/MCP/external caller<br>downstream: _review_text, bool, re.fullmatch | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:4218::_review_workflow_guidance</code> | _review_workflow_guidance(items: list[dict[str, Any]], task_id: str) -> dict[str, Any]<br>function | Coordinates list, str, next_tools.append, actions.append, next_tools.extend, dict.fromkeys. | _review_workflow_guidance(items: list[dict[str, Any]], task_id: str) -> dict[str, Any] | none found statically | CLI/MCP/external caller<br>downstream: list, str, next_tools.append, actions.append, next_tools.extend, dict.fromkeys | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:4296::_latex_delimiter_status</code> | _latex_delimiter_status(text: str) -> tuple[bool, bool]<br>function | Coordinates len, text.startswith. | _latex_delimiter_status(text: str) -> tuple[bool, bool] | none found statically | CLI/MCP/external caller<br>downstream: len, text.startswith | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:4330::validate_review_task</code> | validate_review_task(task_id: str, question_ids: list[str] \| None, require_knowledge: bool, require_source: bool, risks_only: bool) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | validate_review_task(task_id: str, question_ids: list[str] \| None, require_knowledge: bool, require_source: bool, risks_only: bool) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, get_review_task_full, enumerate, sum, _review_workflow_guidance, full.get | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:4465::find_duplicate_review_tasks</code> | find_duplicate_review_tasks(task_type: str \| None, source: str \| None, limit: int) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | find_duplicate_review_tasks(task_type: str \| None, source: str \| None, limit: int) -> dict[str, Any] | SQLite?MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _import_service().find_duplicate_review_tasks, _tool_error, _import_service, str | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:4482::delete_review_tasks</code> | delete_review_tasks(task_ids: list[str], confirmed: bool) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | delete_review_tasks(task_ids: list[str], confirmed: bool) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, list, _import_service().delete_review_tasks, result.update, dict.fromkeys, _tool_error | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:4510::suggest_knowledge_points_for_task</code> | suggest_knowledge_points_for_task(task_id: str, question_ids: list[str] \| None, max_suggestions: int) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | suggest_knowledge_points_for_task(task_id: str, question_ids: list[str] \| None, max_suggestions: int) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, get_review_task_full, _metadata_management_service().suggest_knowledge_points, str(full.get('task', {}).get('task_id') or task_id).strip, full.get, isinstance | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:4529::clean_review_task_latex</code> | clean_review_task_latex(task_id: str, question_ids: list[str] \| None, dry_run: bool, reason: str \| None, expected_updated_at: str \| None) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | clean_review_task_latex(task_id: str, question_ids: list[str] \| None, dry_run: bool, reason: str \| None, expected_updated_at: str \| None) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, get_review_task_full, str(full.get('task', {}).get('task_id') or task_id).strip, full.get, isinstance, str(item).strip | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:4637::_merged_option_source</code> | _merged_option_source(item: dict[str, Any]) -> str<br>function | Coordinates item.get, isinstance, value.strip. | _merged_option_source(item: dict[str, Any]) -> str | none found statically | CLI/MCP/external caller<br>downstream: item.get, isinstance, value.strip | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:4648::_extract_option_block</code> | _extract_option_block(source: str) -> str<br>function | Coordinates re.search, first_option.start, _OPTION_METADATA_BOUNDARY_RE.search, first_option.end, end_match.start, len. | _extract_option_block(source: str) -> str | none found statically | CLI/MCP/external caller<br>downstream: re.search, first_option.start, _OPTION_METADATA_BOUNDARY_RE.search, first_option.end, end_match.start, len | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:4659::_split_merged_option_text</code> | _split_merged_option_text(source: str) -> list[tuple[str, str]]<br>function | Coordinates _extract_option_block, list, enumerate, _MERGED_OPTION_RE.finditer, len, match.group(1).upper. | _split_merged_option_text(source: str) -> list[tuple[str, str]] | none found statically | CLI/MCP/external caller<br>downstream: _extract_option_block, list, enumerate, _MERGED_OPTION_RE.finditer, len, match.group(1).upper | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:4677::_build_split_options</code> | _build_split_options(raw_options: Any, split_options: list[tuple[str, str]]) -> list[dict[str, Any]]<br>function | Coordinates isinstance. | _build_split_options(raw_options: Any, split_options: list[tuple[str, str]]) -> list[dict[str, Any]] | none found statically | CLI/MCP/external caller<br>downstream: isinstance | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:4689::split_merged_options</code> | split_merged_options(task_id: str, question_ids: list[str] \| None, dry_run: bool, reason: str \| None, expected_updated_at: str \| None) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | split_merged_options(task_id: str, question_ids: list[str] \| None, dry_run: bool, reason: str \| None, expected_updated_at: str \| None) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, get_review_task_full, str(full.get('task', {}).get('task_id') or task_id).strip, full.get, isinstance, str(item).strip | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:4773::deduplicate_review_task_questions</code> | deduplicate_review_task_questions(task_id: str, dry_run: bool, reason: str \| None, expected_updated_at: str \| None) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | deduplicate_review_task_questions(task_id: str, dry_run: bool, reason: str \| None, expected_updated_at: str \| None) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, get_review_task_full, str(full.get('task', {}).get('task_id') or task_id).strip, full.get, isinstance, str(raw.get('question_id') or raw.get('id') or raw.get('draft_id') or '').strip | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:4855::update_review_task_draft</code> | update_review_task_draft(task_id: str, updates: list[dict[str, Any]], dry_run: bool, reason: str \| None, expected_updated_at: str \| None) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | update_review_task_draft(task_id: str, updates: list[dict[str, Any]], dry_run: bool, reason: str \| None, expected_updated_at: str \| None) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, get_review_task_full, str(full.get('task', {}).get('task_id') or task_id).strip, enumerate, len, full.get | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:4996::list_question_tags</code> | list_question_tags(query: str \| None, question_ids: list[str] \| None, limit: int) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | list_question_tags(query: str \| None, question_ids: list[str] \| None, limit: int) -> dict[str, Any] | SQLite?MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _query_question_tags, _parse_tags, items.append, _connect_formal_read_db, _table_exists | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5059::diagnose_tag_maintenance</code> | diagnose_tag_maintenance(query: str \| None, limit: int, min_similarity: float) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | diagnose_tag_maintenance(query: str \| None, limit: int, min_similarity: float) -> dict[str, Any] | SQLite?MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _diagnose_tag_maintenance, _tool_error, min, max, _formal_db_path | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5077::suggest_question_tags</code> | suggest_question_tags(question_ids: list[str]) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | suggest_question_tags(question_ids: list[str]) -> dict[str, Any] | SQLite?MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _suggest_question_tags, _tool_error, _formal_db_path, str, isinstance | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5089::maintain_question_tags</code> | maintain_question_tags(question_ids: list[str] \| None, add_tags: list[str] \| None, remove_tags: list[str] \| None, merge_map: dict[str, list[str]] \| None, dry_run: bool, reason: str \| None, create_catalog_tags: bool) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | maintain_question_tags(question_ids: list[str] \| None, add_tags: list[str] \| None, remove_tags: list[str] \| None, merge_map: dict[str, list[str]] \| None, dry_run: bool, reason: str \| None, create_catalog_tags: bool) -> dict[str, Any] | SQLite?MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _maintain_question_tags, _tool_error, bool, _formal_db_path, str | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5115::list_change_batches</code> | list_change_batches(change_type: str \| None, status: str \| None, limit: int) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | list_change_batches(change_type: str \| None, status: str \| None, limit: int) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _change_audit_service().list_batches, _change_audit_service | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5129::get_change_batch</code> | get_change_batch(batch_id: str) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | get_change_batch(batch_id: str) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _change_audit_service().get_batch, _change_audit_service | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5135::rollback_change_batch</code> | rollback_change_batch(batch_id: str, dry_run: bool, reason: str \| None, allow_conflicts: bool) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | rollback_change_batch(batch_id: str, dry_run: bool, reason: str \| None, allow_conflicts: bool) -> dict[str, Any] | SQLite?MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _change_audit_service().rollback_batch, _connect_formal_write_db, _ensure_review_queue_outbox_schema, conn.execute, conn.commit | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5166::batch_replace_question_tags</code> | batch_replace_question_tags(updates: list[dict[str, Any]], dry_run: bool, reason: str \| None) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | batch_replace_question_tags(updates: list[dict[str, Any]], dry_run: bool, reason: str \| None) -> dict[str, Any] | SQLite?MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, enumerate, ','.join, len, str(item.get('question_id') or '').strip, _normalize_tags | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5277::return_question_to_review</code> | return_question_to_review(question_id: str, reason: str, dry_run: bool, operation_id: str \| None) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | return_question_to_review(question_id: str, reason: str, dry_run: bool, operation_id: str \| None) -> dict[str, Any] | SQLite?MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, str(question_id or '').strip, str(operation_id or '').strip, _tool_error, _connect_formal_write_db, _ensure_review_queue_outbox_schema | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5399::reconcile_review_queue_outbox</code> | reconcile_review_queue_outbox(limit: int) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | reconcile_review_queue_outbox(limit: int) -> dict[str, Any] | SQLite?MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, min, sum, max, _connect_formal_write_db, _ensure_review_queue_outbox_schema | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5425::batch_replace_question_knowledge_points</code> | batch_replace_question_knowledge_points(updates: list[dict[str, Any]], dry_run: bool, reason: str \| None) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | batch_replace_question_knowledge_points(updates: list[dict[str, Any]], dry_run: bool, reason: str \| None) -> dict[str, Any] | SQLite?MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, enumerate, sorted, len, str(raw.get('question_id') or '').strip, raw.get | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5565::find_similar_questions</code> | find_similar_questions(question_id: str, limit: int, same_question_type: bool, same_knowledge_point: bool, difficulty_tolerance: int) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | find_similar_questions(question_id: str, limit: int, same_question_type: bool, same_knowledge_point: bool, difficulty_tolerance: int) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, SimilarQuestionsService().find_similar, _dump_model, SimilarQuestionsService, min, int | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
## scripts/physics_vault_mcp_server.py

共 48 个函数记录

| 标识 | 签名 / 类别 | 职责 | 输入 ⇒ 输出 | 副作用 | 上游 / 下游 | 归属 | 优化提示 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| <code>scripts/physics_vault_mcp_server.py:5583::_canonical_duplicate_candidates</code> | _canonical_duplicate_candidates() -> tuple[list[dict[str, Any]], list[dict[str, Any]]]<br>function | Coordinates _canonical_duplicate_groups_from_members, _connect_formal_read_db, conn.execute('\n SELECT q.question_id, q.question_type, q.status, q.created_at, q.content_hash,\n qti.title_text, qti.stem_text, qti.options_json, qti.answer_text,\n qti.source_text\n FROM questions q\n JOIN question_text_index qti ON qti.question_id = q.question_id\n ').fetchall, canonical_question_fingerprint, by_fingerprint.setdefault(fingerprint, []).append, str. | _canonical_duplicate_candidates() -> tuple[list[dict[str, Any]], list[dict[str, Any]]] | SQLite | CLI/MCP/external caller<br>downstream: _canonical_duplicate_groups_from_members, _connect_formal_read_db, conn.execute('\n SELECT q.question_id, q.question_type, q.status, q.created_at, q.content_hash,\n qti.title_text, qti.stem_text, qti.options_json, qti.answer_text,\n qti.source_text\n FROM questions q\n JOIN question_text_index qti ON qti.question_id = q.question_id\n ').fetchall, canonical_question_fingerprint, by_fingerprint.setdefault(fingerprint, []).append, str | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5679::_canonical_duplicate_groups_from_members</code> | _canonical_duplicate_groups_from_members(by_fingerprint: dict[str, list[dict[str, Any]]]) -> list[dict[str, Any]]<br>function | Coordinates by_fingerprint.items, groups.sort, sorted, groups.append, len, int. | _canonical_duplicate_groups_from_members(by_fingerprint: dict[str, list[dict[str, Any]]]) -> list[dict[str, Any]] | none found statically | CLI/MCP/external caller<br>downstream: by_fingerprint.items, groups.sort, sorted, groups.append, len, int | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:5701::_ensure_canonical_duplicate_archive_schema</code> | _ensure_canonical_duplicate_archive_schema(conn: sqlite3.Connection) -> None<br>function | Coordinates conn.executescript. | _ensure_canonical_duplicate_archive_schema(conn: sqlite3.Connection) -> None | SQLite | CLI/MCP/external caller<br>downstream: conn.executescript | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:5731::_canonical_duplicate_merge_plan</code> | _canonical_duplicate_merge_plan(primary_question_id: str, duplicate_question_ids: list[str]) -> tuple[dict[str, Any] \| None, dict[str, Any] \| None]<br>function | Coordinates str(primary_question_id or '').strip, list, dict.fromkeys, _connect_formal_read_db, ','.join, conn.execute(f'\n SELECT q.question_id, q.question_type, q.status, q.review_comment, q.created_at,\n qti.title_text, qti.stem_text, qti.options_json, qti.answer_text, qti.tags_json\n FROM questions q\n JOIN question_text_index qti ON qti.question_id = q.question_id\n WHERE q.question_id IN ({placeholders})\n ', [primary_id, *duplicate_ids]).fetchall. | _canonical_duplicate_merge_plan(primary_question_id: str, duplicate_question_ids: list[str]) -> tuple[dict[str, Any] \| None, dict[str, Any] \| None] | SQLite | CLI/MCP/external caller<br>downstream: str(primary_question_id or '').strip, list, dict.fromkeys, _connect_formal_read_db, ','.join, conn.execute(f'\n SELECT q.question_id, q.question_type, q.status, q.review_comment, q.created_at,\n qti.title_text, qti.stem_text, qti.options_json, qti.answer_text, qti.tags_json\n FROM questions q\n JOIN question_text_index qti ON qti.question_id = q.question_id\n WHERE q.question_id IN ({placeholders})\n ', [primary_id, *duplicate_ids]).fetchall | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5763::fingerprint</code> | fingerprint(row: sqlite3.Row) -> str \| None<br>function | Coordinates canonical_question_fingerprint. | fingerprint(row: sqlite3.Row) -> str \| None | SQLite | nested in _canonical_duplicate_merge_plan<br>downstream: canonical_question_fingerprint | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:5862::scan_canonical_duplicate_questions</code> | scan_canonical_duplicate_questions(limit: int) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | scan_canonical_duplicate_questions(limit: int) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, min, _canonical_duplicate_candidates, max, len, sum | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5880::backfill_canonical_question_hashes</code> | backfill_canonical_question_hashes(dry_run: bool, reason: str, overwrite_existing: bool) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | backfill_canonical_question_hashes(dry_run: bool, reason: str, overwrite_existing: bool) -> dict[str, Any] | SQLite?MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _canonical_duplicate_candidates, len, _connect_formal_write_db, conn.execute, _ensure_change_audit_schema | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5931::merge_canonical_duplicate_questions</code> | merge_canonical_duplicate_questions(primary_question_id: str, duplicate_question_ids: list[str], dry_run: bool, reason: str) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | merge_canonical_duplicate_questions(primary_question_id: str, duplicate_question_ids: list[str], dry_run: bool, reason: str) -> dict[str, Any] | SQLite?MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _canonical_duplicate_merge_plan, _connect_formal_write_db, conn.execute, _ensure_canonical_duplicate_archive_schema, list | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:6096::restore_canonical_duplicate_merge</code> | restore_canonical_duplicate_merge(merge_batch_id: str, dry_run: bool, reason: str) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | restore_canonical_duplicate_merge(merge_batch_id: str, dry_run: bool, reason: str) -> dict[str, Any] | SQLite?MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, str(merge_batch_id or '').strip, _tool_error, _connect_formal_read_db, conn.execute("SELECT * FROM canonical_duplicate_archives WHERE merge_batch_id = ? AND state = 'archived' ORDER BY created_at DESC, archive_id DESC", (batch_id,)).fetchall, _connect_formal_write_db | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:6181::list_canonical_duplicate_merges</code> | list_canonical_duplicate_merges(state: Literal['archived', 'restored', 'all'], limit: int) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | list_canonical_duplicate_merges(state: Literal['archived', 'restored', 'all'], limit: int) -> dict[str, Any] | SQLite?MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, min, max, _connect_formal_read_db, conn.execute(f"\n SELECT merge_batch_id, primary_question_id, state, reason, created_at, restored_at, restore_reason,\n GROUP_CONCAT(duplicate_question_id, ',') AS duplicate_question_ids\n FROM canonical_duplicate_archives\n {where}\n GROUP BY merge_batch_id, primary_question_id, state, reason, created_at, restored_at, restore_reason\n ORDER BY created_at DESC, merge_batch_id DESC\n LIMIT ?\n ", [*params, safe_limit]).fetchall, len | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:6224::submit_ai_generated_review</code> | submit_ai_generated_review(source_text: str, source: str, chat_context: str \| None, session_id: str \| None) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | submit_ai_generated_review(source_text: str, source: str, chat_context: str \| None, session_id: str \| None) -> dict[str, Any] | file I/O?MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, source_text.startswith, _import_service().create_ai_generated_review_task, (ROOT / source_text.removeprefix(file_prefix).strip()).resolve, candidate.read_text, result.get | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:6263::submit_import_job</code> | submit_import_job(batch_id: str, source: str, session_id: str \| None, operator: str) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | submit_import_job(batch_id: str, source: str, session_id: str \| None, operator: str) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, str(batch_id or '').strip, _task_center_service().submit_batch_job, _compact_job, str, _job_tool_error | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:6291::submit_ai_clean_job</code> | submit_ai_clean_job(batch_id: str, source: str, session_id: str \| None, operator: str) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | submit_ai_clean_job(batch_id: str, source: str, session_id: str \| None, operator: str) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, str(batch_id or '').strip, _task_center_service().submit_batch_job, _compact_job, str, _job_tool_error | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:6319::submit_word_export_job</code> | submit_word_export_job(lesson_package: dict[str, Any], include_answers: bool \| None, include_analysis: bool \| None, file_name: str \| None, template_id: str \| None, format_spec: dict[str, Any] \| None, answer_position: Literal['after_question', 'end'] \| None, source: str, session_id: str \| None, operator: str) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | submit_word_export_job(lesson_package: dict[str, Any], include_answers: bool \| None, include_analysis: bool \| None, file_name: str \| None, template_id: str \| None, format_spec: dict[str, Any] \| None, answer_position: Literal['after_question', 'end'] \| None, source: str, session_id: str \| None, operator: str) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _submit_export_job, _task_action_context | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:6346::submit_pptx_export_job</code> | submit_pptx_export_job(lesson_package: dict[str, Any], include_answers: bool, include_analysis: bool, file_name: str \| None, source: str, session_id: str \| None, operator: str) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | submit_pptx_export_job(lesson_package: dict[str, Any], include_answers: bool, include_analysis: bool, file_name: str \| None, source: str, session_id: str \| None, operator: str) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _submit_export_job, _task_action_context | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:6367::get_job_status</code> | get_job_status(task_id: str) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | get_job_status(task_id: str) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, str(task_id or '').strip, _tool_error, _task_center_service().get_task, _compact_job, str | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:6380::list_jobs</code> | list_jobs(statuses: list[str] \| None, task_types: list[str] \| None, created_from: str \| None, created_to: str \| None, page: int, page_size: int) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | list_jobs(statuses: list[str] \| None, task_types: list[str] \| None, created_from: str \| None, created_to: str \| None, page: int, page_size: int) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, max, min, int, _task_center_service().list_tasks, _job_tool_error | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:6412::retry_job</code> | retry_job(task_id: str, confirmed: bool, source: str, session_id: str \| None, operator: str) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | retry_job(task_id: str, confirmed: bool, source: str, session_id: str \| None, operator: str) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, str(task_id or '').strip, _task_center_service, service.retry_job, _compact_job, str | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:6454::cancel_job</code> | cancel_job(task_id: str, confirmed: bool, source: str, session_id: str \| None, operator: str) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | cancel_job(task_id: str, confirmed: bool, source: str, session_id: str \| None, operator: str) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, str(task_id or '').strip, _task_center_service, service.cancel_job, _compact_job, str | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:6494::_task_action_context</code> | _task_action_context(source: str, session_id: str \| None, operator: str) -> TaskActionContext<br>function | Coordinates TaskActionContext, str(source or 'physics_vault_mcp').strip, str(session_id).strip, str(operator or 'MCP user').strip, str. | _task_action_context(source: str, session_id: str \| None, operator: str) -> TaskActionContext | none found statically | CLI/MCP/external caller<br>downstream: TaskActionContext, str(source or 'physics_vault_mcp').strip, str(session_id).strip, str(operator or 'MCP user').strip, str | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6509::_compact_job</code> | _compact_job(task: dict[str, Any]) -> dict[str, Any]<br>function | Coordinates compact.get, isinstance, task.get, _mcp_scalar, bool, str. | _compact_job(task: dict[str, Any]) -> dict[str, Any] | none found statically | CLI/MCP/external caller<br>downstream: compact.get, isinstance, task.get, _mcp_scalar, bool, str | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6542::_mcp_scalar</code> | _mcp_scalar(value: Any) -> Any<br>function | Coordinates hasattr, callable, value.isoformat. | _mcp_scalar(value: Any) -> Any | none found statically | CLI/MCP/external caller<br>downstream: hasattr, callable, value.isoformat | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6548::_parse_job_datetime</code> | _parse_job_datetime(raw: str \| None) -> inferred<br>function | Coordinates datetime.fromisoformat, str(raw).strip().replace, parsed.replace, str(raw).strip, str. | _parse_job_datetime(raw: str \| None) -> inferred | file I/O | CLI/MCP/external caller<br>downstream: datetime.fromisoformat, str(raw).strip().replace, parsed.replace, str(raw).strip, str | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6557::_submit_export_job</code> | _submit_export_job(export_format: Literal['word', 'pptx'], lesson_package: dict[str, Any]) -> dict[str, Any]<br>function | Coordinates _task_center_service, getattr, _tool_error, format_spec_for_template, validate_format_spec, bool. | _submit_export_job(export_format: Literal['word', 'pptx'], lesson_package: dict[str, Any]) -> dict[str, Any] | none found statically | CLI/MCP/external caller<br>downstream: _task_center_service, getattr, _tool_error, format_spec_for_template, validate_format_spec, bool | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:6619::_job_tool_error</code> | _job_tool_error(exc: Exception) -> dict[str, Any]<br>function | Coordinates getattr, str, _tool_error. | _job_tool_error(exc: Exception) -> dict[str, Any] | none found statically | CLI/MCP/external caller<br>downstream: getattr, str, _tool_error | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6632::_query_knowledge_points</code> | _query_knowledge_points(keyword: str, limit: int) -> list[dict[str, Any]]<br>function | Coordinates params.append, params.extend, _connect_formal_read_db, conn.execute(f'\n SELECT topic1_id, topic1_name, topic2_id, topic2_name, topic3_id, topic3_name,\n source_chapter, status, note\n FROM knowledge_points\n {where}\n ORDER BY topic1_id, topic2_id, topic3_id\n LIMIT ?\n ', params).fetchall, dict, conn.execute. | _query_knowledge_points(keyword: str, limit: int) -> list[dict[str, Any]] | SQLite | CLI/MCP/external caller<br>downstream: params.append, params.extend, _connect_formal_read_db, conn.execute(f'\n SELECT topic1_id, topic1_name, topic2_id, topic2_name, topic3_id, topic3_name,\n source_chapter, status, note\n FROM knowledge_points\n {where}\n ORDER BY topic1_id, topic2_id, topic3_id\n LIMIT ?\n ', params).fetchall, dict, conn.execute | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6662::_query_question_tags</code> | _query_question_tags(query: str, question_ids: list[str], limit: int) -> list[dict[str, Any]]<br>function | Coordinates params.append, where_parts.append, params.extend, _connect_formal_read_db, conn.execute(f'\n SELECT q.question_id, q.canonical_title, q.question_type, q.difficulty,\n q.module, q.source, qti.title_text, qti.tags_json, qti.source_text\n FROM questions q\n LEFT JOIN question_text_index qti ON qti.question_id = q.question_id\n {where_sql}\n ORDER BY q.updated_at DESC, q.question_id\n LIMIT ?\n ', params).fetchall, dict. | _query_question_tags(query: str, question_ids: list[str], limit: int) -> list[dict[str, Any]] | SQLite | CLI/MCP/external caller<br>downstream: params.append, where_parts.append, params.extend, _connect_formal_read_db, conn.execute(f'\n SELECT q.question_id, q.canonical_title, q.question_type, q.difficulty,\n q.module, q.source, qti.title_text, qti.tags_json, qti.source_text\n FROM questions q\n LEFT JOIN question_text_index qti ON qti.question_id = q.question_id\n {where_sql}\n ORDER BY q.updated_at DESC, q.question_id\n LIMIT ?\n ', params).fetchall, dict | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6702::_fetch_formal_question_summaries</code> | _fetch_formal_question_summaries(question_ids: list[str]) -> dict[str, dict[str, Any]]<br>function | Coordinates ','.join, list, _connect_formal_read_db, conn.execute(f'\n SELECT q.question_id, q.canonical_title, q.question_type, q.difficulty,\n q.status, q.review_status, q.source,\n qti.title_text, qti.stem_text, qti.tags_json, qti.source_text\n FROM questions q\n LEFT JOIN question_text_index qti ON qti.question_id = q.question_id\n WHERE q.question_id IN ({placeholders})\n ', safe_ids).fetchall, dict.fromkeys, _parse_tags. | _fetch_formal_question_summaries(question_ids: list[str]) -> dict[str, dict[str, Any]] | SQLite | CLI/MCP/external caller<br>downstream: ','.join, list, _connect_formal_read_db, conn.execute(f'\n SELECT q.question_id, q.canonical_title, q.question_type, q.difficulty,\n q.status, q.review_status, q.source,\n qti.title_text, qti.stem_text, qti.tags_json, qti.source_text\n FROM questions q\n LEFT JOIN question_text_index qti ON qti.question_id = q.question_id\n WHERE q.question_id IN ({placeholders})\n ', safe_ids).fetchall, dict.fromkeys, _parse_tags | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6735::_table_exists</code> | _table_exists(conn: sqlite3.Connection, table_name: str) -> bool<br>function | Coordinates conn.execute("SELECT name FROM sqlite_master WHERE type='table' AND name = ?", (table_name,)).fetchone, conn.execute. | _table_exists(conn: sqlite3.Connection, table_name: str) -> bool | SQLite | CLI/MCP/external caller<br>downstream: conn.execute("SELECT name FROM sqlite_master WHERE type='table' AND name = ?", (table_name,)).fetchone, conn.execute | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6760::_clean_latex_value</code> | _clean_latex_value(value: Any, key: str \| None) -> tuple[Any, int]<br>function | Coordinates isinstance, _normalize_ocr_tables_in_text, _REVIEW_TRAILING_ESCAPED_DOLLAR_RE.subn, _REVIEW_SPLIT_MATH_OPERATOR_RE.subn, _REVIEW_MATH_ITALIC_RE.sub, normalize_math_delimiters. | _clean_latex_value(value: Any, key: str \| None) -> tuple[Any, int] | file I/O | CLI/MCP/external caller<br>downstream: isinstance, _normalize_ocr_tables_in_text, _REVIEW_TRAILING_ESCAPED_DOLLAR_RE.subn, _REVIEW_SPLIT_MATH_OPERATOR_RE.subn, _REVIEW_MATH_ITALIC_RE.sub, normalize_math_delimiters | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:6766::replace</code> | replace(match: re.Match[str]) -> str<br>function | Coordinates ' '.join, match.group(1).split, match.group, len. | replace(match: re.Match[str]) -> str | file I/O | nested in _clean_latex_value<br>downstream: ' '.join, match.group(1).split, match.group, len | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6806::_normalize_ocr_tables_in_text</code> | _normalize_ocr_tables_in_text(text: str) -> tuple[str, int]<br>function | Coordinates text.replace('\r\n', '\n').replace('\r', '\n').split, len, _OCR_TABLE_BORDER_RE.match, output.append, '\n'.join, text.replace('\r\n', '\n').replace. | _normalize_ocr_tables_in_text(text: str) -> tuple[str, int] | file I/O | CLI/MCP/external caller<br>downstream: text.replace('\r\n', '\n').replace('\r', '\n').split, len, _OCR_TABLE_BORDER_RE.match, output.append, '\n'.join, text.replace('\r\n', '\n').replace | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6826::_parse_ocr_table</code> | _parse_ocr_table(lines: list[str], start_index: int) -> tuple[list[str], int] \| None<br>function | Coordinates len, any, re.findall, line.strip, _split_ocr_table_row, _markdown_table_row. | _parse_ocr_table(lines: list[str], start_index: int) -> tuple[list[str], int] \| None | none found statically | CLI/MCP/external caller<br>downstream: len, any, re.findall, line.strip, _split_ocr_table_row, _markdown_table_row | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6854::_split_ocr_table_row</code> | _split_ocr_table_row(line: str, column_count: int) -> list[str] \| None<br>function | Coordinates _MATH_CELL_RE.findall, len, cell.strip, leftover.replace, leftover.strip, re.split. | _split_ocr_table_row(line: str, column_count: int) -> list[str] \| None | file I/O | CLI/MCP/external caller<br>downstream: _MATH_CELL_RE.findall, len, cell.strip, leftover.replace, leftover.strip, re.split | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6875::_markdown_table_row</code> | _markdown_table_row(cells: list[str]) -> str<br>function | Coordinates ' \| '.join, cell.replace. | _markdown_table_row(cells: list[str]) -> str | file I/O | CLI/MCP/external caller<br>downstream: ' \| '.join, cell.replace | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6879::_update_review_task_questions</code> | _update_review_task_questions(task_id: str, updated_questions: list[Any], changed_items: list[dict[str, Any]]) -> None<br>function | Coordinates _connect_review_db, conn.execute, conn.execute('SELECT result_json, updated_at FROM import_pipeline_tasks WHERE task_id = ?', (str(task_id).strip(),)).fetchone, str, str(expected_updated_at or '').strip, _parse_json_dict. | _update_review_task_questions(task_id: str, updated_questions: list[Any], changed_items: list[dict[str, Any]]) -> None | SQLite | CLI/MCP/external caller<br>downstream: _connect_review_db, conn.execute, conn.execute('SELECT result_json, updated_at FROM import_pipeline_tasks WHERE task_id = ?', (str(task_id).strip(),)).fetchone, str, str(expected_updated_at or '').strip, _parse_json_dict | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6926::_safe_count</code> | _safe_count(conn: sqlite3.Connection, table_name: str) -> int \| None<br>function | Coordinates int, conn.execute(f'SELECT COUNT(*) FROM {table_name}').fetchone, conn.execute. | _safe_count(conn: sqlite3.Connection, table_name: str) -> int \| None | SQLite | CLI/MCP/external caller<br>downstream: int, conn.execute(f'SELECT COUNT(*) FROM {table_name}').fetchone, conn.execute | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6933::_fetch_questions</code> | _fetch_questions(conn: sqlite3.Connection, question_ids: list[str]) -> dict[str, sqlite3.Row]<br>function | Coordinates ','.join, conn.execute(f'\n SELECT question_id, canonical_title, question_type, difficulty, module\n FROM questions\n WHERE question_id IN ({placeholders})\n ', question_ids).fetchall, conn.execute. | _fetch_questions(conn: sqlite3.Connection, question_ids: list[str]) -> dict[str, sqlite3.Row] | SQLite | CLI/MCP/external caller<br>downstream: ','.join, conn.execute(f'\n SELECT question_id, canonical_title, question_type, difficulty, module\n FROM questions\n WHERE question_id IN ({placeholders})\n ', question_ids).fetchall, conn.execute | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6948::_fetch_topics</code> | _fetch_topics(conn: sqlite3.Connection, topic3_ids: list[str]) -> dict[str, sqlite3.Row]<br>function | Coordinates ','.join, conn.execute(f"\n SELECT topic1_id, topic1_name, topic2_id, topic2_name, topic3_id, topic3_name, source_chapter, status\n FROM knowledge_points\n WHERE topic3_id IN ({placeholders}) AND status = 'active'\n ", topic3_ids).fetchall, conn.execute. | _fetch_topics(conn: sqlite3.Connection, topic3_ids: list[str]) -> dict[str, sqlite3.Row] | SQLite | CLI/MCP/external caller<br>downstream: ','.join, conn.execute(f"\n SELECT topic1_id, topic1_name, topic2_id, topic2_name, topic3_id, topic3_name, source_chapter, status\n FROM knowledge_points\n WHERE topic3_id IN ({placeholders}) AND status = 'active'\n ", topic3_ids).fetchall, conn.execute | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6963::_topic_payload</code> | _topic_payload(row: sqlite3.Row) -> dict[str, Any]<br>function | Performs a local runtime calculation or state transition. | _topic_payload(row: sqlite3.Row) -> dict[str, Any] | SQLite | CLI/MCP/external caller<br>downstream: no direct call found | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6975::_short_id</code> | _short_id() -> str<br>function | Coordinates uuid.uuid4. | _short_id() -> str | none found statically | CLI/MCP/external caller<br>downstream: uuid.uuid4 | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6979::_parse_tags</code> | _parse_tags(raw: Any) -> list[str]<br>function | Coordinates isinstance, _normalize_tags, json.loads, str, str(raw).replace('гм', ',').replace('бв', ',').split, str(raw).replace('гм', ',').replace. | _parse_tags(raw: Any) -> list[str] | file I/O | CLI/MCP/external caller<br>downstream: isinstance, _normalize_tags, json.loads, str, str(raw).replace('гм', ',').replace('бв', ',').split, str(raw).replace('гм', ',').replace | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:6991::_raw_tags</code> | _raw_tags(raw: Any) -> list[str]<br>function | Coordinates isinstance, json.loads, item.strip, str(item).strip, str, str(raw).replace('гм', ',').replace('бв', ',').split. | _raw_tags(raw: Any) -> list[str] | file I/O | CLI/MCP/external caller<br>downstream: isinstance, json.loads, item.strip, str(item).strip, str, str(raw).replace('гм', ',').replace('бв', ',').split | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:7005::_parse_json_dict</code> | _parse_json_dict(raw: Any) -> dict[str, Any]<br>function | Coordinates isinstance, json.loads, str. | _parse_json_dict(raw: Any) -> dict[str, Any] | none found statically | CLI/MCP/external caller<br>downstream: isinstance, json.loads, str | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:7017::_preview_text</code> | _preview_text(raw: Any, limit: int) -> str<br>function | Coordinates ' '.join, str(raw or '').split, len, text[:limit].rstrip, str. | _preview_text(raw: Any, limit: int) -> str | none found statically | CLI/MCP/external caller<br>downstream: ' '.join, str(raw or '').split, len, text[:limit].rstrip, str | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:7024::_review_question_summary</code> | _review_question_summary(item: Any, index: int, risk_count: int \| None) -> dict[str, Any]<br>function | Coordinates str(item.get('question_id') or item.get('id') or item.get('draft_id') or '').strip, str(item.get('title') or item.get('canonical_title') or item.get('stem') or '').strip, str(item.get('stem') or item.get('stem_text') or item.get('content') or '').strip, isinstance, item.get, _preview_text. | _review_question_summary(item: Any, index: int, risk_count: int \| None) -> dict[str, Any] | none found statically | CLI/MCP/external caller<br>downstream: str(item.get('question_id') or item.get('id') or item.get('draft_id') or '').strip, str(item.get('title') or item.get('canonical_title') or item.get('stem') or '').strip, str(item.get('stem') or item.get('stem_text') or item.get('content') or '').strip, isinstance, item.get, _preview_text | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:7050::_review_knowledge_summary</code> | _review_knowledge_summary(item: Any, index: int) -> dict[str, Any]<br>function | Coordinates isinstance, item.get, _preview_text. | _review_knowledge_summary(item: Any, index: int) -> dict[str, Any] | none found statically | CLI/MCP/external caller<br>downstream: isinstance, item.get, _preview_text | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:7068::_normalize_tags</code> | _normalize_tags(value: Any) -> list[str]<br>function | Coordinates isinstance, set, value.replace('гм', ',').replace('бв', ',').split, str(item).strip, ' '.join, tag.casefold. | _normalize_tags(value: Any) -> list[str] | file I/O | CLI/MCP/external caller<br>downstream: isinstance, set, value.replace('гм', ',').replace('бв', ',').split, str(item).strip, ' '.join, tag.casefold | runtime / MCP gateway | keep |
## scripts/physics_vault_mcp_server.py

共 17 个函数记录

| 标识 | 签名 / 类别 | 职责 | 输入 ⇒ 输出 | 副作用 | 上游 / 下游 | 归属 | 优化提示 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| <code>scripts/physics_vault_mcp_server.py:4659::_split_merged_option_text</code> | _split_merged_option_text(source: str) -> list[tuple[str, str]]<br>function | Coordinates _extract_option_block, list, enumerate, _MERGED_OPTION_RE.finditer, len, match.group(1).upper. | _split_merged_option_text(source: str) -> list[tuple[str, str]] | none found statically | CLI/MCP/external caller<br>downstream: _extract_option_block, list, enumerate, _MERGED_OPTION_RE.finditer, len, match.group(1).upper | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:4677::_build_split_options</code> | _build_split_options(raw_options: Any, split_options: list[tuple[str, str]]) -> list[dict[str, Any]]<br>function | Coordinates isinstance. | _build_split_options(raw_options: Any, split_options: list[tuple[str, str]]) -> list[dict[str, Any]] | none found statically | CLI/MCP/external caller<br>downstream: isinstance | runtime / MCP gateway | keep |
| <code>scripts/physics_vault_mcp_server.py:4689::split_merged_options</code> | split_merged_options(task_id: str, question_ids: list[str] \| None, dry_run: bool, reason: str \| None, expected_updated_at: str \| None) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | split_merged_options(task_id: str, question_ids: list[str] \| None, dry_run: bool, reason: str \| None, expected_updated_at: str \| None) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, get_review_task_full, str(full.get('task', {}).get('task_id') or task_id).strip, full.get, isinstance, str(item).strip | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:4773::deduplicate_review_task_questions</code> | deduplicate_review_task_questions(task_id: str, dry_run: bool, reason: str \| None, expected_updated_at: str \| None) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | deduplicate_review_task_questions(task_id: str, dry_run: bool, reason: str \| None, expected_updated_at: str \| None) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, get_review_task_full, str(full.get('task', {}).get('task_id') or task_id).strip, full.get, isinstance, str(raw.get('question_id') or raw.get('id') or raw.get('draft_id') or '').strip | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:4855::update_review_task_draft</code> | update_review_task_draft(task_id: str, updates: list[dict[str, Any]], dry_run: bool, reason: str \| None, expected_updated_at: str \| None) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | update_review_task_draft(task_id: str, updates: list[dict[str, Any]], dry_run: bool, reason: str \| None, expected_updated_at: str \| None) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, get_review_task_full, str(full.get('task', {}).get('task_id') or task_id).strip, enumerate, len, full.get | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:4996::list_question_tags</code> | list_question_tags(query: str \| None, question_ids: list[str] \| None, limit: int) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | list_question_tags(query: str \| None, question_ids: list[str] \| None, limit: int) -> dict[str, Any] | SQLite?MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _query_question_tags, _parse_tags, items.append, _connect_formal_read_db, _table_exists | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5059::diagnose_tag_maintenance</code> | diagnose_tag_maintenance(query: str \| None, limit: int, min_similarity: float) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | diagnose_tag_maintenance(query: str \| None, limit: int, min_similarity: float) -> dict[str, Any] | SQLite?MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _diagnose_tag_maintenance, _tool_error, min, max, _formal_db_path | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5077::suggest_question_tags</code> | suggest_question_tags(question_ids: list[str]) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | suggest_question_tags(question_ids: list[str]) -> dict[str, Any] | SQLite?MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _suggest_question_tags, _tool_error, _formal_db_path, str, isinstance | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5089::maintain_question_tags</code> | maintain_question_tags(question_ids: list[str] \| None, add_tags: list[str] \| None, remove_tags: list[str] \| None, merge_map: dict[str, list[str]] \| None, dry_run: bool, reason: str \| None, create_catalog_tags: bool) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | maintain_question_tags(question_ids: list[str] \| None, add_tags: list[str] \| None, remove_tags: list[str] \| None, merge_map: dict[str, list[str]] \| None, dry_run: bool, reason: str \| None, create_catalog_tags: bool) -> dict[str, Any] | SQLite?MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _maintain_question_tags, _tool_error, bool, _formal_db_path, str | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5115::list_change_batches</code> | list_change_batches(change_type: str \| None, status: str \| None, limit: int) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | list_change_batches(change_type: str \| None, status: str \| None, limit: int) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _change_audit_service().list_batches, _change_audit_service | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5129::get_change_batch</code> | get_change_batch(batch_id: str) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | get_change_batch(batch_id: str) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _change_audit_service().get_batch, _change_audit_service | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5135::rollback_change_batch</code> | rollback_change_batch(batch_id: str, dry_run: bool, reason: str \| None, allow_conflicts: bool) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | rollback_change_batch(batch_id: str, dry_run: bool, reason: str \| None, allow_conflicts: bool) -> dict[str, Any] | SQLite?MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, _change_audit_service().rollback_batch, _connect_formal_write_db, _ensure_review_queue_outbox_schema, conn.execute, conn.commit | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5166::batch_replace_question_tags</code> | batch_replace_question_tags(updates: list[dict[str, Any]], dry_run: bool, reason: str \| None) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | batch_replace_question_tags(updates: list[dict[str, Any]], dry_run: bool, reason: str \| None) -> dict[str, Any] | SQLite?MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, enumerate, ','.join, len, str(item.get('question_id') or '').strip, _normalize_tags | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5277::return_question_to_review</code> | return_question_to_review(question_id: str, reason: str, dry_run: bool, operation_id: str \| None) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | return_question_to_review(question_id: str, reason: str, dry_run: bool, operation_id: str \| None) -> dict[str, Any] | SQLite?MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, str(question_id or '').strip, str(operation_id or '').strip, _tool_error, _connect_formal_write_db, _ensure_review_queue_outbox_schema | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5399::reconcile_review_queue_outbox</code> | reconcile_review_queue_outbox(limit: int) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | reconcile_review_queue_outbox(limit: int) -> dict[str, Any] | SQLite?MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, min, sum, max, _connect_formal_write_db, _ensure_review_queue_outbox_schema | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5425::batch_replace_question_knowledge_points</code> | batch_replace_question_knowledge_points(updates: list[dict[str, Any]], dry_run: bool, reason: str \| None) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | batch_replace_question_knowledge_points(updates: list[dict[str, Any]], dry_run: bool, reason: str \| None) -> dict[str, Any] | SQLite?MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, enumerate, sorted, len, str(raw.get('question_id') or '').strip, raw.get | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
| <code>scripts/physics_vault_mcp_server.py:5565::find_similar_questions</code> | find_similar_questions(question_id: str, limit: int, same_question_type: bool, same_knowledge_point: bool, difficulty_tolerance: int) -> dict[str, Any]<br>MCP tool | Registers or handles an MCP operation. | find_similar_questions(question_id: str, limit: int, same_question_type: bool, same_knowledge_point: bool, difficulty_tolerance: int) -> dict[str, Any] | MCP registration/tool | CLI/MCP/external caller<br>downstream: server.tool, SimilarQuestionsService().find_similar, _dump_model, SimilarQuestionsService, min, int | runtime / MCP gateway | split orchestration/I-O boundary if it grows; otherwise keep |
<!-- FUNCTION-MAP-END -->
