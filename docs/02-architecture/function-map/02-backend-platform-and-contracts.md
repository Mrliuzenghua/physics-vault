# 函数地图 3：后端平台与契约

> 覆盖：`apps/api/src/physics_vault_api/` 根目录全部生产 `.py`（含兼容入口）、`routers/**/*.py`、`repositories/**/*.py`、`schemas/**/*.py` 的全部生产函数、类方法、私有方法及 async 方法。`services/` 与 `tasks/` 由代码地图 1 负责；测试、生成文件和依赖包不在范围内。
>
> 扫描方式：Python AST 深度遍历函数/异步函数与类，再以 `rg` 的 `def` / `async def` 声明交叉核对。后续源码变动时，所属工作流必须同步更新本分册。

## 扫描结果

| 项目 | 结果 |
| --- | --- |
| 扫描文件数 | 106 |
| 扫描函数数 | 569 |
| 扫描类数（含 Pydantic Schema） | 284 |
| 未解析文件 | 0 |
| 范围分布 | 根目录平台模块 11 文件 / 98 函数；Router 41 文件 / 234 函数；Repository 19 文件 / 234 函数；Schema 35 文件 / 3 函数（另有 211 个 Schema 类清单） |

## 阅读约定

每条函数记录均含：**标识**、**签名**、**职责**、**输入 ⇒ 输出**、**副作用**、**上游 / 下游**、**归属**、**优化提示**。行号来自 AST；嵌套 Router 端点也按真实定义行记录。

- Router 的 HTTP 副作用表示 FastAPI 请求/响应处理；数据库、文件或 MCP 通常由下游 Service/专用依赖执行。
- Repository 的数据库副作用来自函数体中的连接、SQL、事务与行读取调用；纯映射辅助明确标为无。
- Schema 的模型清单说明 Pydantic 字段解析、默认值和约束职责；它们没有 HTTP、数据库或文件副作用。

## ?__init__.py? (1 functions)
- **标识**: ?__init__.py:8::__getattr__?; **签名**: ?def __getattr__(name: str) -> Any?; **职责**: Performs private platform support.; **输入 ⇒ 输出**: signature parameters => ?Any?; **副作用**: none / in-memory; **上游 / 下游**: upstream startup or same-module callers; downstream AttributeError; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
## ?app.py? (8 functions)
- **标识**: ?app.py:18::_resolve_project_file?; **签名**: ?def _resolve_project_file(file_path: str) -> Path?; **职责**: Performs private platform support.; **输入 ⇒ 输出**: signature parameters => ?Path?; **副作用**: none / in-memory; **上游 / 下游**: upstream startup or same-module callers; downstream project_root().resolve, (root / file_path).resolve, HTTPException, project_root; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?app.py:26::_cache_response?; **签名**: ?def _cache_response(response: FileResponse, max_age: int = 86400) -> FileResponse?; **职责**: Performs private platform support.; **输入 ⇒ 输出**: signature parameters => ?FileResponse?; **副作用**: none / in-memory; **上游 / 下游**: upstream startup or same-module callers; downstream response.headers.setdefault; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?app.py:31::_thumbnail_cache_path?; **签名**: ?def _thumbnail_cache_path(target: Path, width: int) -> Path?; **职责**: Performs private platform support.; **输入 ⇒ 输出**: signature parameters => ?Path?; **副作用**: none / in-memory; **上游 / 下游**: upstream startup or same-module callers; downstream target.stat, hashlib.sha1(key.encode('utf-8')).hexdigest, hashlib.sha1, key.encode; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?app.py:38::_build_thumbnail?; **签名**: ?def _build_thumbnail(target: Path, cache_path: Path, width: int) -> None?; **职责**: Performs private platform support.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: file/upload; **上游 / 下游**: upstream startup or same-module callers; downstream cache_path.parent.mkdir, Image.open, ImageOps.exif_transpose, image.thumbnail; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?app.py:52::create_app?; **签名**: ?def create_app() -> FastAPI?; **职责**: Performs platform startup, configuration, database-schema, path, or compatibility work.; **输入 ⇒ 输出**: signature parameters => ?FastAPI?; **副作用**: file/upload; **上游 / 下游**: upstream startup or same-module callers; downstream initialize_database, FastAPI, app.add_middleware, app.get; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?app.py:68::create_app.health?; **签名**: ?def health() -> dict[str, str]?; **职责**: Performs platform startup, configuration, database-schema, path, or compatibility work.; **输入 ⇒ 输出**: signature parameters => ?dict[str, str]?; **副作用**: none / in-memory; **上游 / 下游**: upstream FastAPI GET /health; downstream app.get; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?app.py:72::create_app.serve_file?; **签名**: ?def serve_file(file_path: str) -> FileResponse?; **职责**: Performs platform startup, configuration, database-schema, path, or compatibility work.; **输入 ⇒ 输出**: signature parameters => ?FileResponse?; **副作用**: file/upload; **上游 / 下游**: upstream FastAPI GET /files/{file_path:path}; downstream app.get, _cache_response, FileResponse, _resolve_project_file; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?app.py:76::create_app.serve_thumbnail?; **签名**: ?def serve_thumbnail(file_path: str, w: int = Query(default=720, ge=120, le=1600)) -> FileResponse?; **职责**: Performs platform startup, configuration, database-schema, path, or compatibility work.; **输入 ⇒ 输出**: signature parameters => ?FileResponse?; **副作用**: file/upload; **上游 / 下游**: upstream FastAPI GET /thumbs/{file_path:path}; downstream app.get, Query, _resolve_project_file, _thumbnail_cache_path; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
## ?application.py? (7 functions)
- **标识**: ?application.py:114::_build_import_pipeline_service?; **签名**: ?def _build_import_pipeline_service(task_repo: SQLiteImportTaskRepository, mcp_gateway: McpGatewayService) -> ImportPipelineService?; **职责**: Performs private platform support.; **输入 ⇒ 输出**: signature parameters => ?ImportPipelineService?; **副作用**: none / in-memory; **上游 / 下游**: upstream startup or same-module callers; downstream ImportPipelineService, PandocAdapter, DocumentCleaningService, StructuredQuestionParsingService; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?application.py:138::ExportWorkerContainer.build?; **签名**: ?def build(cls, task_queue_settings: TaskQueueSettings | None = None) -> 'ExportWorkerContainer'?; **职责**: Performs platform startup, configuration, database-schema, path, or compatibility work.; **输入 ⇒ 输出**: signature parameters => ?'ExportWorkerContainer'?; **副作用**: none / in-memory; **上游 / 下游**: upstream startup or same-module callers; downstream SQLiteImportTaskRepository, task_repo.recover_stale_tasks, cls, TaskQueueSettings.from_env; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?application.py:157::WorkerContainer.build?; **签名**: ?def build(cls, mcp_settings: McpSettings | None = None, task_queue_settings: TaskQueueSettings | None = None, recover_stale: bool = True) -> 'WorkerContainer'?; **职责**: Performs platform startup, configuration, database-schema, path, or compatibility work.; **输入 ⇒ 输出**: signature parameters => ?'WorkerContainer'?; **副作用**: none / in-memory; **上游 / 下游**: upstream startup or same-module callers; downstream McpGatewayService, _should_restore_runtime_ai_config, SQLiteImportTaskRepository, cls; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?application.py:297::ApplicationContainer.build?; **签名**: ?def build(cls, mcp_settings: McpSettings | None = None) -> 'ApplicationContainer'?; **职责**: Performs platform startup, configuration, database-schema, path, or compatibility work.; **输入 ⇒ 输出**: signature parameters => ?'ApplicationContainer'?; **副作用**: none / in-memory; **上游 / 下游**: upstream startup or same-module callers; downstream McpGatewayService, ImageCatalogRepository, EmbeddingStatusRepository, _should_restore_runtime_ai_config; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?application.py:380::ApplicationContainer.router_manifest?; **签名**: ?def router_manifest(self) -> Iterable[tuple[str, APIRouter]]?; **职责**: Performs platform startup, configuration, database-schema, path, or compatibility work.; **输入 ⇒ 输出**: signature parameters => ?Iterable[tuple[str, APIRouter]]?; **副作用**: none / in-memory; **上游 / 下游**: upstream startup or same-module callers; downstream build_home_router, build_import_pipeline_router, build_lesson_exports_router, build_lesson_documents_router; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?application.py:445::ApplicationContainer.routers?; **签名**: ?def routers(self) -> Iterable[APIRouter]?; **职责**: Performs platform startup, configuration, database-schema, path, or compatibility work.; **输入 ⇒ 输出**: signature parameters => ?Iterable[APIRouter]?; **副作用**: none / in-memory; **上游 / 下游**: upstream startup or same-module callers; downstream self.router_manifest; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?application.py:452::_should_restore_runtime_ai_config?; **签名**: ?def _should_restore_runtime_ai_config(settings: McpSettings, explicit_settings: bool) -> bool?; **职责**: Performs private platform support.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: environment configuration; **上游 / 下游**: upstream startup or same-module callers; downstream os.getenv, os.getenv('PHYSICS_RESTORE_RUNTIME_AI_CONFIG', 'true').strip().lower, os.getenv('PHYSICS_RESTORE_RUNTIME_AI_CONFIG', 'true').strip; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
## ?bootstrap.py? (1 functions)
- **标识**: ?bootstrap.py:7::configure_workspace_imports?; **签名**: ?def configure_workspace_imports() -> None?; **职责**: Performs platform startup, configuration, database-schema, path, or compatibility work.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: file/upload; **上游 / 下游**: upstream startup or same-module callers; downstream packages_root.exists, str, Path(__file__).resolve, sys.path.insert; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
## ?config.py? (8 functions)
- **标识**: ?config.py:8::_env_flag?; **签名**: ?def _env_flag(name: str, default: bool) -> bool?; **职责**: Performs private platform support.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: environment configuration; **上游 / 下游**: upstream startup or same-module callers; downstream os.getenv, raw_value.strip().lower, raw_value.strip; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?config.py:15::_env_int?; **签名**: ?def _env_int(name: str, default: int) -> int?; **职责**: Performs private platform support.; **输入 ⇒ 输出**: signature parameters => ?int?; **副作用**: environment configuration; **上游 / 下游**: upstream startup or same-module callers; downstream os.getenv, int; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?config.py:38::McpSettings.command_map?; **签名**: ?def command_map(self) -> dict[str, str]?; **职责**: Performs platform startup, configuration, database-schema, path, or compatibility work.; **输入 ⇒ 输出**: signature parameters => ?dict[str, str]?; **副作用**: none / in-memory; **上游 / 下游**: upstream startup or same-module callers; downstream no explicit call; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?config.py:49::McpSettings.from_env?; **签名**: ?def from_env(cls) -> 'McpSettings'?; **职责**: Builds configuration from external settings.; **输入 ⇒ 输出**: signature parameters => ?'McpSettings'?; **副作用**: file/upload, environment configuration; **上游 / 下游**: upstream startup or same-module callers; downstream cls, Path(__file__).resolve, os.getenv('PHYSICS_MCP_MODE', 'mock').strip().lower, os.getenv('PHYSICS_MCP_VL_COMMAND', '').strip; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?config.py:63::McpSettings.resolved_mode?; **签名**: ?def resolved_mode(self) -> str?; **职责**: Performs platform startup, configuration, database-schema, path, or compatibility work.; **输入 ⇒ 输出**: signature parameters => ?str?; **副作用**: none / in-memory; **上游 / 下游**: upstream startup or same-module callers; downstream no explicit call; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?config.py:88::TaskQueueSettings.max_attempts?; **签名**: ?def max_attempts(self) -> int?; **职责**: Performs platform startup, configuration, database-schema, path, or compatibility work.; **输入 ⇒ 输出**: signature parameters => ?int?; **副作用**: none / in-memory; **上游 / 下游**: upstream startup or same-module callers; downstream no explicit call; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?config.py:92::TaskQueueSettings.from_env?; **签名**: ?def from_env(cls) -> 'TaskQueueSettings'?; **职责**: Builds configuration from external settings.; **输入 ⇒ 输出**: signature parameters => ?'TaskQueueSettings'?; **副作用**: environment configuration; **上游 / 下游**: upstream startup or same-module callers; downstream os.getenv('PHYSICS_TASK_BROKER_URL', 'redis://127.0.0.1:6379/0').strip, bool, cls, os.getenv; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?config.py:128::LessonExportSettings.from_env?; **签名**: ?def from_env(cls) -> 'LessonExportSettings'?; **职责**: Builds configuration from external settings.; **输入 ⇒ 输出**: signature parameters => ?'LessonExportSettings'?; **副作用**: none / in-memory; **上游 / 下游**: upstream startup or same-module callers; downstream cls, max, _env_int; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
## ?database.py? (3 functions)
- **标识**: ?database.py:11::apply_connection_pragmas?; **签名**: ?def apply_connection_pragmas(conn: sqlite3.Connection, writable: bool = True) -> None?; **职责**: Performs platform startup, configuration, database-schema, path, or compatibility work.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream startup or same-module callers; downstream conn.execute; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?database.py:20::connect_db?; **签名**: ?def connect_db(db_path: str | Path | None = None, writable: bool = True) -> sqlite3.Connection?; **职责**: Performs platform startup, configuration, database-schema, path, or compatibility work.; **输入 ⇒ 输出**: signature parameters => ?sqlite3.Connection?; **副作用**: database read/write; **上游 / 下游**: upstream startup or same-module callers; downstream sqlite3.connect, apply_connection_pragmas, Path, default_db_path; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?database.py:32::db_transaction?; **签名**: ?def db_transaction(db_path: str | Path | None = None) -> Iterator[sqlite3.Connection]?; **职责**: Performs platform startup, configuration, database-schema, path, or compatibility work.; **输入 ⇒ 输出**: signature parameters => ?Iterator[sqlite3.Connection]?; **副作用**: database read/write; **上游 / 下游**: upstream startup or same-module callers; downstream connect_db, conn.commit, conn.close, conn.rollback; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
## ?db_schema.py? (5 functions)
- **标识**: ?db_schema.py:478::ensure_database_parent?; **签名**: ?def ensure_database_parent(db_path: Path | None = None) -> Path?; **职责**: Performs platform startup, configuration, database-schema, path, or compatibility work.; **输入 ⇒ 输出**: signature parameters => ?Path?; **副作用**: file/upload; **上游 / 下游**: upstream startup or same-module callers; downstream resolved.parent.mkdir, default_db_path; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?db_schema.py:484::initialize_database?; **签名**: ?def initialize_database(db_path: Path | str | None = None) -> Path?; **职责**: Performs platform startup, configuration, database-schema, path, or compatibility work.; **输入 ⇒ 输出**: signature parameters => ?Path?; **副作用**: database read/write; **上游 / 下游**: upstream startup or same-module callers; downstream ensure_database_parent, connect_db, conn.executescript, apply_schema_migrations; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?db_schema.py:503::ensure_import_task_lifecycle_schema?; **签名**: ?def ensure_import_task_lifecycle_schema(conn: sqlite3.Connection) -> None?; **职责**: Performs platform startup, configuration, database-schema, path, or compatibility work.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream startup or same-module callers; downstream conn.execute, IMPORT_TASK_LIFECYCLE_COLUMNS.items, str, isinstance; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?db_schema.py:587::apply_schema_migrations?; **签名**: ?def apply_schema_migrations(conn: sqlite3.Connection) -> None?; **职责**: Performs platform startup, configuration, database-schema, path, or compatibility work.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write, file/upload; **上游 / 下游**: upstream startup or same-module callers; downstream conn.execute, sorted, MIGRATIONS_DIR.exists, MIGRATIONS_DIR.glob; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
- **标识**: ?db_schema.py:627::reset_database?; **签名**: ?def reset_database(db_path: Path | str | None = None, backup: bool = True, backup_dir: Path | str | None = None) -> tuple[Path, Path | None]?; **职责**: Performs platform startup, configuration, database-schema, path, or compatibility work.; **输入 ⇒ 输出**: signature parameters => ?tuple[Path, Path | None]?; **副作用**: file/upload; **上游 / 下游**: upstream startup or same-module callers; downstream ensure_database_parent, resolved.exists, initialize_database, resolved.unlink; **归属**: backend platform / Platform; **优化提示**: preserve external contract; extract only pure mapping/protocol helpers.
### Source file: legacy_app.py
- **标识**: ?legacy_app.py:30::load_env_file?; **签名**: ?def load_env_file(...) -> None?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream path.read_text(encoding='utf-8').splitlines, path.exists, raw_line.strip, line.split, key.strip, value.strip().strip('"').strip, path.read_text, line.startswith, os.environ.setdefa; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:61::get_connection?; **签名**: ?def get_connection(...) -> sqlite3.Connection?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?sqlite3.Connection?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream connect_db, DB_PATH.exists, FileNotFoundError; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:67::row_to_dict?; **签名**: ?def row_to_dict(...) -> dict[str, Any]?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream row.keys; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:71::parse_json_list?; **签名**: ?def parse_json_list(...) -> list[Any]?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?list[Any]?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream json.loads, isinstance; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:81::fetch_distinct_strings?; **签名**: ?def fetch_distinct_strings(...) -> list[str]?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?list[str]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream connection.execute(sql).fetchall, connection.execute; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:85::fetch_knowledge_points_for_questions?; **签名**: ?def fetch_knowledge_points_for_questions(...) -> dict[str, list[dict[str, Any]]]?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?dict[str, list[dict[str, Any]]]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream ','.join, connection.execute(f'\n        SELECT\n            question_id,\n            rank,\n            topic1_id,\n            topic1_name,\n            topic2_id,\n            ; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:121::question_payload?; **签名**: ?def question_payload(...) -> dict[str, Any]?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream parse_json_list, knowledge_map.get, payload.get, str, payload.pop, row_to_dict(row).items, row_to_dict; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:131::resolve_embedding_client?; **签名**: ?def resolve_embedding_client(...) -> OpenAI?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?OpenAI?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream provider.strip().lower, os.environ.get, OpenAI, HTTPException, provider.strip; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:145::resolve_embedding_model?; **签名**: ?def resolve_embedding_model(...) -> str?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?str?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream provider.strip().lower, provider.strip; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:152::utc_now?; **签名**: ?def utc_now(...) -> str?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?str?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream datetime.now(UTC).replace(microsecond=0).isoformat().replace, datetime.now(UTC).replace(microsecond=0).isoformat, datetime.now(UTC).replace, datetime.now; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: legacy_app.py
- **标识**: ?legacy_app.py:156::get_encoding?; **签名**: ?def get_encoding(...) -> unannotated?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?unannotated?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream model_name.startswith, tiktoken.get_encoding; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:162::truncate_text?; **签名**: ?def truncate_text(...) -> tuple[str, int]?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?tuple[str, int]?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream get_encoding, encoding.encode, encoding.decode, len; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:171::compute_content_hash?; **签名**: ?def compute_content_hash(...) -> str?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?str?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream hashlib.sha256(text.encode('utf-8')).hexdigest, hashlib.sha256, text.encode; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:175::build_question_embedding_text?; **签名**: ?def build_question_embedding_text(...) -> str?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?str?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream '；'.join, sections.append, '\n'.join(sections).strip, '\n'.join, (row['stem_text'] or '').strip; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:196::create_processing_run?; **签名**: ?def create_processing_run(...) -> str?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?str?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream connection.execute, connection.commit, datetime.now(UTC).strftime, utc_now, json.dumps, datetime.now, uuid.uuid4; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:218::finish_processing_run?; **签名**: ?def finish_processing_run(...) -> None?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream connection.execute, connection.commit, utc_now, json.dumps; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:230::upsert_question_embedding?; **签名**: ?def upsert_question_embedding(...) -> None?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream f'EMB-{question_id}-{vector_type}-{model_name}'.replace, connection.execute, json.dumps; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:269::parse_vector_json?; **签名**: ?def parse_vector_json(...) -> list[float]?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?list[float]?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream json.loads, isinstance, floats.append, float; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:287::cosine_similarity?; **签名**: ?def cosine_similarity(...) -> float?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?float?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream sum, math.sqrt, len, zip; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:298::fetch_question_candidates?; **签名**: ?def fetch_question_candidates(...) -> list[sqlite3.Row]?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?list[sqlite3.Row]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream add_exact, connection.execute(sql, params).fetchall, where.append, params.extend, add_knowledge_exists, params.append, ' AND '.join, rank_params.append, int, connection.execute; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: legacy_app.py
- **标识**: ?legacy_app.py:347::fetch_question_candidates.add_exact?; **签名**: ?def add_exact(...) -> None?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream where.append, params.append; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:352::fetch_question_candidates.add_knowledge_exists?; **签名**: ?def add_knowledge_exists(...) -> None?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream where.append, params.extend, rank_params.append; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:458::compute_keyword_match?; **签名**: ?def compute_keyword_match(...) -> bool?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream query.strip().lower, (row['canonical_title'] or '').lower, (row['stem_text'] or '').lower, query.strip; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:469::semantic_similarity_for_row?; **签名**: ?def semantic_similarity_for_row(...) -> float | None?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?float | None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream embedding_map.get, cosine_similarity; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:483::topic_consistency_bonus?; **签名**: ?def topic_consistency_bonus(...) -> float?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?float?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream none; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:638::serve_file?; **签名**: ?def serve_file(...) -> unannotated?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?unannotated?; **副作用**: file/upload; **上游 / 下游**: upstream module callers; downstream app.get, FileResponse, response.headers.setdefault, HTTPException, str, full_path.exists, full_path.is_file; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:648::index?; **签名**: ?def index(...) -> str?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?str?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream none; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:1085::health?; **签名**: ?def health(...) -> HealthResponse?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?HealthResponse?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream app.get, HealthResponse, closing, connection.execute, get_connection, str; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:1103::unified_search_questions?; **签名**: ?def unified_search_questions(...) -> list[UnifiedQuestionSearchItem]?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?list[UnifiedQuestionSearchItem]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream app.get, Query, resolve_embedding_model, scored.sort, HTTPException, resolve_embedding_client, client.embeddings.create, closing, fetch_knowledge_points_for_questions, semantic_sim; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:1338::update_question?; **签名**: ?def update_question(...) -> dict?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?dict?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream QuestionWriteService, service.save_batch, row_to_dict, parse_json_list, knowledge_map.get, payload.get, HTTPException, closing, conn.execute(sql, (question_id,)).fetchone, fetch_kn; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: legacy_app.py
- **标识**: ?legacy_app.py:1444::get_filter_facets?; **签名**: ?def get_filter_facets(...) -> FilterFacets?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?FilterFacets?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream app.get, FilterFacets, closing, fetch_distinct_strings, get_connection, connection.execute('SELECT DISTINCT year FROM papers WHERE year IS NOT NULL ORDER BY year DESC').fetchall, c; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:1482::list_review_queue?; **签名**: ?def list_review_queue(...) -> list[ReviewQueueItem]?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?list[ReviewQueueItem]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream app.get, Query, closing, connection.execute(sql, (status, status, queue_type, queue_type, limit, offset)).fetchall, ReviewQueueItem, get_connection, connection.execute, row_to_dict; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:1529::review_question?; **签名**: ?def review_question(...) -> ReviewActionResponse?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?ReviewActionResponse?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream ReviewActionResponse, closing, connection.execute('SELECT question_id, status FROM questions WHERE question_id = ?', (question_id,)).fetchone, connection.execute, connection.commit; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:1606::propose_fix?; **签名**: ?def propose_fix(...) -> ProposeFixResponse?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?ProposeFixResponse?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream ProposeFixResponse, closing, connection.execute('SELECT question_id FROM questions WHERE question_id = ?', (question_id,)).fetchone, connection.execute, connection.commit, get_conn; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:1658::list_pending_fixes?; **签名**: ?def list_pending_fixes(...) -> list[PendingFixItem]?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?list[PendingFixItem]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, connection.execute(sql).fetchall, row_to_dict, payload.get, items.append, get_connection, json.loads, PendingFixItem, connection.execute, d.pop; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:1704::list_rejected_questions?; **签名**: ?def list_rejected_questions(...) -> list[RejectedQuestionItem]?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?list[RejectedQuestionItem]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream Query, closing, connection.execute(sql, (limit, offset)).fetchall, RejectedQuestionItem, get_connection, connection.execute, row_to_dict; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:1760::get_fix_detail?; **签名**: ?def get_fix_detail(...) -> FixDetailResponse?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?FixDetailResponse?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream row_to_dict, payload.get, FixDetailResponse, closing, connection.execute('\n            SELECT rq.review_id, rq.entity_id AS question_id, rq.queue_type, rq.status,\n               ; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:1801::decide_fix?; **签名**: ?def decide_fix(...) -> DecideFixResponse?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?DecideFixResponse?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream DecideFixResponse, closing, connection.execute('SELECT review_id, entity_id, status, payload_json FROM review_queue WHERE review_id = ?', (review_id,)).fetchone, connection.commit,; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:1863::report_issue?; **签名**: ?def report_issue(...) -> ReportIssueResponse?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?ReportIssueResponse?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream ReportIssueResponse, closing, connection.execute('SELECT question_id FROM questions WHERE question_id = ?', (question_id,)).fetchone, connection.execute, connection.commit, get_con; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:1904::get_review_history?; **签名**: ?def get_review_history(...) -> list[ReviewQueueItem]?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?list[ReviewQueueItem]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, connection.execute(sql, (question_id,)).fetchall, ReviewQueueItem, get_connection, connection.execute, row_to_dict; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: legacy_app.py
- **标识**: ?legacy_app.py:1921::list_question_versions?; **签名**: ?def list_question_versions(...) -> list[QuestionVersionSummary]?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?list[QuestionVersionSummary]?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream QuestionWriteRepository, repo.list_versions, QuestionVersionSummary, HTTPException; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:1932::get_question_version?; **签名**: ?def get_question_version(...) -> QuestionVersionDetail?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?QuestionVersionDetail?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream QuestionWriteRepository, QuestionVersionDetail, repo.get_version, HTTPException; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:1947::rollback_question_version?; **签名**: ?def rollback_question_version(...) -> dict?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?dict?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream QuestionWriteRepository, QuestionWriteService, service.save_batch, repo.get_version, HTTPException, snapshot.get, isinstance, json.loads, v.get, snapshot.get('stem_text', '').split; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:2031::import_question?; **签名**: ?def import_question(...) -> ImportQuestionResponse?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?ImportQuestionResponse?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream (content.get('stem') or '').strip, classification.get, source.get, content.get, '\n'.join, ImportQuestionResponse, HTTPException, text_parts.append, img.get, closing, connection.ex; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?legacy_app.py:2226::build_question_embeddings?; **签名**: ?def build_question_embeddings(...) -> EmbeddingBuildResponse?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?EmbeddingBuildResponse?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream payload.provider.strip().lower, resolve_embedding_model, EmbeddingBuildResponse, resolve_embedding_client, ','.join, params.extend, params.append, closing, connection.execute(sql, ; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: paths.py
- **标识**: ?paths.py:7::project_root?; **签名**: ?def project_root(...) -> Path?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?Path?; **副作用**: file/upload; **上游 / 下游**: upstream module callers; downstream Path(__file__).resolve, Path; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?paths.py:11::data_root?; **签名**: ?def data_root(...) -> Path?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?Path?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream project_root; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?paths.py:15::_configured_path?; **签名**: ?def _configured_path(...) -> Path?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?Path?; **副作用**: file/upload; **上游 / 下游**: upstream module callers; downstream os.getenv(env_name, '').strip, Path(configured).expanduser, path.is_absolute, os.getenv, Path, project_root; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?paths.py:23::default_db_path?; **签名**: ?def default_db_path(...) -> Path?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?Path?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream _configured_path, data_root; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?paths.py:30::default_review_db_path?; **签名**: ?def default_review_db_path(...) -> Path?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?Path?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream _configured_path, data_root; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: paths.py
- **标识**: ?paths.py:37::default_assets_dir?; **签名**: ?def default_assets_dir(...) -> Path?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?Path?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream data_root; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?paths.py:41::default_backups_dir?; **签名**: ?def default_backups_dir(...) -> Path?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?Path?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream data_root; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?paths.py:45::default_import_batches_dir?; **签名**: ?def default_import_batches_dir(...) -> Path?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?Path?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream data_root; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?paths.py:49::default_exports_dir?; **签名**: ?def default_exports_dir(...) -> Path?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?Path?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream _configured_path, data_root; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: runtime_config.py
- **标识**: ?runtime_config.py:41::RuntimeAiConfig.is_configured?; **签名**: ?def is_configured(...) -> bool?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream bool; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?runtime_config.py:46::RuntimeAiConfig.to_dict?; **签名**: ?def to_dict(...) -> dict[str, Any]?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream none; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?runtime_config.py:53::RuntimeAiConfig.from_dict?; **签名**: ?def from_dict(...) -> 'RuntimeAiConfig'?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?'RuntimeAiConfig'?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream cls, str, service_type.lower, base_url.lower, model_name.lower, AiServiceConfig, _parse_svc, raw.get, lower_model.startswith, int, data.get, defaults.model_name.lower, default_vl_c; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?runtime_config.py:54::RuntimeAiConfig.from_dict._parse_svc?; **签名**: ?def _parse_svc(...) -> AiServiceConfig?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?AiServiceConfig?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream str, service_type.lower, base_url.lower, model_name.lower, AiServiceConfig, raw.get, lower_model.startswith, int, defaults.model_name.lower; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?runtime_config.py:109::_load_from_file?; **签名**: ?def _load_from_file(...) -> RuntimeAiConfig | None?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?RuntimeAiConfig | None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream dict.fromkeys, json.loads, RuntimeAiConfig.from_dict, config_file.exists, config_file.read_text, cfg.is_configured, logger.info, logger.warning, _CONFIG_FILE.parent.mkdir, _CONFIG_; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?runtime_config.py:127::default_vl_config?; **签名**: ?def default_vl_config(...) -> AiServiceConfig?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?AiServiceConfig?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream AiServiceConfig, os.getenv, int; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: runtime_config.py
- **标识**: ?runtime_config.py:139::default_llm_config?; **签名**: ?def default_llm_config(...) -> AiServiceConfig?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?AiServiceConfig?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream AiServiceConfig, os.getenv, int; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?runtime_config.py:151::default_runtime_config?; **签名**: ?def default_runtime_config(...) -> RuntimeAiConfig?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?RuntimeAiConfig?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream RuntimeAiConfig, default_vl_config, default_llm_config; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?runtime_config.py:155::_save_to_file?; **签名**: ?def _save_to_file(...) -> None?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream _CONFIG_FILE.parent.mkdir, _CONFIG_FILE.write_text, json.dumps, logger.warning, config.to_dict; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?runtime_config.py:167::get_runtime_config?; **签名**: ?def get_runtime_config(...) -> RuntimeAiConfig?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?RuntimeAiConfig?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream none; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?runtime_config.py:172::update_runtime_config?; **签名**: ?def update_runtime_config(...) -> None?; **职责**: Performs platform startup/configuration/path/compatibility work.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream _save_to_file; **归属**: backend / Platform; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/agents.py
- **标识**: ?routers/agents.py:21::build_agents_router?; **签名**: ?def build_agents_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, router.post, ClaudeCodeAgentService, service.get_config, service.update_config, service.test_claude_code, service.cleanup_review_latex, StreamingResponse, Ag; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/agents.py:26::build_agents_router.get_config?; **签名**: ?def get_config(...) -> AgentConfigResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?AgentConfigResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.get_config; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/agents.py:30::build_agents_router.update_config?; **签名**: ?def update_config(...) -> AgentConfigResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?AgentConfigResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.update_config, AgentConfig.model_validate, payload.model_dump; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/agents.py:34::build_agents_router.test_claude_code?; **签名**: ?def test_claude_code(...) -> AgentTestResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?AgentTestResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.test_claude_code; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/agents.py:38::build_agents_router.review_latex_cleanup?; **签名**: ?def review_latex_cleanup(...) -> ReviewLatexCleanupResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ReviewLatexCleanupResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.cleanup_review_latex, HTTPException, result.model_dump; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/agents.py
- **标识**: ?routers/agents.py:49::build_agents_router.question_picker?; **签名**: ?async def question_picker(...) -> QuestionPickerAgentResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?QuestionPickerAgentResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.pick_questions, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/agents.py:59::build_agents_router.question_picker_stream?; **签名**: ?async def question_picker_stream(...) -> StreamingResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?StreamingResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, StreamingResponse, event_lines, service.stream_pick_questions, json.dumps, event.model_dump; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/agents.py:60::build_agents_router.question_picker_stream.event_lines?; **签名**: ?async def event_lines(...) -> unannotated?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?unannotated?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream service.stream_pick_questions, json.dumps, event.model_dump; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/ai_assistant.py
- **标识**: ?routers/ai_assistant.py:9::build_ai_assistant_router?; **签名**: ?def build_ai_assistant_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.post, AiAssistantService, service.chat, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/ai_assistant.py:16::build_ai_assistant_router.chat?; **签名**: ?async def chat(...) -> AiAssistantChatResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?AiAssistantChatResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.chat, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/ai_generation.py
- **标识**: ?routers/ai_generation.py:20::build_ai_generation_router?; **签名**: ?def build_ai_generation_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.post, router.delete, service.clear_knowledge_cache, service.delete_knowledge_cache_entry, HTTPException, service.generate_analysis, service.generate_knowledge, st; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/ai_generation.py:33::build_ai_generation_router.generate_analysis?; **签名**: ?async def generate_analysis(...) -> GenerateAnalysisResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?GenerateAnalysisResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.generate_analysis, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/ai_generation.py:55::build_ai_generation_router.generate_knowledge?; **签名**: ?async def generate_knowledge(...) -> GenerateKnowledgeResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?GenerateKnowledgeResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.generate_knowledge, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/ai_generation.py:73::build_ai_generation_router.clear_knowledge_cache?; **签名**: ?async def clear_knowledge_cache(...) -> dict[str, int]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict[str, int]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.delete, service.clear_knowledge_cache; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/ai_generation.py:82::build_ai_generation_router.delete_knowledge_cache_entry?; **签名**: ?async def delete_knowledge_cache_entry(...) -> dict[str, bool]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict[str, bool]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.delete, service.delete_knowledge_cache_entry, HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/analysis_batch.py
- **标识**: ?routers/analysis_batch.py:12::build_analysis_batch_router?; **签名**: ?def build_analysis_batch_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.post, service.generate_batch, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/analysis_batch.py:24::build_analysis_batch_router.batch_generate_analysis?; **签名**: ?async def batch_generate_analysis(...) -> BatchGenerateAnalysisResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?BatchGenerateAnalysisResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.generate_batch, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/annotations.py
- **标识**: ?routers/annotations.py:38::build_annotation_router?; **签名**: ?def build_annotation_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, router.post, router.put, router.delete, AnnotationService, service.list, body.model_dump, service.create, service.update, service.delete, body.model_dump().i; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/annotations.py:45::build_annotation_router.list_annotations?; **签名**: ?async def list_annotations(...) -> list[dict[str, Any]]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?list[dict[str, Any]]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.list; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/annotations.py:49::build_annotation_router.create_annotation?; **签名**: ?async def create_annotation(...) -> dict[str, Any]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, body.model_dump, service.create; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/annotations.py:55::build_annotation_router.update_annotation?; **签名**: ?async def update_annotation(...) -> dict[str, Any]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.put, service.update, body.model_dump().items, HTTPException, body.model_dump, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/annotations.py:63::build_annotation_router.delete_annotation?; **签名**: ?async def delete_annotation(...) -> dict[str, str]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict[str, str]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.delete, service.delete, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/assets_manager.py
- **标识**: ?routers/assets_manager.py:17::build_assets_manager_router?; **签名**: ?def build_assets_manager_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, router.post, router.delete, AssetsManagerService, Query, service.get_asset_list, service.get_cleanup_preview, service.analyze_storage, service.get_import_cac; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/assets_manager.py:30::build_assets_manager_router.list_assets?; **签名**: ?def list_assets(...) -> AssetListResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?AssetListResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, Query, service.get_asset_list, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/assets_manager.py:68::build_assets_manager_router.cleanup_preview?; **签名**: ?async def cleanup_preview(...) -> CleanupPreviewResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?CleanupPreviewResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.get_cleanup_preview, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/assets_manager.py
- **标识**: ?routers/assets_manager.py:78::build_assets_manager_router.storage_analysis?; **签名**: ?async def storage_analysis(...) -> StorageAnalysisResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?StorageAnalysisResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, Query, service.analyze_storage, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/assets_manager.py:91::build_assets_manager_router.cache_cleanup_preview?; **签名**: ?async def cache_cleanup_preview(...) -> CacheCleanupPreviewResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?CacheCleanupPreviewResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, Query, service.get_import_cache_cleanup_preview, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/assets_manager.py:101::build_assets_manager_router.cleanup_import_cache?; **签名**: ?async def cleanup_import_cache(...) -> CleanupResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?CleanupResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.cleanup_import_cache, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/assets_manager.py:115::build_assets_manager_router.unused_cache_preview?; **签名**: ?async def unused_cache_preview(...) -> CacheCleanupPreviewResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?CacheCleanupPreviewResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, Query, service.get_unused_cache_cleanup_preview, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/assets_manager.py:129::build_assets_manager_router.cleanup_unused_cache?; **签名**: ?async def cleanup_unused_cache(...) -> CleanupResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?CleanupResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.cleanup_unused_cache, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/assets_manager.py:143::build_assets_manager_router.cleanup_unreferenced?; **签名**: ?async def cleanup_unreferenced(...) -> CleanupResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?CleanupResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.cleanup_unreferenced, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/assets_manager.py:159::build_assets_manager_router.delete_asset?; **签名**: ?async def delete_asset(...) -> DeleteAssetResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?DeleteAssetResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.delete, service.delete_single, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/change_audit.py
- **标识**: ?routers/change_audit.py:17::build_change_audit_router?; **签名**: ?def build_change_audit_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, router.post, service.list_batches, service.get_batch, service.rollback_batch, result.get, HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/change_audit.py:21::build_change_audit_router.list_batches?; **签名**: ?async def list_batches(...) -> dict[str, Any]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.list_batches; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/change_audit.py:29::build_change_audit_router.get_batch?; **签名**: ?async def get_batch(...) -> dict[str, Any]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.get_batch, result.get, HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/change_audit.py
- **标识**: ?routers/change_audit.py:37::build_change_audit_router.rollback_batch?; **签名**: ?async def rollback_batch(...) -> dict[str, Any]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.rollback_batch, result.get, HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/collections.py
- **标识**: ?routers/collections.py:16::build_collections_router?; **签名**: ?def build_collections_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, router.post, CollectionsService, service.get_tree, service.create, service.batch_move, service.remove_from_collection, service.get_for_question, HTTPExceptio; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/collections.py:29::build_collections_router.get_tree?; **签名**: ?async def get_tree(...) -> list[CollectionNode]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?list[CollectionNode]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.get_tree, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/collections.py:40::build_collections_router.create_collection?; **签名**: ?async def create_collection(...) -> CreateCollectionResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?CreateCollectionResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.create, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/collections.py:55::build_collections_router.batch_move?; **签名**: ?async def batch_move(...) -> BatchMoveResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?BatchMoveResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.batch_move, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/collections.py:67::build_collections_router.remove_questions?; **签名**: ?async def remove_questions(...) -> dict[str, int]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict[str, int]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.remove_from_collection, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/collections.py:83::build_collections_router.get_question_collections?; **签名**: ?async def get_question_collections(...) -> list[CollectionNode]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?list[CollectionNode]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.get_for_question, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/embedding_builds.py
- **标识**: ?routers/embedding_builds.py:9::build_embedding_builds_router?; **签名**: ?def build_embedding_builds_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.post, service.build, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/embedding_builds.py:12::build_embedding_builds_router.build_question_embeddings?; **签名**: ?def build_question_embeddings(...) -> EmbeddingBuildResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?EmbeddingBuildResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.build, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/embedding_status.py
- **标识**: ?routers/embedding_status.py:9::build_embedding_status_router?; **签名**: ?def build_embedding_status_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, service.list_question_embeddings; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/embedding_status.py
- **标识**: ?routers/embedding_status.py:13::build_embedding_status_router.list_embedding_status?; **签名**: ?def list_embedding_status(...) -> list[EmbeddingStatusItem]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?list[EmbeddingStatusItem]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.list_question_embeddings; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/export_package.py
- **标识**: ?routers/export_package.py:15::_resolve_export_paths?; **签名**: ?def _resolve_export_paths(...) -> tuple[Path, Path]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?tuple[Path, Path]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream Path, default_db_path, default_assets_dir; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/export_package.py:22::build_export_package_router?; **签名**: ?def build_export_package_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP, file/upload; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, Query, _resolve_export_paths, FileResponse, ExportPackageService, svc.build_export, HTTPException, temp_dir.exists, str, _cleanup, shutil.rmtree; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/export_package.py:33::build_export_package_router.export_package?; **签名**: ?async def export_package(...) -> FileResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?FileResponse?; **副作用**: HTTP, file/upload; **上游 / 下游**: upstream module callers; downstream router.get, Query, _resolve_export_paths, FileResponse, ExportPackageService, svc.build_export, HTTPException, temp_dir.exists, str, _cleanup, shutil.rmtree; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/export_package.py:64::build_export_package_router.export_package._cleanup?; **签名**: ?async def _cleanup(...) -> unannotated?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?unannotated?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream temp_dir.exists, shutil.rmtree; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/favorites.py
- **标识**: ?routers/favorites.py:17::build_favorites_router?; **签名**: ?def build_favorites_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, router.post, router.put, router.delete, FavoritesService, service.update_group, service.delete_group, service.get_item, service.list_favorites, sorted, servi; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/favorites.py:28::build_favorites_router.list_groups?; **签名**: ?async def list_groups(...) -> list[FavoriteGroupItem]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?list[FavoriteGroupItem]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.list_groups, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/favorites.py:35::build_favorites_router.create_group?; **签名**: ?async def create_group(...) -> FavoriteGroupItem?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?FavoriteGroupItem?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.create_group, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/favorites.py:42::build_favorites_router.update_group?; **签名**: ?async def update_group(...) -> dict[str, bool]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict[str, bool]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.put, service.update_group, HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/favorites.py:49::build_favorites_router.delete_group?; **签名**: ?async def delete_group(...) -> dict[str, bool]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict[str, bool]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.delete, service.delete_group, HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/favorites.py
- **标识**: ?routers/favorites.py:58::build_favorites_router.assign?; **签名**: ?async def assign(...) -> BatchFavoriteResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?BatchFavoriteResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.assign, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/favorites.py:65::build_favorites_router.batch_star?; **签名**: ?async def batch_star(...) -> BatchFavoriteResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?BatchFavoriteResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.batch_star, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/favorites.py:72::build_favorites_router.remove?; **签名**: ?async def remove(...) -> dict[str, int]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict[str, int]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.remove, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/favorites.py:82::build_favorites_router.get_item?; **签名**: ?async def get_item(...) -> FavoriteItemView | None?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?FavoriteItemView | None?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.get_item; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/favorites.py:86::build_favorites_router.list_items?; **签名**: ?async def list_items(...) -> list[FavoriteItemView]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?list[FavoriteItemView]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.list_favorites; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/favorites.py:98::build_favorites_router.get_ids?; **签名**: ?async def get_ids(...) -> list[str]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?list[str]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, sorted, service.get_favorite_ids; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/home.py
- **标识**: ?routers/home.py:7::build_home_router?; **签名**: ?def build_home_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/home.py:11::build_home_router.home?; **签名**: ?def home(...) -> str?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?str?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/image_catalog.py
- **标识**: ?routers/image_catalog.py:9::build_image_catalog_router?; **签名**: ?def build_image_catalog_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, Query, service.list; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/image_catalog.py:15::build_image_catalog_router.list_images?; **签名**: ?def list_images(...) -> list[ImageAssetItem]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?list[ImageAssetItem]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, Query, service.list; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/image_management.py
- **标识**: ?routers/image_management.py:20::build_image_management_router?; **签名**: ?def build_image_management_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, router.post, router.put, router.patch, router.delete, ImageManagementService, service.list_cache_images, File, service.update_image, service.delete_image, se; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/image_management.py:29::build_image_management_router.list_cache_images?; **签名**: ?async def list_cache_images(...) -> list[CachedImageAsset]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?list[CachedImageAsset]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.list_cache_images; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/image_management.py:33::build_image_management_router.upload_cache_images?; **签名**: ?async def upload_cache_images(...) -> CacheUploadResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?CacheUploadResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, File, service.upload_cache_files, HTTPException, file.close, file.read, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/image_management.py:44::build_image_management_router.list_images?; **签名**: ?async def list_images(...) -> ImageListResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ImageListResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.list_images, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/image_management.py:51::build_image_management_router.add_image?; **签名**: ?async def add_image(...) -> QuestionImageDetail?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?QuestionImageDetail?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.add_image, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/image_management.py:58::build_image_management_router.add_cached_image?; **签名**: ?async def add_cached_image(...) -> QuestionImageDetail?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?QuestionImageDetail?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.add_cached_image, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/image_management.py:65::build_image_management_router.replace_image?; **签名**: ?async def replace_image(...) -> QuestionImageDetail?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?QuestionImageDetail?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.put, service.replace_image, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/image_management.py:72::build_image_management_router.update_image?; **签名**: ?async def update_image(...) -> dict[str, bool]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict[str, bool]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.patch, service.update_image; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/image_management.py:77::build_image_management_router.delete_image?; **签名**: ?async def delete_image(...) -> dict[str, bool]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict[str, bool]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.delete, service.delete_image, HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/image_management.py:84::build_image_management_router.reorder_images?; **签名**: ?async def reorder_images(...) -> dict[str, bool]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict[str, bool]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.reorder_images; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/image_management.py
- **标识**: ?routers/image_management.py:89::build_image_management_router.validate_images?; **签名**: ?async def validate_images(...) -> ValidationResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ValidationResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.validate; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/image_management.py:93::build_image_management_router.available_images?; **签名**: ?async def available_images(...) -> list[dict]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?list[dict]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.available_images; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/import_pipeline.py
- **标识**: ?routers/import_pipeline.py:46::_safe_upload_name?; **签名**: ?def _safe_upload_name(...) -> str?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?str?; **副作用**: HTTP, file/upload; **上游 / 下游**: upstream module callers; downstream Path(source).suffix.lower, re.sub('[^\\w\\-.()\\u4e00-\\u9fff]+', '_', stem, flags=re.UNICODE).strip, Path, Path(source).stem.strip, re.sub, uuid4; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:54::build_import_pipeline_router?; **签名**: ?def build_import_pipeline_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP, file/upload; **上游 / 下游**: upstream module callers; downstream APIRouter, router.post, router.get, router.delete, ImportTaskDispatcher, File, service.create_batch_from_upload, ImportBatchResponse.model_validate, service.run_batch_pandoc, Pando; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:63::build_import_pipeline_router.create_import_batch?; **签名**: ?async def create_import_batch(...) -> ImportBatchResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ImportBatchResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, File, service.create_batch_from_upload, ImportBatchResponse.model_validate, file.read; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:69::build_import_pipeline_router.run_batch_pandoc?; **签名**: ?def run_batch_pandoc(...) -> PandocBatchResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?PandocBatchResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.run_batch_pandoc, PandocBatchResponse.model_validate, HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:76::build_import_pipeline_router.run_batch_ai_clean?; **签名**: ?def run_batch_ai_clean(...) -> AiCleanBatchResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?AiCleanBatchResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.run_batch_ai_clean, AiCleanBatchResponse.model_validate, HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:83::build_import_pipeline_router.structure_batch_questions?; **签名**: ?def structure_batch_questions(...) -> AiStructureBatchResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?AiStructureBatchResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.structure_batch_questions, AiStructureBatchResponse.model_validate, HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:90::build_import_pipeline_router.recognize_batch?; **签名**: ?async def recognize_batch(...) -> RecognizeBatchResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?RecognizeBatchResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, RecognizeBatchResponse.model_validate, service.recognize_batch, result.get, HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:102::build_import_pipeline_router.queue_batch_pandoc?; **签名**: ?def queue_batch_pandoc(...) -> ImportPipelineTaskResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ImportPipelineTaskResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, task_dispatcher.submit_batch_stage, ImportPipelineTaskResponse.model_validate, import_task_to_response; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/import_pipeline.py
- **标识**: ?routers/import_pipeline.py:107::build_import_pipeline_router.queue_batch_ai_clean?; **签名**: ?def queue_batch_ai_clean(...) -> ImportPipelineTaskResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ImportPipelineTaskResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, task_dispatcher.submit_batch_stage, ImportPipelineTaskResponse.model_validate, import_task_to_response; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:112::build_import_pipeline_router.queue_batch_ai_structure?; **签名**: ?def queue_batch_ai_structure(...) -> ImportPipelineTaskResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ImportPipelineTaskResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, task_dispatcher.submit_batch_stage, ImportPipelineTaskResponse.model_validate, import_task_to_response; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:117::build_import_pipeline_router.queue_batch_recognition?; **签名**: ?def queue_batch_recognition(...) -> ImportPipelineTaskResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ImportPipelineTaskResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, task_dispatcher.submit_batch_stage, ImportPipelineTaskResponse.model_validate, import_task_to_response; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:122::build_import_pipeline_router.get_task_queue_status?; **签名**: ?def get_task_queue_status(...) -> dict[str, str | bool]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict[str, str | bool]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, task_dispatcher.status; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:126::build_import_pipeline_router.extract_batch_images?; **签名**: ?def extract_batch_images(...) -> ExtractBatchImagesResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ExtractBatchImagesResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.extract_batch_images, ExtractBatchImagesResponse.model_validate, result.get, HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:134::build_import_pipeline_router.refine_batch_questions?; **签名**: ?def refine_batch_questions(...) -> AiRefineBatchResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?AiRefineBatchResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.refine_batch_questions, AiRefineBatchResponse.model_validate; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:140::build_import_pipeline_router.complete_draft_metadata?; **签名**: ?async def complete_draft_metadata(...) -> DraftMetadataResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?DraftMetadataResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, DraftMetadataResponse.model_validate, service.complete_draft_metadata; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:151::build_import_pipeline_router.get_batch_status?; **签名**: ?def get_batch_status(...) -> ImportBatchStatusResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ImportBatchStatusResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, ImportBatchStatusResponse.model_validate, service.get_batch_status; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:155::build_import_pipeline_router.list_import_batches?; **签名**: ?def list_import_batches(...) -> dict?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.list_batch_overview; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:160::build_import_pipeline_router.retry_import_batch?; **签名**: ?def retry_import_batch(...) -> dict?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.retry_batch; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/import_pipeline.py
- **标识**: ?routers/import_pipeline.py:165::build_import_pipeline_router.confirm_batch_questions?; **签名**: ?def confirm_batch_questions(...) -> ConfirmBatchQuestionsResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ConfirmBatchQuestionsResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.confirm_batch_questions, ConfirmBatchQuestionsResponse, len, asset.model_dump; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:181::build_import_pipeline_router.submit_ai_generated_review?; **签名**: ?def submit_ai_generated_review(...) -> AiGeneratedReviewResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?AiGeneratedReviewResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.create_ai_generated_review_task, AiGeneratedReviewResponse, str, int, result.get; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:199::build_import_pipeline_router.list_review_tasks?; **签名**: ?def list_review_tasks(...) -> ReviewTaskListResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ReviewTaskListResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.list_review_tasks, ReviewTaskListResponse, str, items.append, ReviewTaskListItem, result.get, summary.get, int, task.created_at.isoformat, task.updated_at.isofo; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:231::build_import_pipeline_router.delete_review_task?; **签名**: ?def delete_review_task(...) -> DeleteReviewTaskResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?DeleteReviewTaskResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.delete, service.delete_review_task, DeleteReviewTaskResponse, draft_repository.delete; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:239::build_import_pipeline_router.upload_batch_image?; **签名**: ?async def upload_batch_image(...) -> ImportMediaAsset?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ImportMediaAsset?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, File, service.add_batch_image, ImportMediaAsset.model_validate, file.read, HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:248::build_import_pipeline_router.list_batch_images?; **签名**: ?def list_batch_images(...) -> list[ImportMediaAsset]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?list[ImportMediaAsset]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, ImportMediaAsset.model_validate, service.list_batch_images; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:253::build_import_pipeline_router.upload_import_file?; **签名**: ?async def upload_import_file(...) -> UploadImportFileResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?UploadImportFileResponse?; **副作用**: HTTP, file/upload; **上游 / 下游**: upstream module callers; downstream router.post, File, upload_dir.mkdir, _safe_upload_name, target.write_bytes, UploadImportFileResponse, default_import_batches_dir, file.read, str, len; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:270::build_import_pipeline_router.convert_document?; **签名**: ?def convert_document(...) -> ConvertDocumentResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ConvertDocumentResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.convert_document, ConvertDocumentResponse, ImportPipelineTaskResponse.model_validate, import_task_to_response; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:275::build_import_pipeline_router.clean_document?; **签名**: ?def clean_document(...) -> CleanDocumentResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?CleanDocumentResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.clean_document, CleanDocumentResponse, ImportPipelineTaskResponse.model_validate, import_task_to_response; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:280::build_import_pipeline_router.parse_structured_questions?; **签名**: ?def parse_structured_questions(...) -> ParseStructuredQuestionsResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ParseStructuredQuestionsResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.parse_structured_questions, ParseStructuredQuestionsResponse, ImportPipelineTaskResponse.model_validate, import_task_to_response; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/import_pipeline.py
- **标识**: ?routers/import_pipeline.py:285::build_import_pipeline_router.ai_parse_document?; **签名**: ?async def ai_parse_document(...) -> AiParseDocumentResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?AiParseDocumentResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, AiParseDocumentResponse, service.ai_parse_document, ImportPipelineTaskResponse.model_validate, import_task_to_response; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/import_pipeline.py:290::build_import_pipeline_router.get_import_task?; **签名**: ?def get_import_task(...) -> ImportPipelineTaskResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ImportPipelineTaskResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.get_task, ImportPipelineTaskResponse.model_validate, import_task_to_response; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/knowledge_points.py
- **标识**: ?routers/knowledge_points.py:9::build_knowledge_points_router?; **签名**: ?def build_knowledge_points_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, router.put, router.delete, Query, service.list, service.question_counts_by_topic3, service.list_for_question, HTTPException, service.replace_for_question, se; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/knowledge_points.py:15::build_knowledge_points_router.list_knowledge_points?; **签名**: ?def list_knowledge_points(...) -> list[KnowledgePointItem]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?list[KnowledgePointItem]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, Query, service.list; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/knowledge_points.py:37::build_knowledge_points_router.knowledge_point_counts?; **签名**: ?def knowledge_point_counts(...) -> dict[str, int]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict[str, int]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.question_counts_by_topic3; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/knowledge_points.py:41::build_knowledge_points_router.list_question_knowledge_points?; **签名**: ?def list_question_knowledge_points(...) -> list[QuestionKnowledgePointLink]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?list[QuestionKnowledgePointLink]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.list_for_question, HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/knowledge_points.py:48::build_knowledge_points_router.replace_question_knowledge_points?; **签名**: ?def replace_question_knowledge_points(...) -> list[QuestionKnowledgePointLink]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?list[QuestionKnowledgePointLink]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.put, service.replace_for_question, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/knowledge_points.py:60::build_knowledge_points_router.upsert_question_knowledge_point?; **签名**: ?def upsert_question_knowledge_point(...) -> QuestionKnowledgePointLink?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?QuestionKnowledgePointLink?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.put, service.upsert_for_question, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/knowledge_points.py:72::build_knowledge_points_router.delete_question_knowledge_point?; **签名**: ?def delete_question_knowledge_point(...) -> dict[str, str]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict[str, str]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.delete, service.delete_for_question, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/lesson_documents.py
- **标识**: ?routers/lesson_documents.py:22::build_lesson_documents_router?; **签名**: ?def build_lesson_documents_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, router.post, router.patch, Query, get_saved_handout, save_saved_handout, rename_saved_handout, list_saved_handout_versions, list_saved_handouts, HTTPExceptio; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/lesson_documents.py
- **标识**: ?routers/lesson_documents.py:26::build_lesson_documents_router.list_handouts?; **签名**: ?def list_handouts(...) -> dict?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, Query, list_saved_handouts; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/lesson_documents.py:30::build_lesson_documents_router.get_handout?; **签名**: ?def get_handout(...) -> dict?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, get_saved_handout, HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/lesson_documents.py:37::build_lesson_documents_router.save_handout?; **签名**: ?def save_handout(...) -> dict?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, save_saved_handout; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/lesson_documents.py:41::build_lesson_documents_router.rename_handout?; **签名**: ?def rename_handout(...) -> dict?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.patch, rename_saved_handout, HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/lesson_documents.py:48::build_lesson_documents_router.list_handout_versions?; **签名**: ?def list_handout_versions(...) -> dict?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, list_saved_handout_versions, HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/lesson_documents.py:55::build_lesson_documents_router.restore_handout_version?; **签名**: ?def restore_handout_version(...) -> dict?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, restore_saved_handout_version, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/lesson_exports.py
- **标识**: ?routers/lesson_exports.py:11::build_lesson_exports_router?; **签名**: ?def build_lesson_exports_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.post, LessonExportDispatcher, ImportPipelineTaskResponse.model_validate, submit, task_dispatcher.submit, serialize_export_task, HTTPException, str(exc).lower, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/lesson_exports.py:18::build_lesson_exports_router.submit?; **签名**: ?def submit(...) -> ImportPipelineTaskResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ImportPipelineTaskResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream ImportPipelineTaskResponse.model_validate, task_dispatcher.submit, serialize_export_task, HTTPException, str(exc).lower, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/lesson_exports.py:27::build_lesson_exports_router.export_word?; **签名**: ?def export_word(...) -> ImportPipelineTaskResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ImportPipelineTaskResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, submit; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/lesson_exports.py:31::build_lesson_exports_router.export_pptx?; **签名**: ?def export_pptx(...) -> ImportPipelineTaskResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ImportPipelineTaskResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, submit; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/lesson_reflections.py
- **标识**: ?routers/lesson_reflections.py:18::build_lesson_reflections_router?; **签名**: ?def build_lesson_reflections_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, router.post, Query, get_lesson_reflection, list_lesson_reflections, HTTPException, save_lesson_reflection, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/lesson_reflections.py:26::build_lesson_reflections_router.list_reflections?; **签名**: ?def list_reflections(...) -> dict?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, Query, list_lesson_reflections; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/lesson_reflections.py:33::build_lesson_reflections_router.get_reflection?; **签名**: ?def get_reflection(...) -> dict?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, get_lesson_reflection, HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/lesson_reflections.py:40::build_lesson_reflections_router.save_reflection?; **签名**: ?def save_reflection(...) -> dict?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, save_lesson_reflection, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/lesson_reflections.py:56::build_mcp_lesson_reflections_router?; **签名**: ?def build_mcp_lesson_reflections_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream build_lesson_reflections_router; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/mcp.py
- **标识**: ?routers/mcp.py:73::_http_error?; **签名**: ?def _http_error(...) -> HTTPException?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?HTTPException?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/mcp.py:87::_provider_error?; **签名**: ?def _provider_error(...) -> HTTPException?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?HTTPException?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/mcp.py:100::_config_error?; **签名**: ?def _config_error(...) -> HTTPException?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?HTTPException?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/mcp.py:107::_public_provider_config?; **签名**: ?def _public_provider_config(...) -> dict[str, Any]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream none; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/mcp.py:119::build_mcp_router?; **签名**: ?def build_mcp_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, router.post, service.status, get_runtime_config, McpStatusResponse.model_validate, McpRuntimeConfigResponse, RuntimeAiConfig.from_dict, update_runtime_config; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/mcp.py
- **标识**: ?routers/mcp.py:123::build_mcp_router.get_status?; **签名**: ?async def get_status(...) -> McpStatusResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?McpStatusResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.status, get_runtime_config, McpStatusResponse.model_validate, status.get; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/mcp.py:135::build_mcp_router.get_config?; **签名**: ?async def get_config(...) -> McpRuntimeConfigResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?McpRuntimeConfigResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, get_runtime_config, McpRuntimeConfigResponse, _public_provider_config, runtime.is_configured; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/mcp.py:146::build_mcp_router.update_config?; **签名**: ?async def update_config(...) -> McpConfigResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?McpConfigResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, RuntimeAiConfig.from_dict, update_runtime_config, service.notify_config_updated, McpConfigResponse, payload.model_dump, runtime.is_configured; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/mcp.py:159::build_mcp_router.test_connection?; **签名**: ?async def test_connection(...) -> McpTestConnectionResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?McpTestConnectionResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, McpTestConnectionResponse, service.test_connection, HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/mcp.py:166::build_mcp_router.chat_test?; **签名**: ?async def chat_test(...) -> ChatTestResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ChatTestResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, get_runtime_config, ChatTestResponse, runtime.llm.api_key.strip, _config_error, runtime.llm.base_url.strip, runtime.llm.model_name.strip, OpenAI, client.chat.completio; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/mcp.py:217::build_mcp_router.refine_question_format?; **签名**: ?async def refine_question_format(...) -> McpTaskResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?McpTaskResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, McpTaskResponse, service.refine_question_format, _http_error, _provider_error; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/mcp.py:227::build_mcp_router.parse_document?; **签名**: ?async def parse_document(...) -> McpTaskResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?McpTaskResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, McpTaskResponse, service.parse_document, _http_error, _provider_error, request.model_dump; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/mcp.py:237::build_mcp_router.detect_question_regions?; **签名**: ?async def detect_question_regions(...) -> McpTaskResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?McpTaskResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, McpTaskResponse, service.detect_question_regions, _http_error, _provider_error, request.model_dump; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/mcp.py:247::build_mcp_router.parse_question_region?; **签名**: ?async def parse_question_region(...) -> McpTaskResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?McpTaskResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, McpTaskResponse, service.parse_question_region, _http_error, _provider_error, request.model_dump; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/mcp.py:257::build_mcp_router.generate_analysis?; **签名**: ?async def generate_analysis(...) -> McpTaskResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?McpTaskResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, McpTaskResponse, service.generate_analysis, _http_error, _provider_error, request.question.model_dump; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/mcp.py
- **标识**: ?routers/mcp.py:274::build_mcp_router.generate_knowledge?; **签名**: ?async def generate_knowledge(...) -> McpTaskResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?McpTaskResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, McpTaskResponse, service.generate_knowledge, _http_error, _provider_error, request.model_dump; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/mcp.py:284::build_mcp_router.generate_metadata?; **签名**: ?async def generate_metadata(...) -> McpTaskResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?McpTaskResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, McpTaskResponse, service.generate_metadata, _http_error, _provider_error, request.model_dump; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/metadata_batch.py
- **标识**: ?routers/metadata_batch.py:11::build_metadata_batch_router?; **签名**: ?def build_metadata_batch_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.post, service.process, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/metadata_batch.py:24::build_metadata_batch_router.batch_metadata?; **签名**: ?async def batch_metadata(...) -> BatchMetadataResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?BatchMetadataResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.process, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/mistake.py
- **标识**: ?routers/mistake.py:28::build_mistake_router?; **签名**: ?def build_mistake_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.post, router.get, service.mark, MistakeResult, service.unmark, service.batch_mark, BatchMistakeResponse, service.batch_unmark, service.list_mistakes, HTTPExceptio; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/mistake.py:32::build_mistake_router.mark_mistake?; **签名**: ?async def mark_mistake(...) -> MistakeResult?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?MistakeResult?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.mark, MistakeResult, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/mistake.py:40::build_mistake_router.unmark_mistake?; **签名**: ?async def unmark_mistake(...) -> MistakeResult?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?MistakeResult?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.unmark, MistakeResult, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/mistake.py:48::build_mistake_router.batch_mark?; **签名**: ?async def batch_mark(...) -> BatchMistakeResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?BatchMistakeResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.batch_mark, BatchMistakeResponse, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/mistake.py:56::build_mistake_router.batch_unmark?; **签名**: ?async def batch_unmark(...) -> BatchMistakeResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?BatchMistakeResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.batch_unmark, BatchMistakeResponse, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/mistake.py:64::build_mistake_router.list_mistakes?; **签名**: ?async def list_mistakes(...) -> list[str]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?list[str]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.list_mistakes, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/mistake.py
- **标识**: ?routers/mistake.py:71::build_mistake_router.count_mistakes?; **签名**: ?async def count_mistakes(...) -> dict[str, int]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict[str, int]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.count_mistakes, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/paper_drafts.py
- **标识**: ?routers/paper_drafts.py:13::build_paper_drafts_router?; **签名**: ?def build_paper_drafts_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, router.post, router.delete, PaperDraftService, Query, service.list, service.get_latest, service.get, service.save, HTTPException, service.delete, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/paper_drafts.py:20::build_paper_drafts_router.list_drafts?; **签名**: ?async def list_drafts(...) -> PaperDraftListResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?PaperDraftListResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, Query, service.list, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/paper_drafts.py:29::build_paper_drafts_router.get_latest_draft?; **签名**: ?async def get_latest_draft(...) -> PaperDraftResponse | None?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?PaperDraftResponse | None?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.get_latest, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/paper_drafts.py:36::build_paper_drafts_router.get_draft?; **签名**: ?async def get_draft(...) -> PaperDraftResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?PaperDraftResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.get, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/paper_drafts.py:45::build_paper_drafts_router.save_draft?; **签名**: ?async def save_draft(...) -> PaperDraftResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?PaperDraftResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.save, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/paper_drafts.py:56::build_paper_drafts_router.delete_draft?; **签名**: ?async def delete_draft(...) -> dict[str, bool]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict[str, bool]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.delete, service.delete, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/papers.py
- **标识**: ?routers/papers.py:9::build_papers_router?; **签名**: ?def build_papers_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, Query, service.list, service.get, service.list_questions, HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/papers.py:13::build_papers_router.list_papers?; **签名**: ?def list_papers(...) -> list[PaperSummary]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?list[PaperSummary]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, Query, service.list; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/papers.py:25::build_papers_router.get_paper?; **签名**: ?def get_paper(...) -> PaperDetail?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?PaperDetail?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.get, HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/papers.py
- **标识**: ?routers/papers.py:32::build_papers_router.list_paper_questions?; **签名**: ?def list_paper_questions(...) -> list[PaperQuestionSummary]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?list[PaperQuestionSummary]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.list_questions; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/processing_runs.py
- **标识**: ?routers/processing_runs.py:9::build_processing_runs_router?; **签名**: ?def build_processing_runs_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, Query, service.list; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/processing_runs.py:15::build_processing_runs_router.list_processing_runs?; **签名**: ?def list_processing_runs(...) -> list[ProcessingRunItem]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?list[ProcessingRunItem]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, Query, service.list; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/question_details.py
- **标识**: ?routers/question_details.py:9::build_question_details_router?; **签名**: ?def build_question_details_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, service.get, service.list_assets, HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/question_details.py:13::build_question_details_router.get_question?; **签名**: ?def get_question(...) -> QuestionDetail?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?QuestionDetail?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.get, HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/question_details.py:20::build_question_details_router.list_question_assets?; **签名**: ?def list_question_assets(...) -> list[QuestionAsset]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?list[QuestionAsset]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.list_assets; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/question_imports.py
- **标识**: ?routers/question_imports.py:9::build_question_imports_router?; **签名**: ?def build_question_imports_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.post, service.import_question, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/question_imports.py:13::build_question_imports_router.import_question?; **签名**: ?def import_question(...) -> ImportQuestionResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ImportQuestionResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.import_question, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/question_reviews.py
- **标识**: ?routers/question_reviews.py:22::build_question_reviews_router?; **签名**: ?def build_question_reviews_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.post, router.get, service.history, service.pending_fixes, Query, service.rejected_questions, service.review, service.propose_fix, service.report_issue, service.fi; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/question_reviews.py:26::build_question_reviews_router.review_question?; **签名**: ?def review_question(...) -> ReviewActionResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ReviewActionResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.review, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/question_reviews.py
- **标识**: ?routers/question_reviews.py:33::build_question_reviews_router.propose_fix?; **签名**: ?def propose_fix(...) -> ProposeFixResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ProposeFixResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.propose_fix, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/question_reviews.py:40::build_question_reviews_router.report_issue?; **签名**: ?def report_issue(...) -> ReportIssueResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ReportIssueResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.report_issue, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/question_reviews.py:47::build_question_reviews_router.review_history?; **签名**: ?def review_history(...) -> list[ReviewQueueItem]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?list[ReviewQueueItem]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.history; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/question_reviews.py:51::build_question_reviews_router.list_pending_fixes?; **签名**: ?def list_pending_fixes(...) -> list[PendingFixItem]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?list[PendingFixItem]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.pending_fixes; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/question_reviews.py:55::build_question_reviews_router.list_rejected_questions?; **签名**: ?def list_rejected_questions(...) -> list[RejectedQuestionItem]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?list[RejectedQuestionItem]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, Query, service.rejected_questions; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/question_reviews.py:62::build_question_reviews_router.get_fix_detail?; **签名**: ?def get_fix_detail(...) -> FixDetailResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?FixDetailResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.fix_detail, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/question_reviews.py:69::build_question_reviews_router.decide_fix?; **签名**: ?def decide_fix(...) -> DecideFixResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?DecideFixResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.decide, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/question_search.py
- **标识**: ?routers/question_search.py:22::_database_unavailable?; **签名**: ?def _database_unavailable(...) -> HTTPException?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?HTTPException?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/question_search.py:29::build_question_search_router?; **签名**: ?def build_question_search_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, router.post, QuestionSearchService, QuestionWriteService, Query, QuestionSearchParams, service.search, service.get_facets, service.get_by_ids, write_service.; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/question_search.py:60::build_question_search_router.search_questions?; **签名**: ?def search_questions(...) -> SearchResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?SearchResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, Query, QuestionSearchParams, service.search, HTTPException, _database_unavailable, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/question_search.py
- **标识**: ?routers/question_search.py:132::build_question_search_router.get_facets?; **签名**: ?def get_facets(...) -> FilterFacetsResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?FilterFacetsResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.get_facets, _database_unavailable, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/question_search.py:148::build_question_search_router.batch_get_questions?; **签名**: ?def batch_get_questions(...) -> BatchQuestionFetchResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?BatchQuestionFetchResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.get_by_ids, _database_unavailable, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/question_search.py:166::build_question_search_router.batch_delete_questions?; **签名**: ?def batch_delete_questions(...) -> BatchQuestionDeleteResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?BatchQuestionDeleteResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, write_service.delete_batch, result.get, BatchQuestionDeleteResponse, HTTPException, len, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/question_search.py:191::build_question_search_router.return_question_to_review?; **签名**: ?def return_question_to_review(...) -> ReturnQuestionToReviewResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ReturnQuestionToReviewResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, write_service.return_to_review, result.get, ReturnQuestionToReviewResponse, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/question_stats.py
- **标识**: ?routers/question_stats.py:8::build_question_stats_router?; **签名**: ?def build_question_stats_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, service.status_counts; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/question_stats.py:14::build_question_stats_router.question_stats?; **签名**: ?def question_stats(...) -> dict[str, int]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict[str, int]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.status_counts; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/question_updates.py
- **标识**: ?routers/question_updates.py:11::build_question_updates_router?; **签名**: ?def build_question_updates_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.put, service.update, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/question_updates.py:15::build_question_updates_router.update_question?; **签名**: ?def update_question(...) -> QuestionDetail?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?QuestionDetail?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.put, service.update, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/question_variants.py
- **标识**: ?routers/question_variants.py:18::build_question_variants_router?; **签名**: ?def build_question_variants_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.post, service.generate_variants, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/question_variants.py:31::build_question_variants_router.generate_question_variants?; **签名**: ?async def generate_question_variants(...) -> GenerateQuestionVariantsResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?GenerateQuestionVariantsResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.generate_variants, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/question_versions.py
- **标识**: ?routers/question_versions.py:14::build_question_versions_router?; **签名**: ?def build_question_versions_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, router.post, service.list, service.get, service.rollback, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/question_versions.py:18::build_question_versions_router.list_question_versions?; **签名**: ?def list_question_versions(...) -> list[QuestionVersionSummary]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?list[QuestionVersionSummary]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.list; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/question_versions.py:25::build_question_versions_router.get_question_version?; **签名**: ?def get_question_version(...) -> QuestionVersionDetail?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?QuestionVersionDetail?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, service.get, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/question_versions.py:35::build_question_versions_router.rollback_question_version?; **签名**: ?def rollback_question_version(...) -> VersionRollbackResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?VersionRollbackResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.rollback, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/restore_package.py
- **标识**: ?routers/restore_package.py:14::_resolve_restore_paths?; **签名**: ?def _resolve_restore_paths(...) -> tuple[Path, Path, Path]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?tuple[Path, Path, Path]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream default_backups_dir, Path, default_db_path, default_assets_dir; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/restore_package.py:25::build_restore_package_router?; **签名**: ?def build_restore_package_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.post, _resolve_restore_paths, RestorePackageService, svc.restore, RestorePackageResponse, HTTPException, file.filename.lower().endswith, file.read, str, file.file; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/restore_package.py:41::build_restore_package_router.restore_package?; **签名**: ?async def restore_package(...) -> RestorePackageResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?RestorePackageResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, _resolve_restore_paths, RestorePackageService, svc.restore, RestorePackageResponse, HTTPException, file.filename.lower().endswith, file.read, str, file.filename.lower; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/review_queue.py
- **标识**: ?routers/review_queue.py:10::build_review_queue_router?; **签名**: ?def build_review_queue_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, repository.list, len; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/review_queue.py:14::build_review_queue_router.list_review_queue?; **签名**: ?async def list_review_queue(...) -> list[dict[str, Any]]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?list[dict[str, Any]]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, repository.list; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/review_queue.py:22::build_review_queue_router.list_review_queue_api?; **签名**: ?async def list_review_queue_api(...) -> dict[str, Any]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, repository.list, len; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/review_save.py
- **标识**: ?routers/review_save.py:21::build_review_save_router?; **签名**: ?def build_review_save_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, router.put, router.delete, router.post, ReviewSaveService, SQLiteReviewDraftRepository, ReviewDraftResponse, HTTPException, draft_repository.get, ReviewDraft; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/review_save.py:32::build_review_save_router.draft_response?; **签名**: ?def draft_response(...) -> ReviewDraftResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ReviewDraftResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream ReviewDraftResponse, snapshot.updated_at.isoformat; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/review_save.py:40::build_review_save_router.conflict_response?; **签名**: ?def conflict_response(...) -> HTTPException?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?HTTPException?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream HTTPException, draft_response(exc.current).model_dump, draft_response; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/review_save.py:48::build_review_save_router.get_review_draft?; **签名**: ?async def get_review_draft(...) -> ReviewDraftLookupResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ReviewDraftLookupResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, draft_repository.get, ReviewDraftLookupResponse, draft_response; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/review_save.py:53::build_review_save_router.save_review_draft?; **签名**: ?async def save_review_draft(...) -> ReviewDraftResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ReviewDraftResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.put, draft_response, draft_repository.save, payload.state.model_dump, conflict_response; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/review_save.py:61::build_review_save_router.list_review_draft_versions?; **签名**: ?async def list_review_draft_versions(...) -> ReviewDraftVersionListResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ReviewDraftVersionListResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, ReviewDraftVersionListResponse, draft_response, draft_repository.list_versions; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/review_save.py:67::build_review_save_router.delete_review_draft?; **签名**: ?async def delete_review_draft(...) -> dict[str, bool | str]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict[str, bool | str]?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.delete, draft_repository.delete; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/review_save.py:72::build_review_save_router.restore_review_draft?; **签名**: ?async def restore_review_draft(...) -> ReviewDraftResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?ReviewDraftResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, draft_response, draft_repository.restore, HTTPException, conflict_response; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/review_save.py:91::build_review_save_router.save_reviewed?; **签名**: ?async def save_reviewed(...) -> SaveReviewedQuestionsResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?SaveReviewedQuestionsResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.save, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/review_save.py:110::build_review_save_router.save_reviewed_knowledge?; **签名**: ?async def save_reviewed_knowledge(...) -> SaveReviewedKnowledgeResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?SaveReviewedKnowledgeResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.save_knowledge, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/similar_questions.py
- **标识**: ?routers/similar_questions.py:9::build_similar_questions_router?; **签名**: ?def build_similar_questions_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, SimilarQuestionsService, Query, service.find_similar, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/similar_questions.py:26::build_similar_questions_router.find_similar?; **签名**: ?async def find_similar(...) -> SimilarQuestionsResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?SimilarQuestionsResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, Query, service.find_similar, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/system_status.py
- **标识**: ?routers/system_status.py:13::build_system_status_router?; **签名**: ?def build_system_status_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP, database read/write; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, default_db_path, str, db_path.exists, bool, getattr, connect_db, _count_table; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/system_status.py:17::build_system_status_router.db_status?; **签名**: ?def db_status(...) -> dict[str, Any]?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: HTTP, database read/write; **上游 / 下游**: upstream module callers; downstream router.get, default_db_path, str, db_path.exists, bool, getattr, connect_db, _count_table; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/system_status.py:43::_count_table?; **签名**: ?def _count_table(...) -> int?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?int?; **副作用**: HTTP, database read/write; **上游 / 下游**: upstream module callers; downstream conn.execute("SELECT 1 FROM sqlite_master WHERE type = 'table' AND name = ?", (table,)).fetchone, int, conn.execute, conn.execute(f'SELECT COUNT(*) FROM {table}').fetchone; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/tasks.py
- **标识**: ?routers/tasks.py:26::build_tasks_router?; **签名**: ?def build_tasks_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP, file/upload; **上游 / 下游**: upstream module callers; downstream APIRouter, TaskQueueHealthService, router.get, router.post, Query, service.list_tasks, TaskCenterListResponse, queue_health.check, TaskCenterItem.model_validate, service.retry_task; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/tasks.py:34::build_tasks_router.list_tasks?; **签名**: ?def list_tasks(...) -> TaskCenterListResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?TaskCenterListResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, Query, service.list_tasks, TaskCenterListResponse, raw.split, max, TaskCenterItem.model_validate, math.ceil; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/tasks.py:61::build_tasks_router.get_task_queue_health?; **签名**: ?def get_task_queue_health(...) -> dict?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, queue_health.check; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/tasks.py:65::build_tasks_router.get_task?; **签名**: ?def get_task(...) -> TaskCenterItem?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?TaskCenterItem?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, TaskCenterItem.model_validate, service.get_task; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/tasks.py:69::build_tasks_router.retry_task?; **签名**: ?def retry_task(...) -> TaskActionResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?TaskActionResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.retry_task, TaskActionResponse, TaskCenterItem.model_validate; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/tasks.py
- **标识**: ?routers/tasks.py:78::build_tasks_router.cancel_task?; **签名**: ?def cancel_task(...) -> TaskActionResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?TaskActionResponse?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, service.cancel_task, str, TaskActionResponse, TaskCenterItem.model_validate; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/tasks.py:85::build_tasks_router.download_task_result?; **签名**: ?def download_task_result(...) -> FileResponse?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?FileResponse?; **副作用**: HTTP, file/upload; **上游 / 下游**: upstream module callers; downstream router.get, service.resolve_result_file, FileResponse; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: routers/teaching_projects.py
- **标识**: ?routers/teaching_projects.py:22::build_teaching_projects_router?; **签名**: ?def build_teaching_projects_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream APIRouter, router.get, router.post, Query, get_teaching_project, duplicate_teaching_project, list_teaching_projects, HTTPException, save_teaching_project, archive_teaching_project,; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/teaching_projects.py:30::build_teaching_projects_router.list_projects?; **签名**: ?def list_projects(...) -> dict?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, Query, list_teaching_projects; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/teaching_projects.py:34::build_teaching_projects_router.get_project?; **签名**: ?def get_project(...) -> dict?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.get, get_teaching_project, HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/teaching_projects.py:41::build_teaching_projects_router.save_project?; **签名**: ?def save_project(...) -> dict?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, save_teaching_project, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/teaching_projects.py:48::build_teaching_projects_router.archive_project?; **签名**: ?def archive_project(...) -> dict?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, archive_teaching_project, HTTPException, str; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/teaching_projects.py:58::build_teaching_projects_router.duplicate_project?; **签名**: ?def duplicate_project(...) -> dict?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?dict?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream router.post, duplicate_teaching_project, HTTPException; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?routers/teaching_projects.py:67::build_mcp_teaching_projects_router?; **签名**: ?def build_mcp_teaching_projects_router(...) -> APIRouter?; **职责**: Handles FastAPI route/protocol and delegates.; **输入 ⇒ 输出**: signature parameters => ?APIRouter?; **副作用**: HTTP; **上游 / 下游**: upstream module callers; downstream build_teaching_projects_router; **归属**: backend / Router; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/annotation_repo.py
- **标识**: ?repositories/annotation_repo.py:42::_resolve_db_path?; **签名**: ?def _resolve_db_path(...) -> str?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?str?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream str, default_db_path; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/annotation_repo.py
- **标识**: ?repositories/annotation_repo.py:46::_utc_now?; **签名**: ?def _utc_now(...) -> str?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?str?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream datetime.now(timezone.utc).replace(microsecond=0).isoformat().replace, datetime.now(timezone.utc).replace(microsecond=0).isoformat, datetime.now(timezone.utc).replace, datetime.now; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/annotation_repo.py:56::AnnotationRepository.__init__?; **签名**: ?def __init__(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream _resolve_db_path, self._ensure_table; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/annotation_repo.py:60::AnnotationRepository._get_connection?; **签名**: ?def _get_connection(...) -> sqlite3.Connection?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?sqlite3.Connection?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream connect_db; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/annotation_repo.py:63::AnnotationRepository._ensure_table?; **签名**: ?def _ensure_table(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.execute, conn.commit, logger.exception, self._get_connection; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/annotation_repo.py:72::AnnotationRepository.list_by_question?; **签名**: ?def list_by_question(...) -> list[dict[str, Any]]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[dict[str, Any]]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.execute('\n                SELECT *\n                FROM question_annotations\n                WHERE question_id = ?\n                ORDER BY created_at ASC\n      ; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/annotation_repo.py:85::AnnotationRepository.get?; **签名**: ?def get(...) -> dict[str, Any] | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any] | None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.execute('SELECT * FROM question_annotations WHERE annotation_id = ?', (annotation_id,)).fetchone, dict, self._get_connection, conn.execute; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/annotation_repo.py:93::AnnotationRepository.create?; **签名**: ?def create(...) -> dict[str, Any]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream _utc_now, entry.get, str, closing, conn.execute, conn.commit, self.get, uuid4, self._get_connection; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/annotation_repo.py:124::AnnotationRepository.update?; **签名**: ?def update(...) -> dict[str, Any] | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any] | None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self.get, _utc_now, closing, conn.execute, conn.commit, self._get_connection, merged.get; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/annotation_repo.py:156::AnnotationRepository.delete?; **签名**: ?def delete(...) -> bool?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.execute, conn.commit, self._get_connection; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/annotation_repo.py:165::AnnotationRepository.delete_by_question?; **签名**: ?def delete_by_question(...) -> int?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?int?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.execute, conn.commit, self._get_connection; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/collections.py
- **标识**: ?repositories/collections.py:40::_resolve_db_path?; **签名**: ?def _resolve_db_path(...) -> str?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?str?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream str, default_db_path; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/collections.py:47::CollectionsRepository.__init__?; **签名**: ?def __init__(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream _resolve_db_path, self._ensure_tables; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/collections.py:51::CollectionsRepository._get_connection?; **签名**: ?def _get_connection(...) -> sqlite3.Connection?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?sqlite3.Connection?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream connect_db; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/collections.py:54::CollectionsRepository._ensure_tables?; **签名**: ?def _ensure_tables(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.execute, conn.commit, logger.exception, self._get_connection; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/collections.py:63::CollectionsRepository.list_all?; **签名**: ?def list_all(...) -> list[dict[str, Any]]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[dict[str, Any]]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.execute('\n                SELECT c.*, COALESCE(cq.cnt, 0) AS question_count\n                FROM collections c\n                LEFT JOIN (\n                    SEL; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/collections.py:79::CollectionsRepository.get?; **签名**: ?def get(...) -> dict[str, Any] | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any] | None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.execute('SELECT * FROM collections WHERE id = ?', (collection_id,)).fetchone, dict, self._get_connection, conn.execute; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/collections.py:87::CollectionsRepository.create?; **签名**: ?def create(...) -> dict[str, Any]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.execute, conn.commit, self._get_connection, uuid.uuid4; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/collections.py:100::CollectionsRepository.delete?; **签名**: ?def delete(...) -> bool?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.execute, conn.commit, self._get_connection; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/collections.py:113::CollectionsRepository.add_questions?; **签名**: ?def add_questions(...) -> dict[str, Any]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.commit, len, self._get_connection, conn.execute, results.append, str; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/collections.py:148::CollectionsRepository.remove_questions?; **签名**: ?def remove_questions(...) -> int?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?int?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream ','.join, closing, conn.execute, conn.commit, self._get_connection; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/collections.py
- **标识**: ?repositories/collections.py:163::CollectionsRepository.get_questions?; **签名**: ?def get_questions(...) -> list[str]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[str]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.execute('SELECT question_id FROM collection_questions WHERE collection_id = ?', (collection_id,)).fetchall, self._get_connection, conn.execute; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/collections.py:171::CollectionsRepository.get_collections_for_question?; **签名**: ?def get_collections_for_question(...) -> list[dict[str, Any]]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[dict[str, Any]]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.execute('\n                SELECT c.*\n                FROM collections c\n                INNER JOIN collection_questions cq ON cq.collection_id = c.id\n            ; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/embedding_status.py
- **标识**: ?repositories/embedding_status.py:11::EmbeddingStatusRepository.__init__?; **签名**: ?def __init__(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream none; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/embedding_status.py:14::EmbeddingStatusRepository.list_question_embeddings?; **签名**: ?def list_question_embeddings(...) -> list[EmbeddingStatusItem]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[EmbeddingStatusItem]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream str, closing, connection.execute("\n                SELECT model_name, model_version, vector_type, COUNT(*) AS owner_count,\n                       MAX(updated_at) AS last_updated_; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/favorites.py
- **标识**: ?repositories/favorites.py:39::_resolve_db_path?; **签名**: ?def _resolve_db_path(...) -> Path?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?Path?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream Path, default_db_path; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/favorites.py:46::FavoritesRepository.__init__?; **签名**: ?def __init__(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream _resolve_db_path, self._ensure_tables; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/favorites.py:50::FavoritesRepository._db_available?; **签名**: ?def _db_available(...) -> bool?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._db_path.exists; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/favorites.py:53::FavoritesRepository._get_connection?; **签名**: ?def _get_connection(...) -> sqlite3.Connection?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?sqlite3.Connection?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream connect_db; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/favorites.py:56::FavoritesRepository._ensure_tables?; **签名**: ?def _ensure_tables(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._db_available, closing, conn.execute, conn.commit, logger.exception, self._get_connection; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/favorites.py:69::FavoritesRepository.list_groups?; **签名**: ?def list_groups(...) -> list[dict[str, Any]]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[dict[str, Any]]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._db_available, closing, conn.execute('\n                SELECT g.*, COALESCE(fi.cnt, 0) AS question_count\n                FROM favorite_groups g\n                LEFT JOIN (\; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/favorites.py
- **标识**: ?repositories/favorites.py:87::FavoritesRepository.create_group?; **签名**: ?def create_group(...) -> dict[str, Any]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.execute, conn.commit, self._get_connection, conn.execute('SELECT COALESCE(MAX(sort_order), 0) + 1 FROM favorite_groups').fetchone, uuid.uuid4; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/favorites.py:100::FavoritesRepository.update_group?; **签名**: ?def update_group(...) -> bool?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.execute, conn.commit, self._get_connection; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/favorites.py:109::FavoritesRepository.delete_group?; **签名**: ?def delete_group(...) -> bool?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.execute, conn.commit, self._get_connection; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/favorites.py:122::FavoritesRepository.add_or_update?; **签名**: ?def add_or_update(...) -> bool?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.execute('SELECT question_id FROM favorite_items WHERE question_id = ?', (question_id,)).fetchone, self._get_connection, conn.execute, conn.commit, parts.append, param; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/favorites.py:158::FavoritesRepository.batch_assign?; **签名**: ?def batch_assign(...) -> dict[str, Any]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.commit, len, self._get_connection, conn.execute('SELECT question_id FROM favorite_items WHERE question_id = ?', (qid,)).fetchone, conn.execute, parts.append, params.a; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/favorites.py:200::FavoritesRepository.remove?; **签名**: ?def remove(...) -> int?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?int?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream ','.join, closing, conn.execute, conn.commit, self._get_connection; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/favorites.py:213::FavoritesRepository.get_item?; **签名**: ?def get_item(...) -> dict[str, Any] | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any] | None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._db_available, closing, conn.execute('\n                SELECT fi.*, fg.name AS group_name\n                FROM favorite_items fi\n                LEFT JOIN favorite_groups f; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/favorites.py:228::FavoritesRepository.get_favorites?; **签名**: ?def get_favorites(...) -> tuple[list[dict[str, Any]], int]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?tuple[list[dict[str, Any]], int]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream ' AND '.join, self._db_available, where.append, params.append, closing, conn.execute(f'\n                SELECT fi.*, fg.name AS group_name\n                FROM favorite_items fi\; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/favorites.py:273::FavoritesRepository.get_favorite_ids?; **签名**: ?def get_favorite_ids(...) -> set[str]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?set[str]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._db_available, set, closing, conn.execute('SELECT question_id FROM favorite_items').fetchall, self._get_connection, conn.execute; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/image_catalog.py
- **标识**: ?repositories/image_catalog.py:13::ImageCatalogRepository.__init__?; **签名**: ?def __init__(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream none; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/image_catalog.py
- **标识**: ?repositories/image_catalog.py:16::ImageCatalogRepository.list?; **签名**: ?def list(...) -> list[ImageAssetItem]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[ImageAssetItem]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream str, int, closing, connection.execute("\n                SELECT DISTINCT\n                    ia.asset_id, ia.filename, ia.file_path, ia.paper_id, ia.question_id,\n                ; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/import_tasks.py
- **标识**: ?repositories/import_tasks.py:57::InvalidTaskTransition.__init__?; **签名**: ?def __init__(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream super().__init__, super; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:111::InMemoryImportTaskRepository.__init__?; **签名**: ?def __init__(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream RLock; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:116::InMemoryImportTaskRepository.create?; **签名**: ?def create(...) -> ImportTask?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream datetime.now, ImportTask, deepcopy, str, max, uuid4, int; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:141::InMemoryImportTaskRepository.create_or_get?; **签名**: ?def create_or_get(...) -> tuple[ImportTask, bool]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?tuple[ImportTask, bool]?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream max, self.create, self._tasks.values, deepcopy; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:167::InMemoryImportTaskRepository.find_by_idempotency_key?; **签名**: ?def find_by_idempotency_key(...) -> ImportTask | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask | None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream deepcopy, max, self._tasks.values; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:174::InMemoryImportTaskRepository.claim?; **签名**: ?def claim(...) -> ImportTask | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask | None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._require_task, self.mark_running; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:182::InMemoryImportTaskRepository.get?; **签名**: ?def get(...) -> ImportTask | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask | None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._tasks.get, deepcopy; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:187::InMemoryImportTaskRepository.mark_running?; **签名**: ?def mark_running(...) -> ImportTask?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._transition; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:203::InMemoryImportTaskRepository.mark_retrying?; **签名**: ?def mark_retrying(...) -> ImportTask?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._transition, TaskErrorInfo; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/import_tasks.py
- **标识**: ?repositories/import_tasks.py:225::InMemoryImportTaskRepository.mark_completed?; **签名**: ?def mark_completed(...) -> ImportTask?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._transition, deepcopy; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:241::InMemoryImportTaskRepository.mark_failed?; **签名**: ?def mark_failed(...) -> ImportTask?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._transition, TaskErrorInfo; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:263::InMemoryImportTaskRepository.request_cancel?; **签名**: ?def request_cancel(...) -> ImportTask?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._transition; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:266::InMemoryImportTaskRepository.mark_cancelled?; **签名**: ?def mark_cancelled(...) -> ImportTask?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._transition; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:269::InMemoryImportTaskRepository.update_progress?; **签名**: ?def update_progress(...) -> ImportTask?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._require_task, _normalize_progress, datetime.now, deepcopy, InvalidTaskTransition; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:282::InMemoryImportTaskRepository.heartbeat?; **签名**: ?def heartbeat(...) -> ImportTask?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._require_task, datetime.now, deepcopy, InvalidTaskTransition; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:292::InMemoryImportTaskRepository.set_message_id?; **签名**: ?def set_message_id(...) -> ImportTask?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._require_task, datetime.now, deepcopy; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:299::InMemoryImportTaskRepository.recover_stale_tasks?; **签名**: ?def recover_stale_tasks(...) -> list[ImportTask]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[ImportTask]?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream datetime.now, timedelta, self._tasks.values, max, recovered.append, int, self.mark_failed, self.mark_retrying, last_seen.isoformat; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:329::InMemoryImportTaskRepository.list?; **签名**: ?def list(...) -> list[ImportTask]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[ImportTask]?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream list, tasks.sort, max, deepcopy, self._tasks.values, set, int, _normalize_limit, ValueError, sorted; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:353::InMemoryImportTaskRepository.delete?; **签名**: ?def delete(...) -> bool?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._tasks.pop; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/import_tasks.py
- **标识**: ?repositories/import_tasks.py:357::InMemoryImportTaskRepository.record_action_audit?; **签名**: ?def record_action_audit(...) -> TaskActionAudit?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?TaskActionAudit?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._require_task, TaskActionAudit, self._action_audits.append, deepcopy, str, bool, datetime.now, uuid4; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:384::InMemoryImportTaskRepository.list_action_audits?; **签名**: ?def list_action_audits(...) -> list[TaskActionAudit]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[TaskActionAudit]?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream audits.sort, deepcopy, _normalize_limit; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:390::InMemoryImportTaskRepository._require_task?; **签名**: ?def _require_task(...) -> ImportTask?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._tasks.get, KeyError; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:396::InMemoryImportTaskRepository._transition?; **签名**: ?def _transition(...) -> ImportTask?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._require_task, _validate_transition, datetime.now, deepcopy, _normalize_progress, cast; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:449::SQLiteImportTaskRepository.__init__?; **签名**: ?def __init__(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: file/upload; **上游 / 下游**: upstream module callers; downstream Path(self._db_path).parent.mkdir, self._ensure_schema, str, self._migrate_legacy_tasks, default_review_db_path, Path; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:462::SQLiteImportTaskRepository._connect?; **签名**: ?def _connect(...) -> sqlite3.Connection?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?sqlite3.Connection?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream connect_db; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:465::SQLiteImportTaskRepository._ensure_schema?; **签名**: ?def _ensure_schema(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._connect, conn.execute, ensure_import_task_lifecycle_schema, conn.commit, conn.close, conn.rollback; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:477::SQLiteImportTaskRepository._migrate_legacy_tasks?; **签名**: ?def _migrate_legacy_tasks(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream Path, connect_db, self._connect, source.execute("SELECT name FROM sqlite_master WHERE type = 'table' AND name = 'import_pipeline_tasks'").fetchone, source.execute('\n              ; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:523::SQLiteImportTaskRepository.create?; **签名**: ?def create(...) -> ImportTask?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream datetime.now, ImportTask, self._connect, conn.execute, conn.commit, conn.close, str, deepcopy, max, conn.rollback, uuid4, int, _dt_to_text, json.dumps; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:581::SQLiteImportTaskRepository.create_or_get?; **签名**: ?def create_or_get(...) -> tuple[ImportTask, bool]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?tuple[ImportTask, bool]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._connect, conn.execute, conn.execute('\n                SELECT * FROM import_pipeline_tasks\n                WHERE idempotency_key = ?\n                ORDER BY created_at DES; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/import_tasks.py
- **标识**: ?repositories/import_tasks.py:652::SQLiteImportTaskRepository.find_by_idempotency_key?; **签名**: ?def find_by_idempotency_key(...) -> ImportTask | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask | None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._connect, conn.execute('\n                SELECT * FROM import_pipeline_tasks\n                WHERE idempotency_key = ?\n                ORDER BY created_at DESC, task_id DES; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:668::SQLiteImportTaskRepository.claim?; **签名**: ?def claim(...) -> ImportTask | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask | None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream datetime.now, self._connect, conn.execute, conn.execute('SELECT * FROM import_pipeline_tasks WHERE task_id = ?', (task_id,)).fetchone, conn.commit, _row_to_task, conn.close, conn.e; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:710::SQLiteImportTaskRepository.get?; **签名**: ?def get(...) -> ImportTask | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask | None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._connect, conn.execute('SELECT * FROM import_pipeline_tasks WHERE task_id = ?', (task_id,)).fetchone, conn.close, _row_to_task, conn.execute; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:718::SQLiteImportTaskRepository.mark_running?; **签名**: ?def mark_running(...) -> ImportTask?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._transition; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:734::SQLiteImportTaskRepository.mark_retrying?; **签名**: ?def mark_retrying(...) -> ImportTask?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._transition, TaskErrorInfo; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:756::SQLiteImportTaskRepository.mark_completed?; **签名**: ?def mark_completed(...) -> ImportTask?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._transition, deepcopy; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:772::SQLiteImportTaskRepository.mark_failed?; **签名**: ?def mark_failed(...) -> ImportTask?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._transition, TaskErrorInfo; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:794::SQLiteImportTaskRepository.request_cancel?; **签名**: ?def request_cancel(...) -> ImportTask?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._transition; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:797::SQLiteImportTaskRepository.mark_cancelled?; **签名**: ?def mark_cancelled(...) -> ImportTask?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._transition; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:800::SQLiteImportTaskRepository.update_progress?; **签名**: ?def update_progress(...) -> ImportTask?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream _normalize_progress, datetime.now, self._connect, _row_to_task, conn.execute, conn.execute('SELECT * FROM import_pipeline_tasks WHERE task_id = ?', (task_id,)).fetchone, conn.commi; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/import_tasks.py
- **标识**: ?repositories/import_tasks.py:827::SQLiteImportTaskRepository.heartbeat?; **签名**: ?def heartbeat(...) -> ImportTask?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream datetime.now, self._connect, _row_to_task, conn.execute, conn.execute('SELECT * FROM import_pipeline_tasks WHERE task_id = ?', (task_id,)).fetchone, conn.commit, conn.close, cast, ; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:851::SQLiteImportTaskRepository.set_message_id?; **签名**: ?def set_message_id(...) -> ImportTask?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream datetime.now, self._connect, _row_to_task, conn.execute, conn.execute('SELECT * FROM import_pipeline_tasks WHERE task_id = ?', (task_id,)).fetchone, conn.commit, conn.close, cast, ; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:871::SQLiteImportTaskRepository.recover_stale_tasks?; **签名**: ?def recover_stale_tasks(...) -> list[ImportTask]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[ImportTask]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream datetime.now, self._connect, timedelta, conn.execute, conn.execute("\n                SELECT task_id, attempt, max_attempts,\n                       COALESCE(heartbeat_at, updated_; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:928::SQLiteImportTaskRepository.list?; **签名**: ?def list(...) -> list[ImportTask]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[ImportTask]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream params.extend, self._connect, ','.join, clauses.append, conn.execute(f'\n                SELECT * FROM import_pipeline_tasks\n                {where}\n                ORDER BY upda; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:966::SQLiteImportTaskRepository.delete?; **签名**: ?def delete(...) -> bool?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._connect, conn.execute, conn.commit, conn.close, conn.rollback; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:979::SQLiteImportTaskRepository.record_action_audit?; **签名**: ?def record_action_audit(...) -> TaskActionAudit?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?TaskActionAudit?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream datetime.now, TaskActionAudit, self._connect, conn.execute, conn.execute('SELECT 1 FROM import_pipeline_tasks WHERE task_id = ?', (task_id,)).fetchone, conn.commit, conn.close, str; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:1037::SQLiteImportTaskRepository.list_action_audits?; **签名**: ?def list_action_audits(...) -> list[TaskActionAudit]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[TaskActionAudit]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._connect, conn.execute('\n                SELECT * FROM task_action_audit\n                WHERE task_id = ?\n                ORDER BY created_at DESC, audit_id DESC\n        ; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:1066::SQLiteImportTaskRepository._transition?; **签名**: ?def _transition(...) -> ImportTask?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._connect, _row_to_task, conn.execute, conn.execute('SELECT status FROM import_pipeline_tasks WHERE task_id = ?', (task_id,)).fetchone, str, _validate_transition, datetime.now,; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:1172::SQLiteImportTaskRepository._raise_missing_or_invalid?; **签名**: ?def _raise_missing_or_invalid(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream conn.execute('SELECT status FROM import_pipeline_tasks WHERE task_id = ?', (task_id,)).fetchone, InvalidTaskTransition, KeyError, str, conn.execute; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:1179::_validate_transition?; **签名**: ?def _validate_transition(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream _ALLOWED_TRANSITIONS.get, ValueError, frozenset, InvalidTaskTransition; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/import_tasks.py
- **标识**: ?repositories/import_tasks.py:1187::_normalize_progress?; **签名**: ?def _normalize_progress(...) -> int?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?int?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream int, ValueError; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:1194::_normalize_limit?; **签名**: ?def _normalize_limit(...) -> int?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?int?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream max, min, int; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:1198::_dt_to_text?; **签名**: ?def _dt_to_text(...) -> str?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?str?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream value.astimezone(UTC).isoformat, value.astimezone; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:1202::_dt_from_text?; **签名**: ?def _dt_from_text(...) -> datetime | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?datetime | None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream datetime.fromisoformat, value.replace, parsed.replace; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:1209::_json_dict?; **签名**: ?def _json_dict(...) -> dict[str, Any]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream json.loads, isinstance; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/import_tasks.py:1219::_row_to_task?; **签名**: ?def _row_to_task(...) -> ImportTask?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ImportTask?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream str, ImportTask, TaskErrorInfo, cast, _json_dict, int, max, _dt_from_text, bool; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/knowledge_cache.py
- **标识**: ?repositories/knowledge_cache.py:35::_resolve_db_path?; **签名**: ?def _resolve_db_path(...) -> str?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?str?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream str, default_db_path; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/knowledge_cache.py:42::KnowledgeCacheRepository.__init__?; **签名**: ?def __init__(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream _resolve_db_path, self._ensure_table; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/knowledge_cache.py:46::KnowledgeCacheRepository._get_connection?; **签名**: ?def _get_connection(...) -> sqlite3.Connection?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?sqlite3.Connection?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream connect_db; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/knowledge_cache.py:49::KnowledgeCacheRepository._ensure_table?; **签名**: ?def _ensure_table(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.execute, conn.commit, logger.exception, self._get_connection; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/knowledge_cache.py
- **标识**: ?repositories/knowledge_cache.py:57::KnowledgeCacheRepository.get?; **签名**: ?def get(...) -> dict[str, Any] | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any] | None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.execute('SELECT * FROM knowledge_cache WHERE cache_key = ?', (cache_key,)).fetchone, dict, self._get_connection, conn.execute; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/knowledge_cache.py:65::KnowledgeCacheRepository.upsert?; **签名**: ?def upsert(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.execute, conn.commit, self._get_connection, entry.get; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/knowledge_cache.py:94::KnowledgeCacheRepository.delete?; **签名**: ?def delete(...) -> bool?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.execute, conn.commit, self._get_connection; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/knowledge_cache.py:103::KnowledgeCacheRepository.clear_all?; **签名**: ?def clear_all(...) -> int?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?int?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.execute, conn.commit, self._get_connection; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/knowledge_points.py
- **标识**: ?repositories/knowledge_points.py:14::KnowledgePointRepository.__init__?; **签名**: ?def __init__(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream none; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/knowledge_points.py:17::KnowledgePointRepository.question_counts_by_topic3?; **签名**: ?def question_counts_by_topic3(...) -> dict[str, int]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, int]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream str, closing, connection.execute('\n                SELECT topic3_id, COUNT(DISTINCT question_id) AS count\n                FROM question_knowledge_points\n                GROUP BY; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/knowledge_points.py:29::KnowledgePointRepository.list?; **签名**: ?def list(...) -> list[KnowledgePointItem]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[KnowledgePointItem]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream str, closing, connection.execute("\n                SELECT\n                    topic3_id, topic3_name, topic2_id, topic2_name,\n                    topic1_id, topic1_name, source_; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/knowledge_points.py:76::KnowledgePointRepository.list_for_question?; **签名**: ?def list_for_question(...) -> list[QuestionKnowledgePointLink] | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[QuestionKnowledgePointLink] | None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream str, closing, connection.execute('SELECT 1 FROM questions WHERE question_id = ?', (question_id,)).fetchone, connection.execute('\n                SELECT rank, topic1_id, topic1_nam; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/knowledge_points.py:96::KnowledgePointRepository.replace_for_question?; **签名**: ?def replace_for_question(...) -> list[QuestionKnowledgePointLink] | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[QuestionKnowledgePointLink] | None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self.list_for_question, str, closing, self._ensure_active_topics, default_db_path, connect_db, self._question_exists, connection.execute, connection.executemany, connection.commit,; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/knowledge_points.py:118::KnowledgePointRepository.upsert_for_question?; **签名**: ?def upsert_for_question(...) -> QuestionKnowledgePointLink | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?QuestionKnowledgePointLink | None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream str, closing, self._ensure_active_topics, connection.execute('SELECT rank, topic1_id, topic1_name, topic2_id, topic2_name, topic3_id,\n                topic3_name, source_chapter, ; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/knowledge_points.py
- **标识**: ?repositories/knowledge_points.py:147::KnowledgePointRepository.delete_for_question?; **签名**: ?def delete_for_question(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream str, closing, connection.execute, connection.commit, default_db_path, connect_db; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/knowledge_points.py:154::KnowledgePointRepository._question_exists?; **签名**: ?def _question_exists(...) -> bool?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream connection.execute('SELECT 1 FROM questions WHERE question_id = ?', (question_id,)).fetchone, connection.execute; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/knowledge_points.py:158::KnowledgePointRepository._ensure_active_topics?; **签名**: ?def _ensure_active_topics(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream ','.join, sorted, str, LookupError, connection.execute(f"SELECT topic3_id FROM knowledge_points WHERE topic3_id IN ({placeholders}) AND status = 'active'", topic3_ids).fetchall, se; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/paper_drafts.py
- **标识**: ?repositories/paper_drafts.py:18::_json_loads?; **签名**: ?def _json_loads(...) -> Any?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?Any?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream json.loads; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/paper_drafts.py:27::_json_dumps?; **签名**: ?def _json_dumps(...) -> str?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?str?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream json.dumps; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/paper_drafts.py:31::_make_id?; **签名**: ?def _make_id(...) -> str?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?str?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream uuid.uuid4; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/paper_drafts.py:36::PaperDraftRepository.__init__?; **签名**: ?def __init__(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream str, default_db_path; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/paper_drafts.py:39::PaperDraftRepository._get_connection?; **签名**: ?def _get_connection(...) -> sqlite3.Connection?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?sqlite3.Connection?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream connect_db; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/paper_drafts.py:42::PaperDraftRepository.list?; **签名**: ?def list(...) -> list[dict[str, Any]]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[dict[str, Any]]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.execute('\n                SELECT draft_id, title, subtitle, source, status, question_count,\n                       item_count, total_score, created_at, updated_at\n; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/paper_drafts.py:56::PaperDraftRepository.get?; **签名**: ?def get(...) -> dict[str, Any] | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any] | None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream _summary_from_row, _json_loads, closing, conn.execute('\n                SELECT *\n                FROM paper_drafts\n                WHERE draft_id = ?\n                ', (draft_; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/paper_drafts.py
- **标识**: ?repositories/paper_drafts.py:85::PaperDraftRepository.get_latest?; **签名**: ?def get_latest(...) -> dict[str, Any] | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any] | None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self.get, closing, conn.execute('\n                SELECT draft_id\n                FROM paper_drafts\n                ORDER BY updated_at DESC\n                LIMIT 1\n          ; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/paper_drafts.py:99::PaperDraftRepository.upsert?; **签名**: ?def upsert(...) -> dict[str, Any]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self.get, _make_id, closing, conn.execute, conn.execute('SELECT updated_at FROM paper_drafts WHERE draft_id = ?', (resolved_id,)).fetchone, conn.commit, RuntimeError, datetime.now(; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/paper_drafts.py:199::PaperDraftRepository.delete?; **签名**: ?def delete(...) -> bool?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.execute, conn.commit, self._get_connection; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/paper_drafts.py:206::_summary_from_row?; **签名**: ?def _summary_from_row(...) -> dict[str, Any]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream none; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/paper_drafts.py:221::_item_from_row?; **签名**: ?def _item_from_row(...) -> dict[str, Any]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream _json_loads; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/papers.py
- **标识**: ?repositories/papers.py:13::PaperRepository.__init__?; **签名**: ?def __init__(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream none; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/papers.py:16::PaperRepository.list?; **签名**: ?def list(...) -> list[PaperSummary]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[PaperSummary]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream str, closing, connection.execute("\n                SELECT p.paper_id, p.year, p.exam_type, p.region, p.paper_name,\n                       p.subject, p.status, COUNT(qn.question_i; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/papers.py:48::PaperRepository.get?; **签名**: ?def get(...) -> PaperDetail | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?PaperDetail | None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream str, closing, connection.execute('\n                SELECT p.paper_id, p.year, p.exam_type, p.region, p.paper_name, p.subject,\n                       p.status, p.source_path, p.so; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/papers.py:65::PaperRepository.list_questions?; **签名**: ?def list_questions(...) -> list[PaperQuestionSummary]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[PaperQuestionSummary]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream str, closing, connection.execute('\n                SELECT question_id, canonical_title, module, topic2, topic3, difficulty,\n                       question_type AS type, status, ; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/processing_runs.py
- **标识**: ?repositories/processing_runs.py:13::ProcessingRunRepository.__init__?; **签名**: ?def __init__(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream none; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/processing_runs.py
- **标识**: ?repositories/processing_runs.py:16::ProcessingRunRepository.list?; **签名**: ?def list(...) -> list[dict[str, Any]]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[dict[str, Any]]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream str, closing, connection.execute('\n                SELECT\n                    run_id,\n                    pipeline_name,\n                    pipeline_version,\n                ; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/question_details.py
- **标识**: ?repositories/question_details.py:11::_decode_list?; **签名**: ?def _decode_list(...) -> list[str]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[str]?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream json.loads, isinstance, str; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_details.py:21::_decode_json_list?; **签名**: ?def _decode_json_list(...) -> list?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream json.loads, isinstance; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_details.py:32::QuestionDetailRepository.__init__?; **签名**: ?def __init__(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream none; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_details.py:35::QuestionDetailRepository.get?; **签名**: ?def get(...) -> QuestionDetail | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?QuestionDetail | None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream dict, _decode_list, _decode_json_list, QuestionDetail.model_validate, str, closing, connection.execute('\n                SELECT q.question_id, q.canonical_title, q.module, q.topic; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_details.py:74::QuestionDetailRepository.get_write_payload?; **签名**: ?def get_write_payload(...) -> dict | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict | None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream dict, _decode_json_list, str, closing, connection.execute('\n                SELECT q.question_id, q.question_type, q.difficulty, q.module AS knowledge_point,\n                    ; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_details.py:139::QuestionDetailRepository.list_assets?; **签名**: ?def list_assets(...) -> list[QuestionAsset]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[QuestionAsset]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream str, closing, connection.execute('\n                SELECT qa.link_id, qa.asset_id, qa.role, qa.sort_order, qa.placeholder_key,\n                       qa.is_primary, qa.is_verifie; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/question_imports.py
- **标识**: ?repositories/question_imports.py:16::QuestionImportRepository.__init__?; **签名**: ?def __init__(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream str, default_db_path; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_imports.py:19::QuestionImportRepository.create?; **签名**: ?def create(...) -> dict[str, Any]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream str(content.get('stem') or '').strip, str(classification.get('question_type') or '').strip, ValueError, closing, connection.execute('SELECT MAX(CAST(SUBSTR(question_id, 2) AS INTEG; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_imports.py:87::QuestionImportRepository.delete?; **签名**: ?def delete(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, connection.execute, connection.commit, connect_db; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/question_imports.py
- **标识**: ?repositories/question_imports.py:93::QuestionImportRepository._link_knowledge_points?; **签名**: ?def _link_knowledge_points(...) -> tuple[int, list[str]]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?tuple[int, list[str]]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream str(point.get('topic3_id') or '').strip, connection.execute('SELECT 1 FROM knowledge_points WHERE topic3_id = ?', (topic3_id,)).fetchone, int, connection.execute, skipped.append, s; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_imports.py:125::QuestionImportRepository._link_images?; **签名**: ?def _link_images(...) -> int?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?int?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream str(image.get('asset_id') or '').strip, connection.execute('SELECT 1 FROM image_assets WHERE asset_id = ?', (asset_id,)).fetchone, connection.execute, str, image.get, int; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_imports.py:157::_build_text?; **签名**: ?def _build_text(...) -> str?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?str?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream parts.extend, '\n'.join, content.get, parts.append, item.get; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/question_reviews.py
- **标识**: ?repositories/question_reviews.py:12::QuestionReviewRepository.__init__?; **签名**: ?def __init__(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream str, default_db_path; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_reviews.py:15::QuestionReviewRepository.get_status?; **签名**: ?def get_status(...) -> str | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?str | None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, connection.execute('SELECT status FROM questions WHERE question_id = ?', (question_id,)).fetchone, str, connect_db, connection.execute; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_reviews.py:22::QuestionReviewRepository.set_status?; **签名**: ?def set_status(...) -> bool?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, connection.execute, connection.commit, connect_db; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_reviews.py:31::QuestionReviewRepository.get_context?; **签名**: ?def get_context(...) -> dict | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict | None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, connection.execute('\n                SELECT q.question_id, q.module, q.topic2, q.topic3, q.status,\n                       q.primary_paper_id, q.primary_question_no, q.va; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_reviews.py:46::QuestionReviewRepository.list_rejected?; **签名**: ?def list_rejected(...) -> list[dict]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[dict]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, connection.execute('\n                SELECT question_id, module, topic2, topic3, status, primary_paper_id,\n                       primary_question_no, vault_markdown_pat; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_reviews.py:61::QuestionReviewRepository.update_stem_and_status?; **签名**: ?def update_stem_and_status(...) -> bool?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, connection.execute('SELECT 1 FROM questions WHERE question_id = ?', (question_id,)).fetchone, connection.execute, connection.commit, connect_db; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/question_search.py
- **标识**: ?repositories/question_search.py:20::_resolve_db_path?; **签名**: ?def _resolve_db_path(...) -> Path?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?Path?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream default_db_path, Path; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/question_search.py
- **标识**: ?repositories/question_search.py:26::_demo_data_enabled?; **签名**: ?def _demo_data_enabled(...) -> bool?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream os.getenv('PHYSICS_ALLOW_DEMO_DATA', '').strip().lower, os.getenv('PHYSICS_ALLOW_DEMO_DATA', '').strip, os.getenv; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_search.py:39::_row_to_dict?; **签名**: ?def _row_to_dict(...) -> dict[str, Any]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream row.keys; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_search.py:43::_parse_json_list?; **签名**: ?def _parse_json_list(...) -> list[Any]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[Any]?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream json.loads, isinstance; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_search.py:256::QuestionSearchRepository.__init__?; **签名**: ?def __init__(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream _resolve_db_path, _demo_data_enabled, logger.warning, self._has_searchable_questions; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_search.py:266::QuestionSearchRepository._has_searchable_questions?; **签名**: ?def _has_searchable_questions(...) -> bool?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._db_path.exists, closing, conn.execute("\n                    SELECT COUNT(*) AS count\n                    FROM sqlite_master\n                    WHERE type = 'table' AND na; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_search.py:285::QuestionSearchRepository._refresh_mock_state?; **签名**: ?def _refresh_mock_state(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._has_searchable_questions, logger.info; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_search.py:298::QuestionSearchRepository.db_path?; **签名**: ?def db_path(...) -> Path?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?Path?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream none; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_search.py:301::QuestionSearchRepository._get_connection?; **签名**: ?def _get_connection(...) -> sqlite3.Connection?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?sqlite3.Connection?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._refresh_mock_state, sqlite3.connect, logger.debug, self._db_path.is_file, QuestionDatabaseUnavailableError, connect_db; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_search.py:327::QuestionSearchRepository.search_questions?; **签名**: ?def search_questions(...) -> tuple[list[dict[str, Any]], int]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?tuple[list[dict[str, Any]], int]?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._refresh_mock_state, self._sqlite_search, self._mock_search; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_search.py:397::QuestionSearchRepository._mock_search?; **签名**: ?def _mock_search(...) -> tuple[list[dict[str, Any]], int]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?tuple[list[dict[str, Any]], int]?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream list, len, query.lower, any, item.get, bool, (item['canonical_title'] or '').lower, (item.get('answer_text') or '').lower, (item.get('analysis_text') or '').lower, _MOCK_KNOWLEDGE_; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/question_search.py
- **标识**: ?repositories/question_search.py:458::QuestionSearchRepository._sqlite_search?; **签名**: ?def _sqlite_search(...) -> tuple[list[dict[str, Any]], int]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?tuple[list[dict[str, Any]], int]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream _add, _add_knowledge, any, bool, self._fts_available, where.append, params.append, closing, conn.execute(sql, params + [limit, offset]).fetchall, self._fetch_knowledge_points, self; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_search.py:550::QuestionSearchRepository._sqlite_search._add?; **签名**: ?def _add(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream where.append, params.append; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_search.py:555::QuestionSearchRepository._sqlite_search._add_knowledge?; **签名**: ?def _add_knowledge(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream where.append, params.append; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_search.py:695::QuestionSearchRepository.get_questions_by_ids?; **签名**: ?def get_questions_by_ids(...) -> list[dict[str, Any]]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[dict[str, Any]]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream list, self._refresh_mock_state, ','.join, dict.fromkeys, closing, conn.execute(sql, ordered_ids + ordered_ids).fetchall, self._fetch_knowledge_points, _row_to_dict, knowledge_map.g; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_search.py:774::QuestionSearchRepository.embedding_index_revision?; **签名**: ?def embedding_index_revision(...) -> tuple[int, str]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?tuple[int, str]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._refresh_mock_state, closing, conn.execute("\n                SELECT COUNT(*) AS vector_count, COALESCE(MAX(updated_at), '') AS last_updated_at\n                FROM embedding; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_search.py:800::QuestionSearchRepository.embedding_index_health?; **签名**: ?def embedding_index_health(...) -> dict[str, Any]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._refresh_mock_state, closing, conn.execute("\n                SELECT\n                    COUNT(*) AS question_count,\n                    SUM(CASE WHEN e.status = 'ready' THE; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_search.py:848::QuestionSearchRepository.load_question_embeddings?; **签名**: ?def load_question_embeddings(...) -> list[dict[str, Any]]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[dict[str, Any]]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._refresh_mock_state, closing, conn.execute("\n                SELECT owner_id, dimensions, vector_json, updated_at\n                FROM embeddings\n                WHERE owne; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_search.py:875::QuestionSearchRepository.load_method_features?; **签名**: ?def load_method_features(...) -> list[dict[str, Any]] | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[dict[str, Any]] | None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._refresh_mock_state, ','.join, _row_to_dict, closing, conn.execute(f'\n                    SELECT question_id, method_id, branch, level, score,\n                           mat; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_search.py:902::QuestionSearchRepository._fts_available?; **签名**: ?def _fts_available(...) -> bool?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, conn.execute("\n                SELECT 1\n                FROM sqlite_master\n                WHERE type = 'table' AND name = 'question_search_fts'\n                ").fet; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_search.py:917::QuestionSearchRepository._fetch_knowledge_points?; **签名**: ?def _fetch_knowledge_points(...) -> dict[str, list[dict[str, Any]]]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, list[dict[str, Any]]]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream ','.join, conn.execute(f'\n                SELECT\n                    question_id, rank,\n                    topic1_id, topic1_name,\n                    topic2_id, topic2_name,\; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/question_search.py
- **标识**: ?repositories/question_search.py:954::QuestionSearchRepository.get_facets?; **签名**: ?def get_facets(...) -> dict[str, Any]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._refresh_mock_state, self._sqlite_facets, self._mock_facets; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_search.py:961::QuestionSearchRepository._mock_facets?; **签名**: ?def _mock_facets(...) -> dict[str, Any]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream sorted; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_search.py:979::QuestionSearchRepository._sqlite_facets?; **签名**: ?def _sqlite_facets(...) -> dict[str, Any]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream closing, _fetch_distinct, self._get_connection, conn.execute('SELECT DISTINCT year FROM papers WHERE year IS NOT NULL ORDER BY year DESC').fetchall, conn.execute; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_search.py:1017::_fetch_distinct?; **签名**: ?def _fetch_distinct(...) -> list[str]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[str]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream conn.execute(sql).fetchall, conn.execute; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_search.py:1021::_build_fts_query?; **签名**: ?def _build_fts_query(...) -> str?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?str?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream query.strip().replace, query.strip; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/question_stats.py
- **标识**: ?repositories/question_stats.py:12::QuestionStatsRepository.__init__?; **签名**: ?def __init__(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream none; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_stats.py:15::QuestionStatsRepository.status_counts?; **签名**: ?def status_counts(...) -> dict[str, int]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, int]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream str, closing, connection.execute('SELECT status, COUNT(*) AS count FROM questions GROUP BY status').fetchall, int, default_db_path, connect_db, connection.execute; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/question_write.py
- **标识**: ?repositories/question_write.py:24::_resolve_db_path?; **签名**: ?def _resolve_db_path(...) -> Path?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?Path?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream default_db_path, Path; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_write.py:38::QuestionWriteRepository.__init__?; **签名**: ?def __init__(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream _resolve_db_path; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_write.py:42::QuestionWriteRepository.db_path?; **签名**: ?def db_path(...) -> Path?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?Path?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream none; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/question_write.py
- **标识**: ?repositories/question_write.py:49::QuestionWriteRepository._get_connection?; **签名**: ?def _get_connection(...) -> sqlite3.Connection?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?sqlite3.Connection?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream connect_db, self._ensure_version_table, self._db_path.exists, FileNotFoundError; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_write.py:57::QuestionWriteRepository._ensure_version_table?; **签名**: ?def _ensure_version_table(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream conn.execute; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_write.py:79::QuestionWriteRepository.upsert_many?; **签名**: ?def upsert_many(...) -> dict[str, int]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, int]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream len, closing, self._get_connection, conn.commit, _ensure_import_batch, conn.execute('SELECT question_id FROM questions WHERE question_id = ?', (qid,)).fetchone, conn.execute('SELEC; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_write.py:313::QuestionWriteRepository.find_question_ids_by_content_hashes?; **签名**: ?def find_question_ids_by_content_hashes(...) -> dict[str, list[str]]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, list[str]]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream list, ','.join, dict.fromkeys, closing, conn.execute(f'\n                SELECT content_hash, question_id\n                FROM questions\n                WHERE content_hash IN ({p; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_write.py:338::QuestionWriteRepository.delete_many?; **签名**: ?def delete_many(...) -> dict[str, Any]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream list, ','.join, dict.fromkeys, closing, conn.execute(f'SELECT question_id FROM questions WHERE question_id IN ({placeholders})', ordered_ids).fetchall, len, self._get_connection, s; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_write.py:385::QuestionWriteRepository.return_to_review?; **签名**: ?def return_to_review(...) -> dict[str, Any]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream ReviewQueueRepository(migrate_legacy=False).enqueue_rework, self._db_available, FileNotFoundError, (reason or '题目需要回炉重造').strip, closing, conn.execute('SELECT question_id FROM ques; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_write.py:440::QuestionWriteRepository._delete_question_bindings?; **签名**: ?def _delete_question_bindings(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream _table_exists, conn.execute, _table_has_column; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_write.py:505::QuestionWriteRepository._db_available?; **签名**: ?def _db_available(...) -> bool?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream self._db_path.exists; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_write.py:509::QuestionWriteRepository.ensure_metadata_columns?; **签名**: ?def ensure_metadata_columns(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._db_available, closing, conn.commit, self._get_connection, conn.execute; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_write.py:521::QuestionWriteRepository.get_question_metadata?; **签名**: ?def get_question_metadata(...) -> dict[str, dict[str, str | None]]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, dict[str, str | None]]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream ','.join, closing, conn.execute(f'\n                SELECT q.question_id, q.module AS knowledge_point,\n                       qti.tags_json, qti.source_text\n                FROM ; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/question_write.py
- **标识**: ?repositories/question_write.py:548::QuestionWriteRepository.update_question_metadata?; **签名**: ?def update_question_metadata(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._db_available, closing, updates.get, conn.commit, self._get_connection, conn.execute, conn.execute('SELECT 1 FROM question_text_index WHERE question_id = ?', (question_id,)).f; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_write.py:597::QuestionWriteRepository.update_analysis?; **签名**: ?def update_analysis(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._db_available, closing, conn.execute, conn.commit, self._get_connection; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_write.py:616::QuestionWriteRepository.ensure_mistake_column?; **签名**: ?def ensure_mistake_column(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._db_available, closing, conn.commit, self._get_connection, conn.execute; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_write.py:629::QuestionWriteRepository.set_mistake_status?; **签名**: ?def set_mistake_status(...) -> bool?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._db_available, closing, conn.execute, conn.commit, self._get_connection; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_write.py:642::QuestionWriteRepository.batch_set_mistake_status?; **签名**: ?def batch_set_mistake_status(...) -> dict[str, int]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, int]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._db_available, closing, conn.commit, len, self._get_connection, conn.execute; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_write.py:668::QuestionWriteRepository.get_mistake_question_ids?; **签名**: ?def get_mistake_question_ids(...) -> list[str]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[str]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._db_available, closing, conn.execute('SELECT question_id FROM questions WHERE is_mistake = 1 ORDER BY updated_at DESC').fetchall, self._get_connection, conn.execute; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_write.py:682::QuestionWriteRepository.list_versions?; **签名**: ?def list_versions(...) -> list[dict[str, Any]]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[dict[str, Any]]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._db_available, closing, conn.execute('\n                SELECT version_id, question_id, version_number,\n                       change_summary, modified_by, source, created_at; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_write.py:699::QuestionWriteRepository.get_version?; **签名**: ?def get_version(...) -> dict[str, Any] | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any] | None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream dict, self._db_available, closing, conn.execute('\n                SELECT version_id, question_id, version_number,\n                       snapshot_json, change_summary, modified_b; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_write.py:723::QuestionWriteRepository.get_latest_version_number?; **签名**: ?def get_latest_version_number(...) -> int?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?int?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._db_available, closing, conn.execute('SELECT MAX(version_number) AS max_v FROM question_versions WHERE question_id = ?', (question_id,)).fetchone, self._get_connection, conn.e; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_write.py:737::_ensure_import_batch?; **签名**: ?def _ensure_import_batch(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream str(import_batch_id or '').strip, conn.execute, str; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/question_write.py
- **标识**: ?repositories/question_write.py:770::_table_exists?; **签名**: ?def _table_exists(...) -> bool?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream conn.execute("SELECT 1 FROM sqlite_master WHERE type IN ('table', 'view') AND name = ?", (table_name,)).fetchone, conn.execute; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_write.py:778::_table_has_column?; **签名**: ?def _table_has_column(...) -> bool?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream conn.execute(f'PRAGMA table_info({table_name})').fetchall, any, conn.execute; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/question_write.py:783::_capture_version_snapshot?; **签名**: ?def _capture_version_snapshot(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream conn.execute('\n        SELECT question_id, question_type, difficulty, module AS knowledge_point,\n               status, primary_question_no, import_batch_id AS source\n        FR; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/review_drafts.py
- **标识**: ?repositories/review_drafts.py:22::ReviewDraftConflictError.__init__?; **签名**: ?def __init__(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream super().__init__, super; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/review_drafts.py:30::SQLiteReviewDraftRepository.__init__?; **签名**: ?def __init__(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: file/upload; **上游 / 下游**: upstream module callers; downstream max, Path(self._db_path).parent.mkdir, self._ensure_schema, str, min, default_review_db_path, int, Path; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/review_drafts.py:36::SQLiteReviewDraftRepository._ensure_schema?; **签名**: ?def _ensure_schema(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream sqlite3.connect, conn.execute; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/review_drafts.py:66::SQLiteReviewDraftRepository.get?; **签名**: ?def get(...) -> ReviewDraftSnapshot | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ReviewDraftSnapshot | None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream sqlite3.connect, conn.execute('SELECT task_id, version, state_json, updated_at FROM review_workbench_drafts WHERE task_id = ?', (task_id,)).fetchone, _row_to_snapshot, conn.execute; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/review_drafts.py:75::SQLiteReviewDraftRepository.save?; **签名**: ?def save(...) -> ReviewDraftSnapshot?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ReviewDraftSnapshot?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream datetime.now, json.dumps, ReviewDraftSnapshot, sqlite3.connect, conn.execute, conn.execute('SELECT task_id, version, state_json, updated_at FROM review_workbench_drafts WHERE task_; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/review_drafts.py:122::SQLiteReviewDraftRepository.list_versions?; **签名**: ?def list_versions(...) -> list[ReviewDraftSnapshot]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[ReviewDraftSnapshot]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream max, min, sqlite3.connect, conn.execute('\n                SELECT task_id, version, state_json, created_at AS updated_at\n                FROM review_workbench_draft_versions\n    ; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/review_drafts.py:138::SQLiteReviewDraftRepository.restore?; **签名**: ?def restore(...) -> ReviewDraftSnapshot | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ReviewDraftSnapshot | None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream _row_to_snapshot, self.save, sqlite3.connect, conn.execute('\n                SELECT task_id, version, state_json, created_at AS updated_at\n                FROM review_workbench_d; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/review_drafts.py
- **标识**: ?repositories/review_drafts.py:154::SQLiteReviewDraftRepository.delete?; **签名**: ?def delete(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream sqlite3.connect, conn.execute; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/review_drafts.py:160::_dt_to_text?; **签名**: ?def _dt_to_text(...) -> str?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?str?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream value.astimezone(UTC).isoformat, value.astimezone; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/review_drafts.py:164::_dt_from_text?; **签名**: ?def _dt_from_text(...) -> datetime?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?datetime?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream datetime.fromisoformat, value.replace; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/review_drafts.py:168::_row_to_snapshot?; **签名**: ?def _row_to_snapshot(...) -> ReviewDraftSnapshot?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?ReviewDraftSnapshot?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream ReviewDraftSnapshot, json.loads, str, isinstance, int, _dt_from_text; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/review_queue.py
- **标识**: ?repositories/review_queue.py:22::ReviewQueueRepository.__init__?; **签名**: ?def __init__(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream Path, default_review_db_path, default_db_path, self._db_path.parent.mkdir, self._ensure_schema, self.migrate_legacy, self.delete_orphan_question_rows, ValueError; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/review_queue.py:44::ReviewQueueRepository.list?; **签名**: ?def list(...) -> list[dict[str, Any]]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[dict[str, Any]]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream min, params.append, max, where_parts.append, self._connect, conn.execute(f'\n                SELECT review_id, entity_type, entity_id, queue_type, status,\n                       p; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/review_queue.py:76::ReviewQueueRepository.enqueue_rework?; **签名**: ?def enqueue_rework(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream json.dumps, self._connect, conn.execute, conn.commit; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/review_queue.py:106::ReviewQueueRepository.create?; **签名**: ?def create(...) -> str?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?str?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._connect, conn.execute, conn.commit, json.dumps, uuid.uuid4; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/review_queue.py:138::ReviewQueueRepository.delete?; **签名**: ?def delete(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._connect, conn.execute, conn.commit; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/review_queue.py:143::ReviewQueueRepository.list_for_question?; **签名**: ?def list_for_question(...) -> list[dict[str, Any]]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[dict[str, Any]]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._connect, conn.execute("\n                SELECT review_id, entity_type, entity_id, queue_type, status,\n                       priority, reason, payload_json, created_at, upd; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/review_queue.py
- **标识**: ?repositories/review_queue.py:157::ReviewQueueRepository.list_by_type_status?; **签名**: ?def list_by_type_status(...) -> list[dict[str, Any]]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[dict[str, Any]]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._connect, conn.execute('\n                SELECT review_id, entity_type, entity_id, queue_type, status,\n                       priority, reason, payload_json, created_at, upd; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/review_queue.py:171::ReviewQueueRepository.get?; **签名**: ?def get(...) -> dict[str, Any] | None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any] | None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._connect, conn.execute('\n                SELECT review_id, entity_type, entity_id, queue_type, status,\n                       priority, reason, payload_json, created_at, upd; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/review_queue.py:184::ReviewQueueRepository.update_status?; **签名**: ?def update_status(...) -> bool?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._connect, conn.execute, conn.commit; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/review_queue.py:193::ReviewQueueRepository.migrate_legacy?; **签名**: ?def migrate_legacy(...) -> int?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?int?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream int, self.apply_legacy_migration; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/review_queue.py:200::ReviewQueueRepository.preview_legacy_migration?; **签名**: ?def preview_legacy_migration(...) -> dict[str, int | bool]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, int | bool]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._legacy_rows, self._db_path.exists, target_rows.get, len, self._legacy_db_path.exists, self._connect, str, _review_row_values, target.execute('\n                        SELECT; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/review_queue.py:247::ReviewQueueRepository.apply_legacy_migration?; **签名**: ?def apply_legacy_migration(...) -> dict[str, int | bool]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?dict[str, int | bool]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self.preview_legacy_migration, self._legacy_rows, RuntimeError, int, ValueError, self._connect, target.executemany, target.commit, sqlite3.connect, legacy.execute, legacy.commit, _; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/review_queue.py:290::ReviewQueueRepository._legacy_rows?; **签名**: ?def _legacy_rows(...) -> list[sqlite3.Row]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?list[sqlite3.Row]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream sqlite3.connect, source.execute('\n                SELECT review_id, entity_type, entity_id, queue_type, status,\n                       priority, reason, payload_json, created_at,; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/review_queue.py:306::ReviewQueueRepository.delete_orphan_question_rows?; **签名**: ?def delete_orphan_question_rows(...) -> int?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?int?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream self._legacy_db_path.exists, self._connect, review_conn.execute("\n                SELECT review_id, entity_id\n                FROM review_queue\n                WHERE entity_type; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/review_queue.py:355::ReviewQueueRepository._connect?; **签名**: ?def _connect(...) -> sqlite3.Connection?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?sqlite3.Connection?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream sqlite3.connect, self._ensure_schema, self._db_path.as_posix; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/review_queue.py:365::ReviewQueueRepository._ensure_schema?; **签名**: ?def _ensure_schema(...) -> None?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?None?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream sqlite3.connect, active.execute, active.commit, active.close; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: repositories/review_queue.py
- **标识**: ?repositories/review_queue.py:403::_table_exists?; **签名**: ?def _table_exists(...) -> bool?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?bool?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream conn.execute("SELECT 1 FROM sqlite_master WHERE type = 'table' AND name = ?", (table_name,)).fetchone, conn.execute; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/review_queue.py:411::_review_row_values?; **签名**: ?def _review_row_values(...) -> tuple[Any, ...]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?tuple[Any, ...]?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream none; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
- **标识**: ?repositories/review_queue.py:426::_review_ids?; **签名**: ?def _review_ids(...) -> set[str]?; **职责**: Executes persistence/query/result mapping.; **输入 ⇒ 输出**: signature parameters => ?set[str]?; **副作用**: database read/write; **上游 / 下游**: upstream module callers; downstream str, conn.execute('SELECT review_id FROM review_queue').fetchall, conn.execute; **归属**: backend / Repository; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: schemas/lesson_exports.py
- **标识**: ?schemas/lesson_exports.py:19::LessonExportRequest.validate_lesson_package?; **签名**: ?def validate_lesson_package(...) -> dict[str, Any]?; **职责**: Validates or serializes schema values.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream field_validator, str(value.get('id') or '').strip, ValueError, isinstance, value.get, str; **归属**: backend / Schema; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: schemas/lesson_reflections.py
- **标识**: ?schemas/lesson_reflections.py:42::LessonReflectionUpsertRequest.validate_reflection?; **签名**: ?def validate_reflection(...) -> dict[str, Any]?; **职责**: Validates or serializes schema values.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream field_validator, str(value.get('id') or '').strip, ValueError, str(value.get('projectId') or '').strip, str, value.get; **归属**: backend / Schema; **优化提示**: preserve contract; extract only pure helper logic.
### Source file: schemas/teaching_projects.py
- **标识**: ?schemas/teaching_projects.py:58::TeachingProjectUpsertRequest.validate_project?; **签名**: ?def validate_project(...) -> dict[str, Any]?; **职责**: Validates or serializes schema values.; **输入 ⇒ 输出**: signature parameters => ?dict[str, Any]?; **副作用**: none/in-memory; **上游 / 下游**: upstream module callers; downstream field_validator, str(value.get('id') or '').strip, ValueError, isinstance, str, value.get; **归属**: backend / Schema; **优化提示**: preserve contract; extract only pure helper logic.
## Schema validation inventory
- **schemas/agents.py**: AgentConfig (BaseModel); AgentConfigResponse (BaseModel); AgentConfigUpdateRequest (BaseModel); AgentTestResponse (BaseModel); ReviewLatexCleanupRequest (BaseModel); ReviewLatexCleanupResponse (BaseModel); AgentAction (BaseModel); AgentTraceStep (BaseModel); AgentStreamEvent (BaseModel); QuestionPickerAgentRequest (BaseModel); QuestionPickerAgentResponse (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/ai_assistant.py**: AiAssistantMessage (BaseModel); AiAssistantChatRequest (BaseModel); AiAssistantQuestionContext (BaseModel); CompositionSuggestion (BaseModel); AiAssistantChatResponse (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/ai_generation.py**: QuestionSummary (BaseModel); GenerateAnalysisRequest (BaseModel); GenerateAnalysisResponse (BaseModel); GenerateKnowledgeRequest (BaseModel); GenerateKnowledgeResponse (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/analysis_batch.py**: BatchGenerateAnalysisRequest (BaseModel); BatchAnalysisItemResult (BaseModel); BatchGenerateAnalysisResponse (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/assets_manager.py**: AssetItem (BaseModel); AssetStats (BaseModel); AssetBatchSummary (BaseModel); AssetPagination (BaseModel); AssetListResponse (BaseModel); CleanupPreviewResponse (BaseModel); CacheCleanupPreviewResponse (BaseModel); CacheCleanupRequest (BaseModel); DuplicateAssetGroup (BaseModel); StorageAnalysisResponse (BaseModel); CleanupResponse (BaseModel); DeleteAssetResponse (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/collections.py**: CollectionNode (BaseModel); CreateCollectionRequest (BaseModel); CreateCollectionResponse (BaseModel); BatchMoveRequest (BaseModel); BatchMoveItemResult (BaseModel); BatchMoveResponse (BaseModel); RemoveFromCollectionRequest (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/embedding_builds.py**: EmbeddingBuildRequest (BaseModel); EmbeddingBuildResponse (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/embedding_status.py**: EmbeddingStatusItem (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/favorites.py**: FavoriteGroupCreate (BaseModel); FavoriteGroupUpdate (BaseModel); FavoriteGroupItem (BaseModel); FavoriteAssignRequest (BaseModel); BatchStarRequest (BaseModel); FavoriteItemView (BaseModel); BatchFavoriteResponse (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/image_catalog.py**: ImageAssetItem (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/image_management.py**: QuestionImageDetail (BaseModel); ImageListResponse (BaseModel); AddImageRequest (BaseModel); AddCachedImageRequest (BaseModel); CachedImageAsset (BaseModel); CacheUploadResponse (BaseModel); ReplaceImageRequest (BaseModel); UpdateImageRequest (BaseModel); ReorderRequest (BaseModel); PlaceholderIssue (BaseModel); ValidationResponse (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/import_pipeline.py**: ConvertDocumentRequest (BaseModel); UploadImportFileResponse (BaseModel); ImportMediaAsset (BaseModel); ImportBatchResponse (BaseModel); PandocBatchResponse (BaseModel); AiCleanBatchResponse (BaseModel); AiStructureBatchResponse (BaseModel); RecognizeBatchResponse (BaseModel); ExtractBatchImagesResponse (BaseModel); AiRefineBatchRequest (BaseModel); AiRefineBatchResponse (BaseModel); DraftMetadataRequest (BaseModel); DraftMetadataResponse (BaseModel); ConfirmBatchQuestionsRequest (BaseModel); ConfirmBatchQuestionsResponse (BaseModel); AiGeneratedReviewRequest (BaseModel); AiGeneratedReviewResponse (BaseModel); ReviewTaskListItem (BaseModel); ReviewTaskListResponse (BaseModel); DeleteReviewTaskResponse (BaseModel); ImportBatchStatusResponse (BaseModel); CleanDocumentRequest (BaseModel); ParseStructuredQuestionsRequest (BaseModel); TaskErrorResponse (BaseModel); ImportPipelineTaskResponse (BaseModel); ConvertDocumentResponse (BaseModel); CleanDocumentResponse (BaseModel); ParseStructuredQuestionsResponse (BaseModel); AiParseDocumentRequest (BaseModel); AiParseDocumentResponse (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/knowledge_points.py**: KnowledgePointItem (BaseModel); QuestionKnowledgePointLink (BaseModel); QuestionKnowledgePointUpsert (BaseModel); QuestionKnowledgePointBatchItem (QuestionKnowledgePointUpsert). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/lesson_documents.py**: _SavedHandoutResponseBase (BaseModel); SavedHandoutSummary (BaseModel); SavedHandoutVersion (BaseModel); SavedHandoutListResponse (BaseModel); SavedHandoutVersionListResponse (BaseModel); SavedHandoutResponse (_SavedHandoutResponseBase); SavedHandoutUpsertRequest (BaseModel); SavedHandoutRenameRequest (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/lesson_exports.py**: LessonExportRequest (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/lesson_reflections.py**: LessonReflectionItem (BaseModel); LessonReflectionListResponse (BaseModel); LessonReflectionResponse (BaseModel); LessonReflectionUpsertRequest (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/mcp_api.py**: QuestionOptionPayload (BaseModel); QuestionFigurePayload (BaseModel); SubQuestionPayload (BaseModel); StandardQuestionPayload (BaseModel); ParseDocumentRequest (BaseModel); DetectQuestionRegionsRequest (BaseModel); ParseQuestionRegionRequest (BaseModel); GenerateAnalysisRequest (BaseModel); GenerateKnowledgeRequest (BaseModel); MetadataTaskItemPayload (BaseModel); MetadataConstraintsPayload (BaseModel); GenerateMetadataRequest (BaseModel); McpStatusResponse (BaseModel); McpTaskResponse (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/mcp_runtime.py**: McpTestConnectionRequest (BaseModel); McpTestConnectionResponse (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/metadata_batch.py**: ManualValues (BaseModel); BatchMetadataRequest (BaseModel); BatchMetadataItemResult (BaseModel); BatchMetadataResponse (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/mistake.py**: MarkMistakeRequest (BaseModel); BatchMarkMistakeRequest (BaseModel); MistakeItemResult (BaseModel); BatchMarkMistakeResponse (BaseModel); MistakeStatsResponse (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/paper_drafts.py**: PaperDraftItem (BaseModel); PaperDraftUpsertRequest (BaseModel); PaperDraftSummary (BaseModel); PaperDraftResponse (PaperDraftSummary); PaperDraftListResponse (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/papers.py**: PaperSummary (BaseModel); PaperDetail (PaperSummary); PaperQuestionSummary (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/processing_runs.py**: ProcessingRunItem (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/question_details.py**: QuestionDetail (BaseModel); QuestionAsset (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/question_imports.py**: ImportQuestionRequest (BaseModel); ImportQuestionResponse (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/question_reviews.py**: ReviewActionRequest (BaseModel); ReviewActionResponse (BaseModel); ProposeFixRequest (BaseModel); ProposeFixResponse (BaseModel); ReportIssueRequest (BaseModel); ReportIssueResponse (BaseModel); ReviewQueueItem (BaseModel); PendingFixItem (BaseModel); RejectedQuestionItem (BaseModel); FixDetailResponse (BaseModel); DecideFixRequest (BaseModel); DecideFixResponse (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/question_search.py**: SearchMode (str, Enum); QuestionSearchParams (BaseModel); QuestionItem (BaseModel); FacetBlock (BaseModel); SearchResponse (BaseModel); BatchQuestionFetchRequest (BaseModel); BatchQuestionFetchResponse (BaseModel); BatchQuestionDeleteRequest (BaseModel); BatchQuestionDeleteResponse (BaseModel); ReturnQuestionToReviewRequest (BaseModel); ReturnQuestionToReviewResponse (BaseModel); FilterFacetsResponse (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/question_variants.py**: SourceQuestionPayload (BaseModel); GenerateQuestionVariantsRequest (BaseModel); GeneratedVariantQuestion (BaseModel); GenerateQuestionVariantsResponse (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/question_versions.py**: QuestionVersionSummary (BaseModel); QuestionVersionDetail (QuestionVersionSummary); VersionRollbackRequest (BaseModel); VersionRollbackResponse (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/question_write.py**: QuestionRecord (BaseModel); QuestionBatchWriteResult (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/restore_package.py**: RestorePackageResponse (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/review_save.py**: ReviewedQuestionPayload (BaseModel); SaveReviewedQuestionsRequest (BaseModel); ReviewedKnowledgePayload (BaseModel); SaveReviewedKnowledgeRequest (BaseModel); SaveResultItem (BaseModel); SaveReviewedQuestionsResponse (BaseModel); SaveReviewedKnowledgeResponse (BaseModel); ReviewDraftState (BaseModel); SaveReviewDraftRequest (BaseModel); RestoreReviewDraftRequest (BaseModel); ReviewDraftResponse (BaseModel); ReviewDraftLookupResponse (BaseModel); ReviewDraftVersionListResponse (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/similar_questions.py**: SimilarQuestionItem (BaseModel); SimilarQuestionsResponse (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/tasks.py**: TaskErrorInfo (BaseModel); TaskCenterItem (BaseModel); TaskCenterListResponse (BaseModel); TaskActionResponse (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
- **schemas/teaching_projects.py**: _TeachingProjectResponseBase (BaseModel); TeachingProjectSummary (BaseModel); TeachingProjectListResponse (BaseModel); TeachingProjectResponse (_TeachingProjectResponseBase); TeachingProjectUpsertRequest (BaseModel); TeachingProjectArchiveRequest (BaseModel); TeachingProjectDuplicateRequest (BaseModel). Responsibility: Pydantic request/response parsing, default values, and field constraints; no HTTP/database/file side effect.
<!-- FUNCTION_RECORDS -->

## AST / rg 交叉核对

```powershell
rg -n "^\s*(async\s+)?def\s+" apps/api/src/physics_vault_api/*.py apps/api/src/physics_vault_api/routers apps/api/src/physics_vault_api/repositories apps/api/src/physics_vault_api/schemas
```

本次 `rg` 命中 568 个声明行，AST 计为 569 个函数：同一物理行存在多个定义时，AST 才能完整区分。Router 的端点函数嵌套在 `build_*_router` 内，因此以 AST 深度遍历为准。
